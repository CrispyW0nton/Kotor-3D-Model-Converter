# Changes

This file records completed fixes and software changes so future agents can see what has already been done and avoid repeating work.

For each completed change, add a dated entry with:

- The task ID when one applies, such as `T302`
- A short summary of what changed
- The files or area affected
- The verification performed, such as tests, MCP comparisons, or manual checks

## 2026-05-24

- T1403 follow-up: Cleaned up Visual Profile dock layouts so workflow profiles only surface relevant docks. Animation now opens scene/content plus animation/property/node tools; Mesh Editing opens scene/content plus mesh/pivot/property tools; Lighting and Cinegraphics open scene plus lighting/camera/property tools; Clean opens no tool docks. The layout applier now hides detachable docks that a newly selected profile marks invisible, so stale docks from the previous profile do not linger.
  Affected areas: `config/themes/layouts/profile_*.xml`, `src/gui/libtheme/layout_applier.py`, `tests/test_theme_layout_loading.py`.
  Verification: `python -m py_compile src/gui/libtheme/layout_applier.py tests/test_theme_layout_loading.py`; `pytest tests/test_theme_layout_loading.py -q`; `pytest tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_content_browser_panel.py -q`; `python tools/validate_themes.py`; launched the actual Qt main window and cycled Animation, Mesh Editing, Lighting, Cinegraphics, and Clean profiles, saving screenshots and dock visibility summary under `artifacts/visual_profiles_smoke/`.
  Follow-up: Fixed Compact and Cinematic layouts inheriting default-visible optional detachable docks. Nodes, 2DA Browser, and Resource Browser are now hidden when a layout does not explicitly declare those panels, preventing the three-dock stack in the bottom-left corner. Verification: focused optional-dock regression test, full `tests/test_theme_layout_loading.py`, Content Browser/main-window routing smoke, `python tools/validate_themes.py`, and real Qt Compact/Cinematic smoke saved under `artifacts/compact_cinematic_layout_smoke/`.

- T1403 follow-up: Improved the unified Content Browser layout and animation controls. The navigation tree and asset detail/action panel now stack together in a left sidebar, asset filter dropdowns have visible labels, and animation preview actions include a Stop button wired through the existing playback stop route.
  Affected areas: `src/gui/panels/qt_content_browser_panel.py`, `src/gui/windows/qt_main_window.py`, `tests/test_content_browser_panel.py`.
  Verification: `python -m py_compile src/gui/panels/qt_content_browser_panel.py src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py -q`; `pytest tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser -q`; launched the actual app with `python main.py` and captured the visible Content Browser at `artifacts/actual_content_browser_layout_smoke.png`.

- Tuned the main command strip and viewport toolbar polish: command-strip icon buttons now use the same 30x22 button footprint as viewport toolbar buttons, dock-module shortcuts are grouped on the right side of the strip, the ambiguous Scene dock shortcut is now labelled `Scene Information`, the duplicate animation-library shortcut was removed from the strip, and viewport toolbar rows are centered. Sequence Editor and Diagnostics now register with the detachable dock system so they can open as dockable floating workspace windows. Roadmap reference: no specific task ID assigned.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/assets/qt_theme.py`, `src/gui/libtheme/qt_stylesheet_builder.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/assets/qt_theme.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py::test_main_command_strip_groups_dock_modules_on_right_and_sizes_like_viewport tests/test_core_contracts.py::test_viewport_toolbar_flow_layout_centers_rows tests/test_core_contracts.py::test_sequence_and_diagnostics_use_detachable_dock_registry tests/test_core_contracts.py::test_main_window_moves_utility_tabs_to_tools_windows tests/test_core_contracts.py::test_main_window_command_bar_is_fixed_and_docks_are_scrollable tests/test_core_contracts.py::test_viewport_and_character_builder_toolbars_are_scrollable -q --basetemp .pytest_tmp_toolbar_dock_batch`; `python -m pytest tests/test_theme_layout_loading.py -q --basetemp .pytest_tmp_theme_toolbar_dock_batch`; `python -m pytest tests/test_content_browser_panel.py -q --basetemp .pytest_tmp_content_dock_batch`; `python -m pytest tests/test_sequence_editor_core.py -q --basetemp .pytest_tmp_sequence_dock_batch`; launched `QtGhostRiggerMainWindow` directly on the desktop with Default theme selected, verified command/viewport button sizes, centered viewport-toolbar geometry, and Sequence/Diagnostics detachable hosts, and captured `artifacts/actual_qt_window_toolbar_dock_batch_main_visible.png`.
- Follow-up: changed the Sequence Editor toolbar action to auto-dock into the main window on first open while preserving the detachable dock title-bar/context-menu behavior for later undocking.
  Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py::test_sequence_and_diagnostics_use_detachable_dock_registry tests/test_sequence_editor_core.py -q --basetemp .pytest_tmp_sequence_autodock`; launched `QtGhostRiggerMainWindow` directly on the desktop and verified `sequence_dock_visible=True`, `sequence_dock_floating=False`, no sequence floating host, and bottom dock placement; captured `artifacts/actual_qt_window_sequence_autodocked.png`.

- Disabled the main command-strip overflow now that the strip uses compact icon buttons. The scene title stays on the left, the visual profile selector remains pinned to the right, and viewport toolbar overflow behavior is unchanged. Roadmap reference: no specific task ID assigned.
  Affected areas: `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`, `tests/test_theme_layout_loading.py`.
  Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_core_contracts.py tests/test_theme_layout_loading.py`; `python -m pytest tests/test_core_contracts.py::test_main_window_command_bar_is_fixed_and_docks_are_scrollable tests/test_core_contracts.py::test_viewport_and_character_builder_toolbars_are_scrollable tests/test_content_browser_panel.py::test_main_window_exposes_visual_profile_dropdown tests/test_theme_layout_loading.py::test_main_window_reserves_fixed_command_bar_height -q --basetemp .pytest_tmp_command_strip_no_overflow`; `python -m pytest tests/test_theme_layout_loading.py -q --basetemp .pytest_tmp_theme_command_strip_no_overflow`; launched the actual app with `python main.py`, Default theme selected, and captured the settled main window at `artifacts/actual_app_default_command_strip_no_overflow_settled.png`.

- Clarified agent testing policy so MCP and backend probes are reserved for backend/model-pipeline validation, while UI, startup, viewport, theme/layout, and workflow checks must launch and test the actual GhostRigger application on screen. Added `AGENTS.md` to `.gitignore` as a local agent-instruction file. Roadmap reference: no specific task ID assigned.
  Affected areas: `AGENTS.md`, `.gitignore`.
  Verification: `rg -n "MCP|GUI does NOT|show the application|headless|widget screenshots|launch the actual|backend" AGENTS.md`; `Select-String -Path .gitignore -Pattern '^AGENTS\.md$'`.

- Fixed the Level Editor embedded viewport toolbar theming so the standalone Module/Level Editor follows the same native/default theme path as the main app. The embedded viewport toolbar wrapper, scroll-area viewport, embedded `ViewportToolbar`, and spacer gap now use the live application palette in Default/native mode instead of forcing the XML light toolbar colours. Roadmap reference: no specific task ID assigned.
  Affected areas: `src/gui/panels/module_editor/module_editor_viewport_panel.py`, `src/gui/windows/module_editor_window.py`.
  Verification: `python -m py_compile src/gui/panels/module_editor/module_editor_viewport_panel.py src/gui/windows/module_editor_window.py`; `python -m pytest tests/test_theme_layout_loading.py tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable -q --basetemp .pytest_tmp_module_native_viewport_palette`; launched the actual app with `python main.py`, Default theme selected, opened the Level Editor from the real main-window UI, and captured `artifacts/actual_app_default_level_editor_toolbar_fixed.png`.

## 2026-05-23

- Refactored the main command strip so the active scene title is anchored at the far left before command buttons, the visual profile/layout selector is pinned at the far right, and the dense command buttons scroll independently. Converted the command-strip controls to fixed icon toolbar buttons matching the viewport toolbar below, added a folder-plus New Scene button next to Open Scene, removed the vertical separator lines, preserved command names and shortcuts in tooltips, and added full-colour SVG icon fallbacks for the visible main-strip commands. Roadmap reference: no specific task ID assigned.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/libtheme/layout_applier.py`, `src/gui/libtheme/qt_stylesheet_builder.py`, `src/gui/libtheme/icon_manager.py`, `src/gui/assets/qt_icon_manager.py`, `src/gui/assets/qt_theme.py`, `src/gui/icons/*.svg`.
  Verification: `python -m py_compile src/gui/windows/qt_main_window.py src/gui/libtheme/qt_stylesheet_builder.py src/gui/libtheme/layout_applier.py src/gui/libtheme/icon_manager.py src/gui/assets/qt_icon_manager.py src/gui/assets/qt_theme.py`; `python -m pytest tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable tests/test_content_browser_panel.py::test_main_window_exposes_visual_profile_dropdown tests/test_theme_layout_loading.py::test_main_window_reserves_command_bar_overflow_height -q --basetemp .pytest_tmp_command_strip_fullcolor`; `python -m pytest tests/test_theme_layout_loading.py -q --basetemp .pytest_tmp_theme_command_strip_fullcolor`; visible Qt startup screenshot saved to `artifacts/command_strip_fullcolor_no_separators.png`.

- Fixed the Animation Retargeting Workbench FBX source animation list path. Imported `SourceSkeletonClip` objects now carry available FBX action/clip metadata, the Blender UE5 extraction script records every imported action, and the source-clip preview adapter exposes those clips as selectable animation rows in the workbench. Selecting a different FBX action reloads that action by name before running the existing Unreal->KOTOR retarget preview, and the workbench now seeds the source viewport with the selected clip's first sampled pose. Roadmap reference: T2406/T2408.
  Affected areas: `scripts/blender_extract_ue5_animation.py`, `src/core/retargeting/source_animation.py`, `src/core/retargeting/fbx_importer.py`, `src/gui/windows/qt_source_clip_preview_model.py`, `src/gui/panels/qt_animation_panel.py`, `src/gui/windows/qt_retarget_window.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_external_import.py`, `tests/test_ue_fbx_source_import.py`.
  Verification: MCP ground truth refreshed for PMBAM before touching retarget viewport/preview code; `python -m py_compile src/core/retargeting/source_animation.py src/core/retargeting/fbx_importer.py scripts/blender_extract_ue5_animation.py src/gui/windows/qt_source_clip_preview_model.py src/gui/panels/qt_animation_panel.py src/gui/windows/qt_retarget_window.py src/gui/windows/qt_main_window.py tests/test_retarget_external_import.py tests/test_ue_fbx_source_import.py`; `python -m pytest tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_retarget_source_anim_list`; `python -m pytest tests/test_ue_fbx_source_import.py -q --basetemp .pytest_tmp_ue_fbx_available_clips`.

- Added a Blender-backed FBX mesh preview/import option for external FBX files that the legacy `pyassimp`/`assimp_py`/`trimesh` generic mesh importer cannot load. The main viewport FBX import now asks the user to choose either Autodesk FBX SDK or Blender FBX explicitly; it does not silently fall back between them. The Animation Retargeting Workbench source preview can combine the sampled UE/FBX animation skeleton with renderable mesh geometry from the same FBX. The provided `M_Neutral_Stand_Idle_Loop_export.fbx` was verified to contain one mesh and one action; the new importer extracted 27,972 preview vertices and 9,324 triangles. Autodesk FBX SDK remains a manually installed optional dependency and is explicitly documented as non-vendored due to licensing. Roadmap reference: T2406/T2408.
  Affected areas: `scripts/blender_extract_fbx_mesh.py`, `src/converters/blender_fbx_mesh_importer.py`, `src/converters/mesh_converter.py`, `src/gui/windows/qt_source_clip_preview_model.py`, `src/gui/windows/qt_retarget_window.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_external_import.py`, `BACKEND_REGISTRY.md`, `.gitignore`.
  Verification: MCP ground truth refreshed for PMBAM before continuing viewport/model-loading changes; Blender inspection of `C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx` found `mesh_count=1` and action `root|Unreal Take|Base Layer`; real import probe through `import_fbx_mesh_with_blender()` loaded one mesh with 27,972 vertices and 9,324 triangles; `python -m py_compile scripts/blender_extract_fbx_mesh.py src/converters/blender_fbx_mesh_importer.py src/converters/mesh_converter.py src/gui/windows/qt_source_clip_preview_model.py src/gui/windows/qt_retarget_window.py src/gui/windows/qt_main_window.py tests/test_retarget_external_import.py`; `python -m pytest tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_retarget_external_mesh`; `python -m pytest tests/test_fbx_backend_registry.py tests/test_ue_fbx_source_import.py tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_fbx_mesh_smoke`.

- Added an explicit FBX backend registry and setup record so the Retarget Workbench has a documented, non-ambiguous backend strategy. Blender 4.2 headless remains the stable production animation importer/exporter, while Autodesk FBX SDK is represented as an optional secondary backend that probes `import fbx` and `FbxManager.Create()` in the active Python runtime before it can be used. Added backend documentation noting that the current shell uses `C:\Python314\python.exe`, `import fbx` is not installed there, and the Qt runtime should be verified separately before SDK-backed UI choices are enabled. Roadmap reference: T2408.
  Affected areas: `src/core/retargeting/fbx_backend.py`, `src/core/retargeting/fbx_importer.py`, `src/converters/mesh_converter.py`, `BACKEND_REGISTRY.md`, `tests/test_fbx_backend_registry.py`.
  Verification: `python --version`; `python -c "import sys; print(sys.executable); print(sys.version)"`; `python -c "import sys; print(f'Python Version: {sys.version}'); print(f'Python Executable: {sys.executable}'); import fbx"` confirmed the `fbx` module is not installed in the current shell; `python -m pytest tests/test_fbx_backend_registry.py -q --basetemp .pytest_tmp_fbx_backend_registry`; `python -m pytest tests/test_ue_fbx_source_import.py tests/test_retarget_external_import.py tests/test_fbx_backend_registry.py -q --basetemp .pytest_tmp_fbx_backend_smoke`; `python -m py_compile src/core/retargeting/fbx_backend.py src/core/retargeting/fbx_importer.py src/converters/mesh_converter.py tests/test_fbx_backend_registry.py`.

- Fixed the Animation Retargeting Workbench FBX source viewport integration. Imported UE/FBX source animation clips now build a lightweight dummy-node preview model for the workbench source viewport, preserving imported node names/casing, parent hierarchy, rest/global positions, prepared render bounds, and bone/joint overlay visibility. The import handler now passes the loaded `SourceSkeletonClip` to the workbench window instead of only updating metadata labels, so animation-only FBX clips no longer leave the source viewport on "Empty Scene." Roadmap reference: T2406.
  Affected areas: `src/gui/windows/qt_source_clip_preview_model.py`, `src/gui/windows/qt_retarget_window.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `tests/test_retarget_external_import.py`, `tests/test_retarget_workbench_ui_placement.py`.
  Verification: MCP ground truth refreshed for PMBAM before touching viewport/rendering code; `python -m pytest tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_retarget_clip_view`; `python -m pytest tests/test_retarget_workbench_ui_placement.py -q --basetemp .pytest_tmp_retarget_clip_window`; `python -m pytest tests/test_retarget_external_import.py tests/test_retarget_workbench_ui_placement.py tests/test_ghostrigger_retarget_workbench_controller.py -q --basetemp .pytest_tmp_retarget_clip_view_smoke`; `python -m py_compile src/gui/windows/qt_source_clip_preview_model.py src/gui/windows/qt_retarget_window.py src/gui/windows/qt_main_window.py src/gui/qt_lib.py tests/test_retarget_external_import.py tests/test_retarget_workbench_ui_placement.py`; real import probe for `C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx` loaded 87 source nodes / 303 sampled poses and produced an 88-node viewport preview model with prepared render bounds.

- Wired the production UE/FBX source-animation import backend into the Retarget Workbench. `import_ue_fbx_animation_clip()` now defaults to a headless Blender bridge that imports binary FBX files, evaluates armature pose matrices through Blender, converts the extracted payload into the existing `SourceSkeletonClip` contract, preserves parent hierarchy and rest matrices, and records Blender axis/unit conversion metadata. This reuses the MCP/automation extraction path instead of maintaining a separate GUI-only importer. Roadmap reference: T2405.
  Affected areas: `src/core/retargeting/fbx_importer.py`, `tests/test_ue_fbx_source_import.py`.
  Verification: `python -m pytest tests/test_ue_fbx_source_import.py tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_fbx_import_backend`; `python -m py_compile src/core/retargeting/fbx_importer.py tests/test_ue_fbx_source_import.py`; real import probe for `C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx` loaded clip `root|Unreal Take|Base Layer` with 87 nodes, 303 sampled poses, duration `10.033333333333333`, and Blender-evaluated transform metadata.

- Fixed the Animation Retargeting Workbench external FBX import route so selecting a UE/FBX source clip no longer trips the legacy `core` package import path in the generic mesh converter. `mesh_converter` now imports KOTOR model structures from the canonical `src.core.geometry.model_data` module, and the workbench routes Unreal->KOTOR source `.fbx` files directly to the source-clip importer before attempting generic mesh conversion. KOTOR->Unreal target `.fbx` files likewise route to the Unreal skeleton importer first, giving an actionable backend message instead of a misleading mesh-import failure. Roadmap reference: T2405.
  Affected areas: `src/converters/mesh_converter.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_external_import.py`.
  Verification: `python -m pytest tests/test_retarget_external_import.py -q --basetemp .pytest_tmp_retarget_external_import`; `python -m pytest tests/test_retarget_external_import.py tests/test_retarget_workbench_ui_placement.py tests/test_ghostrigger_retarget_workbench_controller.py -q --basetemp .pytest_tmp_retarget_external_import_smoke`; `python -m py_compile src/converters/mesh_converter.py src/gui/windows/qt_main_window.py tests/test_retarget_external_import.py`; manual import probe for `C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx` confirms the old `No module named 'core'` failure is gone and the request reaches the FBX retarget backend gate.

- Fixed the reverse-retarget animation bake so mapped UE/source world-space bone motion is preserved as Aurora local orientation controller motion instead of being flattened when source parent and child tracks move together. The R3.B export path now compares important mapped source tracks against the generated animation block before writing/installing, with gates for root/pelvis/spine/torso/arm/hand/leg motion amplitude. Added regressions for the parent-child cancellation case and for rejecting a flattened arm export. Rebuilt and installed a new custom-patch PMBAM `kpmwin1` candidate after the amplitude gate passed. Roadmap reference: T2402/T2403 custom animation patch workflow.
  Affected areas: `src/core/retargeting/aurora_animation_writer.py`, `tests/test_aurora_animation_writer.py`.
  Verification: `python -m pytest tests/test_aurora_animation_writer.py tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_motion_amp_gate`; `python -m py_compile src/core/retargeting/aurora_animation_writer.py src/core/mdl/mdl_writer.py src/core/validation/animation_block_validator.py tests/test_aurora_animation_writer.py tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py`; rebuilt `exports/manual_override_test_20260523_kpmwin1_motion_amp/pmbam.mdl/.mdx`; installed it to `C:\Program Files (x86)\Steam\steamapps\common\swkotor\Override\pmbam.mdl/.mdx` with `.motionamp_20260523T084730.bak` backups; installed readback verified `kpmwin1` length `10.066666603088379`, raw traversal `61/61`, depth-first order PASS, motion amplitude gate PASS, source/export amplitude ratios `1.000` for pelvis, torso, upper torso, collars, biceps, forearms, and hands, and MDX byte preservation.

- Added explicit UI/workbench boundary rules and expanded the Animation Retargeting Workbench source/target selectors. `AGENTS.md` now requires each roadmap feature to identify its owning studio/window and keep persistent workflow controls inside that surface. The retarget panel now provides searchable game-library dropdowns for both Source and Target, `Load from Game Library` buttons that load the selected dropdown row directly, and `Import External File...` buttons for FBX/OBJ/GLB/GLTF source or target models. The retarget workbench window relays those source/target game-library and external-import requests to the main shell service layer, and its Preview button now routes through the newer `RetargetWorkbenchController` preview path while stamping the selected KOTOR source animation when relevant. Roadmap reference: T2405.
  Affected areas: `AGENTS.md`, `src/gui/panels/qt_animation_panel.py`, `src/gui/windows/qt_retarget_window.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_workbench_ui_placement.py`.
  Verification: `python -m pytest tests/test_retarget_workbench_ui_placement.py -q --basetemp .pytest_tmp_retarget_ui_placement`; `python -m pytest tests/test_retarget_workbench_ui_placement.py tests/test_qt_only_imports.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_retarget_workbench_readiness.py -q --basetemp .pytest_tmp_retarget_selector_smoke`; `python -m pytest tests -q -k "retarget or qt_only or resource" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_retarget_selector_targeted`; `python -m py_compile src/gui/panels/qt_animation_panel.py src/gui/windows/qt_retarget_window.py src/gui/windows/qt_main_window.py tests/test_retarget_workbench_ui_placement.py`.

- Fixed the Retarget Workbench UI placement regression where tri-mode retarget controls and readiness text were rendered in the main viewport header/command bar. The mode selector, output naming controls, and readiness labels now live inside the `Animation Retargeting Workbench` window, while the main shell only synchronizes that window's controls with the retarget controller. Added a regression test that fails if those workflow controls are constructed by the main viewport header/command bar again. Roadmap reference: T2405.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/windows/qt_retarget_window.py`, `tests/test_retarget_workbench_ui_placement.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_workbench_ui_placement.py -q --basetemp .pytest_tmp_retarget_ui_placement`; `python -m pytest tests/test_retarget_workbench_ui_placement.py tests/test_qt_only_imports.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_retarget_workbench_readiness.py -q --basetemp .pytest_tmp_retarget_ui_placement_smoke`; `python -m py_compile src/gui/windows/qt_main_window.py src/gui/windows/qt_retarget_window.py tests/test_retarget_workbench_ui_placement.py`.

- Added the first provider-backed Qt resource browser model slice. `ResourceRecordTableModel` exposes `GameResourceProvider` records with stable columns for resref, type, game, module, layer, source, and size, plus roles for the exact `ResourceAddress`, original record, stable key, layer, source, and resource identity. `ResourceRecordFilterProxyModel` adds model/view filtering by text, restype, layer, module, game, and local-file inclusion while preserving exact resref casing. This starts T2305 without adding a large visible panel yet. Roadmap reference: T2305.
  Affected areas: `src/gui/panels/qt_resource_browser_model.py`, `src/gui/qt_lib.py`, `tests/test_qt_resource_browser_model.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_qt_resource_browser_model.py -q --basetemp .pytest_tmp_qt_resource_browser_model`; `python -m pytest tests/test_qt_resource_browser_model.py tests/test_game_resource_provider.py tests/test_qt_only_imports.py -q --basetemp .pytest_tmp_resource_browser_smoke`; `python -m pytest tests -q -k "resource or retarget or project or validation or export" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_resource_browser_targeted`; `python -m py_compile src/gui/panels/qt_resource_browser_model.py tests/test_qt_resource_browser_model.py src/gui/qt_lib.py src/core/resources/game_resource_provider.py`.

- Fixed the animation-only MDL injection writer so appended local animation blocks serialize animation nodes in vanilla depth-first order: node header, child pointer array, child subtrees, then controller data. The raw export gate now simulates the `UpdateAnimFootprint` child traversal by reading child count at `+0x30`, child pointer array at `+0x2c`, validating child node offsets/cycles, checking declared traversal count for new exports, and requiring child subtrees to start immediately after the child array. Added a regression comparing the new PMBAM `kpmwin1` block against stock `S_Female02.f2a2` serialization order. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `src/core/validation/animation_block_validator.py`, `src/core/retargeting/aurora_animation_writer.py`, `tests/test_mdl_animation_override_roundtrip.py`, `tests/test_animation_export_slot_gate.py`.
  Verification: MCP ground truth refreshed for PMBAM, `S_Female02`, and Ghidra `UpdateAnimFootprint`; `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_anim_depth_order_gate`; `python -m py_compile src/core/mdl/mdl_writer.py src/core/validation/animation_block_validator.py src/core/retargeting/aurora_animation_writer.py tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py`; rebuilt custom-patch PMBAM `kpmwin1` as `exports/manual_override_test_20260523_kpmwin1_depthfirst/pmbam.mdl/.mdx`, installed it to `C:\Program Files (x86)\Steam\steamapps\common\swkotor\Override\pmbam.mdl/.mdx` with `.depthfirst_20260523T081041.bak` backups, and verified installed readback `kpmwin1` length `10.066666603088379`, raw traversal `61/61`, depth-first order PASS, and MDX byte preservation against vanilla PMBAM.

- Added the read-only `GameResourceProvider` foundation so Character, Retarget, Module, Map, and future MCP/Qt resource surfaces can resolve KOTOR resources through one `ResourceAddress`-backed interface instead of per-studio ad hoc lookup. The new provider layer includes normalized queries, provenance records, shadowed-resource reporting, in-memory/project resources, local-file resources, composite lookup, and an adapter over the existing fast `ResourceManager`, plus module-hydration-compatible resource listing/reading helpers. The roadmap now marks T2304 complete and moves the immediate architecture focus to provider-backed Qt resource browser models, ValidationBus UI, undo, and job/progress infrastructure. Roadmap reference: T2304.
  Affected areas: `src/core/resources/__init__.py`, `src/core/resources/game_resource_provider.py`, `tests/test_game_resource_provider.py`, `knowledge_base/roadmap/04_full_suite_completion_roadmap_20260522.md`, `docs/audits/ghostrigger_development_roadmap_recommendations.md`, `.gitignore`.
  Verification: `python -m pytest tests/test_game_resource_provider.py -q --basetemp .pytest_tmp_game_resource_provider`; `python -m pytest tests/test_game_resource_provider.py tests/test_ghostrigger_project_model.py tests/test_validation_bus.py tests/test_export_job.py -q --basetemp .pytest_tmp_game_resource_provider_smoke`; `python -m pytest tests -q -k "retarget or project or validation or export or resource" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_resource_provider_targeted`; `python -m py_compile src/core/resources/__init__.py src/core/resources/game_resource_provider.py tests/test_game_resource_provider.py`; `git diff --check`.

- Added the initial `KOTOR → Unreal` Retarget Workbench pipeline. The new mode samples a KOTOR/Aurora source animation through the existing sampler, retargets it onto an Unreal target skeleton contract, bakes a UE-compatible animation clip with in-place root policy and rest-preserved twist/IK/helper bones, and exports the exact preview through ExportJob using an FBX backend interface. When no backend is configured, export fails before writing with an actionable backend message. The Qt workbench now exposes a UE output clip-name field and treats all three retarget directions as product flows. Roadmap reference: T2404/T2405.
  Affected areas: `src/core/retargeting/unreal_target_skeleton.py`, `src/core/retargeting/kotor_to_unreal_solver.py`, `src/core/retargeting/kotor_to_unreal_preview.py`, `src/core/retargeting/ue_fbx_exporter.py`, `src/core/retargeting/retarget_modes.py`, `src/core/retargeting/retarget_output_naming.py`, `src/core/retargeting/retarget_workbench_readiness.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `src/gui/windows/qt_main_window.py`, `tests/test_kotor_to_unreal_preview_mode.py`, `tests/test_kotor_to_unreal_export.py`, `tests/test_ghostrigger_kotor_to_unreal_workbench_controller.py`.
  Verification: `python -m pytest tests/test_kotor_to_unreal_preview_mode.py tests/test_kotor_to_unreal_export.py tests/test_ghostrigger_kotor_to_unreal_workbench_controller.py -q --basetemp .pytest_tmp_kotor_to_unreal_slice`; `python -m pytest tests/test_kotor_animation_source_sampler.py tests/test_retarget_output_naming.py tests/test_kotor_to_kotor_preview_mode.py tests/test_retarget_workbench_modes.py tests/test_export_job.py tests/test_kotor_to_unreal_preview_mode.py tests/test_kotor_to_unreal_export.py tests/test_ghostrigger_kotor_to_unreal_workbench_controller.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_retarget_workbench_readiness.py tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py tests/test_ghostrigger_retarget_output_naming_controller.py -q --basetemp .pytest_tmp_kotor_to_unreal_smoke`; `python -m pytest tests -q -k "retarget or project or validation or export" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_kotor_to_unreal_targeted`; `python -m py_compile src/core/retargeting/unreal_target_skeleton.py src/core/retargeting/kotor_to_unreal_solver.py src/core/retargeting/kotor_to_unreal_preview.py src/core/retargeting/ue_fbx_exporter.py src/core/retargeting/kotor_source_animation.py src/core/retargeting/retarget_output_naming.py src/core/retargeting/retarget_modes.py src/core/export/export_job.py src/gui/windows/qt_retarget_workbench_controller.py src/gui/windows/qt_retarget_preview_controller.py src/gui/windows/qt_main_window.py tests/test_kotor_to_unreal_preview_mode.py tests/test_kotor_to_unreal_export.py tests/test_ghostrigger_kotor_to_unreal_workbench_controller.py`; `git diff --check`.

- Added a third-pass coding-book scope sanity report and current-state audit for GhostRigger. The new report re-anchors the product around four studios, clarifies what GhostRigger is for, what should stay out of scope, and adds a mandatory scope test for future features: owning studio, modder task, target resource/object, capability label, validation/export gates, and remaining incompleteness. The scope audit gives an honest assessment that Retarget Studio is currently the strongest product surface, shared architecture foundations are real but not yet fully integrated, Character Studio native KOTOR DAG export is the highest product risk, and Module/Map Studio need provider-backed resources, undo, and ExportJob migration before broad modder-facing editing. Roadmap reference: T2304/T2305 planning.
  Affected areas: `knowledge_base/book_notes/coding_books_third_pass_scope_sanity_2026_05_23.md`, `docs/audits/ghostrigger_scope_sanity_audit_2026_05_23.md`, `docs/audits/ghostrigger_program_architecture_audit_2026_05_23.md`, `docs/audits/ghostrigger_roadmap_adjustments_2026_05_23.md`, `knowledge_base/roadmap/04_full_suite_completion_roadmap_20260522.md`, `knowledge_base/book_notes/ghostrigger_programming_crosswalk.md`, `knowledge_base/book_notes/README.md`, `knowledge_base/README.md`.
  Verification: third-pass PDF term scan across all six local coding/UI/architecture books; static source audit with `rg` for controllers, Qt action/model/threading/process/undo usage, project/validation/export foundations, missing provider mentions, stubs/placeholders, and roadmap terms; `python -m pytest tests/test_ghostrigger_project_model.py tests/test_validation_bus.py tests/test_export_job.py tests/test_retarget_workbench_readiness.py tests/test_kotor_to_kotor_preview_mode.py tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py -q --basetemp .pytest_tmp_scope_audit`; `git diff --check`.

- Added a coding-focused GhostRigger knowledge-base refresh and current architecture sanity audit. The new notes synthesize Qt/PySide6 GUI books, official Qt documentation, Refactoring UI, Architecture Patterns with Python, and Clean Architecture into GhostRigger-specific rules for Qt actions, model/view, undoable authoring, background jobs, service boundaries, ResourceAddress, ValidationBus, ExportJob, and modder-facing capability honesty. A second pass across all six books added missed guidance around proxy models, data mappers, filesystem models, QSettings boundaries, QProcess/service bridges, progress/cancel jobs, empty states, defaults, and undo-before-authoring. A verified KOTOR modding source hierarchy was also added so future work can distinguish implementation truth, mature community tools, and tutorial/workflow references. The audit updates the current program snapshot and recommends the next roadmap order: `GameResourceProvider`, provider-backed resource models, ValidationBus issue panel, Module/Map undo foundation, shared job/progress bridge, Retarget quality mode polish, and Character Studio native DAG lock. Roadmap reference: T2304/T2305 planning.
  Affected areas: `knowledge_base/book_notes/qt_python_ui_architecture_for_ghostrigger.md`, `knowledge_base/book_notes/python_clean_architecture_for_ghostrigger.md`, `knowledge_base/book_notes/ghostrigger_programming_crosswalk.md`, `knowledge_base/book_notes/coding_books_second_pass_2026_05_23.md`, `knowledge_base/reference/kotor_modding_verified_sources.md`, `docs/audits/ghostrigger_program_architecture_audit_2026_05_23.md`, `docs/audits/ghostrigger_roadmap_adjustments_2026_05_23.md`, `docs/audits/kotor_modding_source_audit_2026_05_23.md`, `docs/audits/ghostrigger_source_inventory.json`, `docs/audits/ghostrigger_capability_matrix.md`, `docs/audits/ghostrigger_development_roadmap_recommendations.md`, `docs/audits/holocron_toolset_comparison.md`, `knowledge_base/book_notes/README.md`, `knowledge_base/README.md`.
  Verification: PDF outline/term scan for six local books; second-pass term matrix across all six local books; official Qt docs checked for Qt for Python, model/view, QAction, Designer, Undo Framework, and threading; KOTOR source review against PyKotor/OpenKotOR, Holocron Toolset, KotorBlender, MDLOps, xoreos/xoreos-docs, reone, KotOR.js, KotOR Modding Guide/KLE, DeadlyStream tutorial sources, and KOTOR Modding Wiki MDL notes; source audit with `rg` across `src`, `tests`, `docs`, and `knowledge_base`; `git diff --check`.

## 2026-05-22

- Added a modder-facing Retarget Workbench readiness/status layer. The new readiness model summarizes the active mode, selected source, target, target output animation, vanilla-slot versus custom-patch runtime behavior, missing inputs, preview readiness, export readiness, and stale-preview reasons without invoking samplers, solvers, writers, or ExportJob. The Qt shell now exposes stable readiness labels near the mode selector so modders can see whether an output is vanilla-safe or custom-patch-only before preview/export. Roadmap reference: T2405.
  Affected areas: `src/core/retargeting/retarget_workbench_readiness.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_workbench_readiness.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_workbench_readiness.py -q --basetemp .pytest_tmp_retarget_workbench_readiness`; `python -m pytest tests/test_ghostrigger_retarget_workbench_controller.py tests/test_ghostrigger_retarget_output_naming_controller.py tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py tests/test_retarget_workbench_readiness.py -q --basetemp .pytest_tmp_retarget_workbench_ux`; `python -m pytest tests -q -k "retarget or project or validation or export" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_retarget_workbench_ux_targeted`; `python -m py_compile src/core/retargeting/retarget_workbench_readiness.py src/core/retargeting/retarget_modes.py src/core/retargeting/retarget_output_naming.py src/gui/windows/qt_retarget_workbench_controller.py src/gui/windows/qt_retarget_preview_controller.py src/gui/windows/qt_main_window.py tests/test_retarget_workbench_readiness.py`.

- Enabled the Retarget Workbench `KOTOR → KOTOR` mode. The new adapter samples a source KOTOR/Odyssey animation slot through the existing Aurora evaluator/supermodel resolver into a `SourceSkeletonClip`, then reuses the verified preview pipeline to retarget onto a target KOTOR model as either a vanilla slot override or custom animation patch name. The workbench now treats `KOTOR → KOTOR` as an implemented preview/export mode, stores the sampled source and last preview result, applies previews through the existing in-memory viewport adapter, and exports only the exact last preview through the existing ExportJob/Aurora writer path. Roadmap reference: T2403.
  Affected areas: `src/core/retargeting/kotor_to_kotor_preview.py`, `src/core/retargeting/retarget_modes.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `src/gui/windows/qt_main_window.py`, `tests/test_kotor_to_kotor_preview_mode.py`, `tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py`, `tests/test_retarget_workbench_modes.py`, `tests/test_ghostrigger_retarget_workbench_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_kotor_to_kotor_preview_mode.py -q --basetemp .pytest_tmp_kotor_to_kotor_preview`; `python -m pytest tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py -q --basetemp .pytest_tmp_kotor_to_kotor_ui`; `python -m pytest tests/test_kotor_animation_source_sampler.py tests/test_retarget_output_naming.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_retarget_workbench_modes.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_ghostrigger_retarget_output_naming_controller.py tests/test_kotor_to_kotor_preview_mode.py tests/test_ghostrigger_kotor_to_kotor_workbench_controller.py -q --basetemp .pytest_tmp_kotor_to_kotor_smoke`; `python -m pytest tests -q -k "retarget or project or validation or export" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_kotor_to_kotor_targeted`.

- Added custom KOTOR output animation naming for the Retarget Workbench. KOTOR-targeted retarget output can now stay in strict vanilla-slot mode or opt into custom-patch mode, where the generated `AnimationBlock.name` preserves a validated custom name exactly and marks preview/export metadata as requiring the custom animation patch/runtime. Vanilla slot resolution remains the default, display labels never become animation names, preview/export state invalidates when output naming changes, and the Qt shell now exposes vanilla/custom naming controls plus slot/custom-name fields. Roadmap reference: T2402/T2403 custom animation patch workflow.
  Affected areas: `src/core/retargeting/retarget_output_naming.py`, `src/core/retargeting/retarget_mapping.py`, `src/core/retargeting/retarget_solver.py`, `src/core/retargeting/retarget_preview.py`, `src/core/retargeting/retarget_preview_export.py`, `src/core/retargeting/aurora_animation_writer.py`, `src/gui/windows/qt_retarget_preview_controller.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_output_naming.py`, `tests/test_ghostrigger_retarget_output_naming_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_output_naming.py -q --basetemp .pytest_tmp_retarget_output_naming`; `python -m pytest tests/test_ghostrigger_retarget_output_naming_controller.py -q --basetemp .pytest_tmp_retarget_output_naming_ui`; `python -m pytest tests/test_animation_export_slot_gate.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_basic_retarget_solver.py tests/test_export_job.py tests/test_validation_bus.py tests/test_ghostrigger_project_model.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_retarget_output_naming.py tests/test_ghostrigger_retarget_output_naming_controller.py -q --basetemp .pytest_tmp_custom_anim_name_smoke`; `python -m pytest tests -q -k "retarget or project or validation or export" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_custom_anim_name_targeted`.

- Added the shared `ExportJob` transaction foundation for safe multi-artifact exports. The new helper performs preflight validation, same-directory staging, writer callbacks against staged paths, verifier callbacks before promotion, manifest promotion support, best-effort rollback/cleanup, and optional `ValidationBus` publication. Retarget preview MDL/MDX export now uses this staged transaction path while still exporting the exact approved preview animation block through the existing Aurora writer/readback gates. Roadmap reference: T2303.
  Affected areas: `src/core/export/export_job.py`, `src/core/export/__init__.py`, `src/core/retargeting/retarget_preview_export.py`, `tests/test_export_job.py`, `tests/test_retarget_preview_export.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_export_job.py -q --basetemp .pytest_tmp_export_job`; `python -m pytest tests/test_retarget_preview_export.py -q --basetemp .pytest_tmp_export_job_retarget`; `python -m pytest tests/test_validation_bus.py tests/test_ghostrigger_project_model.py tests/test_retarget_preview_export.py tests/test_mdl_animation_override_roundtrip.py -q --basetemp .pytest_tmp_export_job_smoke`; `python -m py_compile src/core/export/export_job.py src/core/export/__init__.py src/core/retargeting/retarget_preview_export.py src/core/retargeting/aurora_animation_writer.py src/core/validation/validation_bus.py tests/test_export_job.py tests/test_retarget_preview_export.py`; `git diff --check`.

- Added the shared `ValidationBus` foundation for suite-wide diagnostics. The new validation package defines UI/navigation-friendly `ValidationIssue`, `ValidationReport`, `ValidationSeverity`, `ValidationSubsystem`, and `ValidationNavigationTarget` models with ResourceAddress-backed targets, JSON roundtrip helpers, deterministic issue IDs, source replacement/append behavior, subscriber snapshots, report merging, and adapters for project validation plus retarget preview audits. Aurora node navigation targets preserve exact node casing for MDL/controller diagnostics. Roadmap reference: T2302.
  Affected areas: `src/core/validation/validation_bus.py`, `src/core/validation/validation_adapters.py`, `src/core/validation/__init__.py`, `tests/test_validation_bus.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_validation_bus.py -q --basetemp .pytest_tmp_validation_bus`; `python -m pytest tests/test_ghostrigger_project_model.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_kotor_animation_source_sampler.py -q --basetemp .pytest_tmp_validation_bus_smoke`; `python -m pytest tests -q -k "retarget or module or walkmesh or character or level or scene or project or validation" --maxfail=10 --disable-warnings --basetemp .pytest_tmp_validation_bus_targeted`; `python -m py_compile src/core/validation/validation_bus.py src/core/validation/validation_adapters.py src/core/validation/__init__.py src/core/project/project_validation.py tests/test_validation_bus.py`; `git diff --check`.

- Fixed the retarget MDL export path so animation-only PMBAM overrides no longer rebuild or compact the vanilla mesh/skin payload. `AuroraAnimationWriter` now routes local animation overrides through an MDL writer injection path that copies the source MDL geometry/name/node/mesh bytes and source MDX byte-for-byte, patches only the model file size plus local animation table fields, and appends the new animation block. Added PMBAM regression coverage proving the exported MDX is identical to vanilla and that the original MDL prefix differs only in those animation/header bytes, which protects skinned-node bonemaps, qbones/tbones, skin weights, mesh index tables, bounds, render/shadow flags, and texture casing from retarget export drift. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `src/core/retargeting/aurora_animation_writer.py`, `tests/test_aurora_animation_writer.py`, `tests/test_animation_export_slot_gate.py`.
  Verification: `python -m pytest tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_mesh_preserve`; `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_retarget_preview_export.py -q --basetemp .pytest_tmp_mesh_preserve_gates`; `python -m py_compile src/core/mdl/mdl_writer.py src/core/retargeting/aurora_animation_writer.py tests/test_aurora_animation_writer.py tests/test_animation_export_slot_gate.py`; `git diff --check`.

- Added the suite-level `GhostRiggerProject` session model and `ResourceAddress` foundation so Character, Retarget, Module, Map, and scenario workflows can share one JSON-serializable project spine without embedding proprietary asset bytes. The new project package includes stable resource address keys, lightweight refs for imported assets, retarget jobs, module workspaces, KMAP/KMAX map projects, scenario packages, validation snapshots, and export candidates, plus validation for duplicate IDs, KOTOR-safe resource refs, custom-animation patch metadata, verified export candidates, and non-JSON metadata. Roadmap reference: T2301.
  Affected areas: `src/core/project/`, `tests/test_ghostrigger_project_model.py`, `docs/architecture/ghostrigger_project_model.md`.
  Verification: `python -m pytest tests/test_ghostrigger_project_model.py -q --basetemp .pytest_tmp_project_model`; `python -m pytest tests/test_secret_hygiene.py tests/test_retarget_workbench_modes.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_kotor_animation_source_sampler.py -q --basetemp .pytest_tmp_project_smoke`; `python -m py_compile src/core/project/__init__.py src/core/project/resource_address.py src/core/project/ghostrigger_project.py src/core/project/project_validation.py tests/test_ghostrigger_project_model.py`; `git diff --check`.

- Fixed the MDL binary writer's final name-table casing regression after live K1 testing showed exported PMBAM still entered A-pose even with full controller coverage. The writer now preserves Aurora node names exactly as loaded from the target model instead of lowercasing the geometry and animation name table, and the animation export-tree validator now compares node names, parents, and child lists case-sensitively. Added PMBAM regression coverage that injects `victory`, reloads the exported MDL/MDX, compares geometry node names/parent paths against vanilla PMBAM byte-for-byte/case-for-case, verifies the local `victory` animation node names use the same casing, and confirms inherited `pause1`, `walk`, and `run` still resolve through the supermodel chain. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `tests/test_aurora_animation_writer.py`.
  Verification: `python -m pytest tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_case_preserve`; `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_retarget_preview_export.py -q --basetemp .pytest_tmp_case_export_gates`; `python -m py_compile src/core/mdl/mdl_writer.py src/core/retargeting/aurora_animation_writer.py src/core/game/kotor_loader.py`.

- Fixed the reverse-retarget MDL export path after K1 live testing showed `victory` loaded but evaluated in A-pose because only 14 limb nodes carried controllers. R3.A/R3.B animation generation now emits full-hierarchy orientation controllers for every target Aurora node, including constant rest-pose keys for unmoving core/control branches such as `rootdummy`, `pelvis_g`, `torso_g`, `torsoUpr_g`, `rcollar_g`, and `lcollar_g`. Root/pelvis position controllers are emitted when source root motion is detected, and the R3.B size gate now allows the expected growth from full-sampled controller coverage. The binary MDL writer also defensively backfills rest-pose orientation controllers on otherwise controllerless animation tree nodes so sparse callers cannot produce a valid-but-unposed local override. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/retargeting/aurora_animation_writer.py`, `src/core/mdl/mdl_writer.py`, `tests/test_aurora_animation_writer.py`.
  Verification: Ghidra MCP identified `UpdateAnimFootprint` as the crash-site tree walker reading child animation-node fields; `python -m pytest tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_full_pose_controllers`; `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_writer_pose_gate`; `python -m pytest tests/test_retarget_preview_export.py tests/test_basic_retarget_solver.py -q --basetemp .pytest_tmp_writer_preview_gate`; `python -m py_compile src/core/retargeting/aurora_animation_writer.py src/core/mdl/mdl_writer.py tests/test_aurora_animation_writer.py`; rebuilt the approved calibrated PMBAM candidate as `exports/manual_override_test_20260522_limb_exact_fullcontrollers_v2/pmbam.mdl/.mdx`, installed it to the local K1 Override with `pmbam.mdl.bak` / `pmbam.mdx.bak` backups, and verified live readback `victory_nodes=61`, `orientation_nodes=61`, `missing_core_orientation=[]`, and no controllerless animation nodes.

- Locked AgentDecompile/Ghidra adapter configuration to local/env-only defaults so private hosts, usernames, and passwords are not exposed through the GitHub repo. The adapter no longer hardcodes the live Ghidra MCP IP or default shared-server username, `.env` and local MCP/Ghidra config patterns are ignored, and a tracked-file hygiene test now fails if the known private endpoint/password values are committed. No roadmap task ID applies.
  Affected areas: `src/kotormcp/adapters_decompile.py`, `.gitignore`, `tests/test_secret_hygiene.py`.
  Verification: `git grep`/`rg` sweep for the known Ghidra host/password strings; `python -m pytest tests/test_secret_hygiene.py -q --basetemp .pytest_tmp_secret_hygiene`; `python -m py_compile src/kotormcp/adapters_decompile.py tests/test_secret_hygiene.py`; `git diff --check`.

- Fixed the MDL animation export tree shape after K1 live testing showed the corrected model header still crashed in `UpdateAnimFootprint` before KPM playback hooks could run. Exported local animation blocks now clone the full target Aurora hierarchy and overlay controllers onto matching nodes by name, so unkeyed toes, hands, fingers, helper nodes, and mesh placeholders remain present with valid parent/child arrays. The main writer and legacy porter now validate that the binary animation export tree mirrors the target node order, parents, and child lists before writing. Added raw-byte regression coverage that walks the emitted animation child arrays and fails if sparse animation blocks omit unkeyed target nodes. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `src/core/mdl/mdl_porter.py`, `tests/test_roundtrip_verification.py`, `knowledge_base/reference/ghidra_odyssey_mcp.md`.
  Verification: Ghidra MCP confirmed `UpdateAnimFootprint` walks animation node child arrays and reads child node fields at `0x00437f3f`; `python -m pytest tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_anim_tree`; `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_anim_tree_export`; rebuilt and installed the local K1 Override `pmbam.mdl/.mdx` with `.animtree_20260522T083000.bak` backups, then verified live header fields, `victory` animation subtype `0x05`, raw animation `node_count=61`, child-array walk `61/61` with no invalid offsets, and GhostRigger readback `PMBAM` with local animation `victory` containing 61 nodes.

- Fixed the MDL binary writers' full engine model header layout after K1 live testing showed the subtype-byte fix was necessary but not sufficient: exported retarget candidates still crashed in `InputBinary::Read` because GhostRigger emitted an 88-byte model-field payload where PyKotor/the engine expect `_ModelHeader.SIZE = 0xC4` / 196 bytes. The main writer and legacy porter now emit the full post-geometry model fields, patch `offset_to_super_root`, preserve `mdx_data_buffer_offset`, `mdx_size`, `mdx_offset`, and place the name-offset table at the canonical `0xC4` boundary with duplicate counts. Added raw-byte regression coverage and expanded the Odyssey Ghidra MCP knowledge note with the model-header offsets. Roadmap reference: T2402 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `src/core/mdl/mdl_porter.py`, `tests/test_roundtrip_verification.py`, `knowledge_base/reference/ghidra_odyssey_mcp.md`.
  Verification: Ghidra MCP reconfirmed the crash path in `InputBinary::Read` / `MaxTree::AsModel`; compared stock PMBAM header fields against the generated Override candidate; `python -m pytest tests/test_roundtrip_verification.py tests/test_mdl_animation_override_roundtrip.py tests/test_animation_export_slot_gate.py -q --basetemp .pytest_tmp_model_header_gate`; `python -m py_compile src/core/mdl/mdl_writer.py src/core/mdl/mdl_porter.py tests/test_roundtrip_verification.py`; rebuilt and installed the local K1 Override `pmbam.mdl/.mdx` with timestamped `.headerfix_20260522T075425.bak` backups, then verified live header fields `topSubtype=0x02`, `offsetToSuperRoot=1028`, `mdxSize=148960`, `nameOffsets=196`, `animSubtype=0x05`, and GhostRigger/PyKotor readback `PMBAM` with local animation `victory`.

- Fixed the MDL binary writer's engine-facing MaxTree subtype bytes after K1 live testing showed exported retarget candidates could crash `swkotor.exe` in `InputBinary::Read` when `MaxTree::AsModel` returned null. The main writer now emits `0x02` at top-level geometry header `+0x4C` and `0x05` at every animation geometry header `+0x4C`; the legacy porter path now writes the top-level model subtype at the same engine offset instead of one byte late. Added raw-byte regression coverage for the emitted headers and documented the Odyssey Ghidra MCP workflow without storing credentials. Roadmap reference: T2402/T2403 export safety.
  Affected areas: `src/core/mdl/mdl_writer.py`, `src/core/mdl/mdl_porter.py`, `tests/test_roundtrip_verification.py`, `knowledge_base/reference/ghidra_odyssey_mcp.md`, `knowledge_base/README.md`.
  Verification: Ghidra MCP confirmed `MaxTree::AsModel` reads byte `[ECX+0x4C]`, masks with `0x7F`, and requires model subtype `0x02` before `InputBinary::Read` writes through the returned pointer; `python -m pytest tests/test_roundtrip_verification.py tests/test_mdl_animation_override_roundtrip.py -q --basetemp .pytest_tmp_mdl_subtype_gate`; patched the live K1 Override `pmbam.mdl` after backing it up to `pmbam.mdl.bak`, with the only byte differences at `0x58: 00→02` and `0x29814: 00→05`; GhostRigger/PyKotor readback loads `PMBAM` with local animation `victory`.

- Added the KOTOR/Odyssey animation source sampler for future Retarget Workbench directions. A valid local or inherited Aurora animation slot can now be sampled through the existing evaluator into a `SourceSkeletonClip` with Aurora node hierarchy metadata, local/global sampled transforms, model-rest or slot-time reference pose support, quaternion continuity, source audit compatibility, and hard failures for invalid slots, unknown controller nodes, non-finite keys, or zero-length orientation keys. The workbench pending-mode status now records that the KOTOR source sampler is available while `KOTOR → KOTOR` preview and `KOTOR → Unreal` FBX export adapters remain pending. Roadmap references: T2401/T2405.
  Affected areas: `src/core/retargeting/kotor_source_animation.py`, `src/core/retargeting/retarget_modes.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `tests/test_kotor_animation_source_sampler.py`, `tests/test_retarget_workbench_modes.py`, `tests/test_ghostrigger_retarget_workbench_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_kotor_animation_source_sampler.py -q --basetemp .pytest_tmp_kotor_source_sampler`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_retarget_workbench_modes.py tests/test_ghostrigger_retarget_preview_controller.py tests/test_ghostrigger_retarget_export_controller.py tests/test_ghostrigger_retarget_workbench_controller.py tests/test_kotor_animation_source_sampler.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/kotor_source_animation.py src/core/retargeting/source_animation.py src/core/retargeting/source_skeleton_audit.py src/core/retargeting/retarget_modes.py src/core/animation/animation_engine.py src/core/game/kotor_loader.py src/core/geometry/model_data.py src/gui/windows/qt_retarget_workbench_controller.py`; `git diff --check`.

- Added the first tri-mode Retarget Workbench shell so the verified UE/FBX-to-KOTOR retarget path now lives under explicit mode contracts alongside pending `KOTOR → KOTOR` and `KOTOR → Unreal` modes. The new workbench controller owns mode/state, invalidates stale preview/export results on mode changes, disables preview/export for pending modes, and delegates the existing `Unreal → KOTOR` preview/export flow without reworking solver math. The main Qt shell now exposes a top-right `retargetModeComboBox` populated with all three modes and keeps the existing Preview/Export actions routed through the workbench. Roadmap references: T2401/T2405.
  Affected areas: `src/core/retargeting/retarget_modes.py`, `src/gui/windows/qt_retarget_workbench_controller.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `tests/test_retarget_workbench_modes.py`, `tests/test_ghostrigger_retarget_workbench_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_workbench_modes.py -q --basetemp .pytest_tmp_retarget_workbench_modes`; `python -m pytest tests/test_ghostrigger_retarget_workbench_controller.py -q --basetemp .pytest_tmp_retarget_workbench_ui`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_ghostrigger_retarget_preview_controller.py tests/test_ghostrigger_retarget_export_controller.py tests/test_retarget_workbench_modes.py tests/test_ghostrigger_retarget_workbench_controller.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/retarget_modes.py src/core/retargeting/retarget_preview.py src/core/retargeting/retarget_preview_export.py src/gui/windows/qt_retarget_preview_controller.py src/gui/windows/qt_retarget_workbench_controller.py src/gui/windows/qt_main_window.py`; `git diff --check`.

- Refreshed the suite-level architecture audit and completion roadmap after syncing LordVader's latest `qt-ghostrigger` KMAX scene, KMAP level, Module Editor, viewport transform, and MDL parser recovery work. The new roadmap now treats GhostRigger as four studios (Character, Retarget, Module, Map) over shared project/session, validation, KMAX/KMAP, resource, preview, and export-job services. Roadmap references: T2301/T2305/T2402.
  Affected areas: `knowledge_base/roadmap/04_full_suite_completion_roadmap_20260522.md`, `knowledge_base/roadmap/README.md`.
  Verification: `git fetch partner`; merged `partner/qt-ghostrigger` into local `qt-ghostrigger`; `python -m pytest tests/test_coordinate_converter.py tests/test_animation_injector.py tests/test_reverse_renamer.py tests/test_aurora_animation_writer.py tests/test_retarget_calibration.py -q --basetemp .pytest_tmp_sync_retarget`; `python -m pytest tests/test_axis_mode.py tests/test_pivot_data.py tests/test_gizmo_mode_state.py tests/test_gizmo_follows_object.py tests/test_gizmo_kmax_transform.py -q --basetemp .pytest_tmp_sync_kmax`; `python -m pytest tests/test_module_categories.py tests/test_module_hydration.py tests/test_module_object_inspector.py tests/test_module_save_pipeline.py tests/test_module_reference_safety.py tests/test_custom_module_packager.py tests/test_lyt_room_graph.py tests/test_map_snap_tools.py tests/test_vis_editor.py tests/test_area_wok_integration.py -q --basetemp .pytest_tmp_sync_modules`; `python -m pytest tests/test_core_contracts.py -q -k "kmax or gizmo or pivot or module_editor or legacy_tk" --basetemp .pytest_tmp_sync_contracts`; targeted `py_compile` for retargeting, level, KMAX, and Qt window modules; `git diff --check`.

- Refined the PMBAM UE idle retarget path after viewport testing showed that full source-rest transfer over-bent the body while clip-frame-zero transfer erased the authored idle pose. R3.B now defaults to a hybrid reference policy that keeps root/torso/lower-body channels stable against clip frame 0 while blending shoulder/arm/finger bind-pose deltas by a tunable weight, and the injection CLI exposes `--source-reference-mode` plus `--hybrid-limb-source-rest-weight` for future calibration passes. The UE-to-Aurora converter now uses the observed PMBAM/UE rest-pose convention `(-X, -Y, Z)`, fixing the old unmirrored-X/Y basis that left arms in an A-pose. The PMBAM rename map now targets real forearm nodes (`Lforearm_g`/`Rforearm_g`) instead of the intermediate bicep links and includes two-joint finger mappings. The final scratch candidate used calibrated-basis exact segment correction across arms, hands, fingers, thighs, shins, feet, and toes; its durable findings now live in the retargeting verification knowledge base while generated MDL/MDX outputs stay ignored. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/coordinate_converter.py`, `src/core/retargeting/aurora_animation_writer.py`, `src/core/retargeting/retarget_calibration.py`, `src/core/retargeting/retarget_solver.py`, `scripts/inject_animation.py`, `knowledge_base/retargeting/ue5_to_aurora_rename_map.json`, `knowledge_base/retargeting/verification/pmbam_ue_idle_pose_accuracy_20260522.md`, `tests/test_coordinate_converter.py`, `tests/test_animation_injector.py`, `tests/test_aurora_animation_writer.py`, `tests/test_retarget_calibration.py`, `tests/test_reverse_renamer.py`.
  Verification: `python -m pytest tests/test_coordinate_converter.py tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_idle_axis`; `python -m pytest tests/test_animation_injector.py tests/test_reverse_renamer.py tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_idle_fix`; `python -m pytest tests/test_coordinate_converter.py tests/test_animation_injector.py tests/test_reverse_renamer.py tests/test_aurora_animation_writer.py -q --basetemp .pytest_tmp_idle_calibrated`; `python -m py_compile src/core/retargeting/coordinate_converter.py src/core/retargeting/aurora_animation_writer.py src/core/retargeting/animation_injector.py scripts/inject_animation.py`; generated/readback-verified hybrid PMBAM `victory` candidates from `M_Neutral_Stand_Idle_Loop_export.fbx`, selected the calibrated-basis exact full-limb segment candidate from GhostRigger viewport captures, verified sampled arm/hand/finger/leg/foot/toe segment direction error at approximately `0.000001` degrees, and installed matching `pmbam.mdl/.mdx` to the local K1 Override folder with timestamped backups.

- Added a general GhostRigger book knowledge layer that is no longer retargeting-specific. The new tracked `knowledge_base/book_notes/` folder captures broad chapter maps and project applications for Vince's *Mathematics for Computer Graphics*, Gregory's *Game Engine Architecture* Volume I PDF, and Dunn/Parberry's *3D Math Primer*, plus a subsystem crosswalk for MDL/MDX resources, rendering, skinning, animation, retargeting, Character Builder, module/walkmesh tools, Qt UI, performance, validation, and export pipelines. Roadmap reference: T1403 animation playback workflow and general engine knowledgebase maintenance.
  Affected areas: `knowledge_base/book_notes/`, `knowledge_base/README.md`.
  Verification: local PDF outline extraction for all three books; documentation-only update; `git diff --check`.

- Added a 3D Math Primer research note for the GhostRigger retargeting pipeline. The new knowledgebase entry fills the practical math gaps around multiple coordinate spaces, nested parent-local spaces, row/column convention hazards, orthonormal basis construction, quaternion negation/inverse/multiplication/difference, plane/pole diagnostics, skeletal bind-pose implications, and why direction matching alone cannot solve PMBAM arm/hand pose quality. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `knowledge_base/retargeting/research/3d_math_primer_for_aurora_retargeting.md`, `knowledge_base/retargeting/README.md`.
  Verification: local PDF outline, keyword/page scan, targeted section extraction, documentation-only update; `git diff --check`.
- No roadmap task ID applies: Added the Autodesk FBX SDK Setup Assistant for local, user-managed SDK configuration without bundling Autodesk files. The assistant detects Python/platform/architecture, opens Autodesk's official FBX SDK download page, lets users select SDK/binding/FbxCommon paths, scans likely SDK files, tests `import fbx`, optional `import FbxCommon`, SDK manager creation, and empty scene creation, then stores verified path settings under `settings.json` `fbx_sdk`. FBX import/export now applies configured SDK paths and prompts to open setup or Autodesk's download page when the SDK is missing. Added setup documentation, an environment diagnostic script, requirements comments, and `.gitignore` guards for local SDK folders. Affected areas: `src/io/fbx/`, `src/gui/dialogs/fbx_sdk_setup_dialog.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `scripts/print_fbx_python_environment.py`, `docs/FBX_SDK_SETUP.md`, `.gitignore`, `requirements.txt`, Qt import tests. Verification: `python -m py_compile src/io/fbx/fbx_sdk_paths.py src/io/fbx/fbx_sdk_setup.py src/io/fbx/fbx_sdk_loader.py src/io/fbx/fbx_diagnostics.py src/io/fbx/fbx_importer.py src/io/fbx/fbx_exporter.py src/gui/dialogs/fbx_sdk_setup_dialog.py src/gui/qt_lib.py src/gui/windows/qt_main_window.py scripts/print_fbx_python_environment.py tests/test_qt_only_imports.py`; `pytest tests/test_qt_only_imports.py -q`; `python scripts/print_fbx_python_environment.py` confirmed this Python 3.13 environment has no `fbx`/`FbxCommon` and reports that cleanly.
  Follow-up: FBX import now auto-detects the recommended backend before import begins and presents that decision as an explicit `Use Auto` choice alongside manual Autodesk SDK and Blender FBX choices. Auto-detection checks the configured Autodesk SDK first, then Blender availability, and the selected backend is used without silently falling through to the other backend after import starts. Roadmap reference: T2408.

- No roadmap task ID applies: Added an optional Autodesk Python FBX SDK bridge for static mesh FBX import/export. The new bridge safely detects `fbx`/`FbxCommon`, imports FBX hierarchy/meshes/transforms/materials/UVs/normals into `KotorModel` objects that enter the existing KMAX scene/viewport path, exports current or selected scene meshes through Autodesk SDK when installed, and adds FBX SDK diagnostics plus clear missing-SDK warnings in the Qt UI. Affected areas: `src/io/fbx/`, `src/gui/windows/qt_main_window.py`, `requirements.txt`, Qt import/FBX bridge tests. Verification: `python -m py_compile src/io/__init__.py src/io/fbx/__init__.py src/io/fbx/fbx_sdk_loader.py src/io/fbx/fbx_scene_adapter.py src/io/fbx/fbx_importer.py src/io/fbx/fbx_exporter.py src/io/fbx/fbx_diagnostics.py src/gui/windows/qt_main_window.py tests/test_qt_only_imports.py`; `pytest tests/test_qt_only_imports.py -q`; direct FBX diagnostic command confirmed this Python 3.13 environment lacks `fbx` and `FbxCommon` and reports the missing dependency without crashing.

- T1403 follow-up: Fixed the reserved top UI command-bar collision by letting `CommandBarScroll` reserve enough vertical height for the main command bar's wrapped/overflow row and horizontal scrollbar before the second icon command bar is laid out below it. Layout application now re-syncs the reserved top rows after toolbar density changes. Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/libtheme/layout_applier.py`, layout tests. Verification: `python -m py_compile src/gui/windows/qt_main_window.py src/gui/libtheme/layout_applier.py src/gui/libtheme/style_tokens.py tests/test_theme_layout_loading.py`; `pytest tests/test_theme_layout_loading.py::test_main_window_reserves_command_bar_overflow_height -q`; visible main-window geometry smoke captured `Logs/main_command_bar_overflow_smoke.png`.

- T1403 follow-up: Split the viewport shade and shader dropdowns into compact icon-button groups. Wireframe now selects wire-only rendering, Solid and Solid+Wire have their own cube icons, Realistic/Shaded/Flat use sphere icons, UV View and Lock View To Camera are icon-only controls, the perspective view dropdown was removed, and the reference-coordinate selector moved into the view-control cluster without its label. The navigation profile control remains a dropdown and now uses branded 3ds Max, Blender, and Maya PNG icons from `src/gui/icons/branded_controls/`. The viewport command bar now has theme-editable `viewportToolbar.background` and `viewportToolbar.border` tokens plus a visible frame. Affected areas: `.gitignore`, `src/gui/viewports/qt_viewport.py`, `src/gui/windows/qt_main_window.py`, `src/gui/libtheme/`, `src/gui/icons/`, theme docs, toolbar tests. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_loader.py src/gui/libtheme/qt_stylesheet_builder.py src/gui/libtheme/theme_editor_window.py tests/test_joint_dot_overlay.py tests/test_theme_layout_loading.py`; `pytest tests/test_joint_dot_overlay.py tests/test_theme_layout_loading.py -q`; visible Qt toolbar smoke captured `Logs/viewport_toolbar_smoke.png`.

- T1403 follow-up: Reworked the viewport command bar's bulky toggle buttons into icon-only SVG controls. Wire, Bones, Texture, Grid, GPU, Dots, Heat, Frame, Gimbal, transform mode, and Measure now use packaged icons with full tooltips; GPU uses the existing `src/gui/icons/gpu_branding/nvidia.png` or `amd.png` assets when detected and falls back to a generic SVG. The duplicate Camera reset button and global WalkMesh button were removed from the visible viewport strip while keeping their underlying actions available elsewhere. Affected areas: `src/gui/viewports/qt_viewport.py`, `src/gui/icons/`, viewport toolbar tests. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_joint_dot_overlay.py`; `pytest tests/test_joint_dot_overlay.py::test_viewport_toolbar_primary_controls_are_icon_only tests/test_joint_dot_overlay.py::test_t405_toolbar_heat_button_tracks_heatmap_enabled tests/test_joint_dot_overlay.py::test_t404_snap_view_widget_exists -q`; visible Qt smoke opened the main window and confirmed the target viewport controls were icon-only with loaded icons.
  Follow-up: Moved the GPU toggle to the far-left of the viewport command bar and enlarged only its icon/padding so the existing NVIDIA/AMD PNG branding fills the button face more clearly. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_joint_dot_overlay.py`; `pytest tests/test_joint_dot_overlay.py::test_viewport_toolbar_primary_controls_are_icon_only tests/test_joint_dot_overlay.py::test_gpu_vendor_icon_uses_packaged_brand_png -q`; visible Qt smoke confirmed GPU is first and enlarged.

- T1403 follow-up: Added XML-driven dock grouping and packaged visual profile layouts for Animation, Mesh Editing, Lighting, Cinegraphics, and Default No Toolboxes. Layout XML now supports `<dockLayout>` groups that tab or split existing dock widgets, the main toolbar exposes a Visual Profile dropdown wired through `LayoutManager`, and the default layout now describes its startup dock grouping in XML. Affected areas: `src/gui/libtheme/`, `src/gui/windows/qt_main_window.py`, `config/themes/layouts/`, theme/layout docs, tests. Verification: `python -m py_compile src/gui/libtheme/layout_model.py src/gui/libtheme/layout_loader.py src/gui/libtheme/layout_validator.py src/gui/libtheme/layout_applier.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py tests/test_content_browser_panel.py`; `pytest tests/test_theme_layout_loading.py tests/test_content_browser_panel.py::test_main_window_exposes_visual_profile_dropdown -q`; `python tools/validate_themes.py`; visible Qt smoke cycled the Visual Profile dropdown through Animation, Mesh Editing, Lighting, Cinegraphics, Clean, and Default.

- T1403 follow-up: Reused the prelaunch `ResourceManager` created for startup Content Browser indexing instead of rebuilding a second manager when the themed main window populates the Resource Browser. The startup payload now carries the already-indexed manager, `_apply_preloaded_library` installs it on the main window after game directories are applied, and `_populate_resource_panel` reads from `_get_resource_manager()` so matching directories reuse the cached index. Affected areas: `src/gui/windows/qt_main_window.py`, startup/library tests. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_prelaunch_library_payload_scans_before_main_window tests/test_content_browser_panel.py::test_preloaded_library_skips_post_show_auto_detect_timer -q`; `pytest tests/test_content_browser_panel.py tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild -q`.

## 2026-05-22

- T1403: Added the first unified Qt Content Browser pass for Library and Animation Library workflows. The main shell now routes model-library scan/load/extract/batch/add-to-scene/retarget actions and current-model animation load/preview/export actions through a dockable Content Browser with asset navigation, searchable/filterable metadata, details/preview affordances, context actions, and first-class animation rows. Content Browser, Scene, Properties, and Animations are now independent dock widgets around a central viewport instead of fixed side tabs, freeing the viewport as the main workspace. Added the `contentBrowser` and `scene` layout panel ids while keeping legacy `library` and `animationLibrary` layout ids for compatibility. Affected areas: `src/gui/panels/qt_content_browser_panel.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `src/gui/libtheme/`, `config/themes/layouts/`, theme/layout docs, `.gitignore`, tests. Verification: `python -m py_compile src/gui/panels/qt_content_browser_panel.py src/gui/qt_lib.py src/gui/windows/qt_main_window.py src/gui/libtheme/layout_applier.py tests/test_content_browser_panel.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py -q`; `pytest tests/test_content_browser_panel.py tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_core_contracts.py::test_main_window_bottom_area_is_resizable_splitter tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable tests/test_qt_only_imports.py::test_application_imports_use_central_qt_lib tests/test_theme_layout_loading.py -q`; `python tools/validate_themes.py`; offscreen main-window dock smoke. Broader `pytest tests/ -x` was started, interrupted, and then skipped per user request to avoid long tests.
  Follow-up: Smoothed the Content Browser's internal nav/list/details splitter so the left navigation and right details panes resize naturally instead of snapping back after layout application. Action buttons now allow the details pane to shrink without forcing a large minimum width. Verification: `python -m py_compile src/gui/panels/qt_content_browser_panel.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_theme_layout_loading.py -q`.
  Follow-up: Promoted detached dock panels into proper top-level windows with normal window/minimize/maximize/close flags, and expanded the Content Browser animation workflow. Animation rows now persist in separate scene/current and scanned-library buckets, scene objects contribute labeled animation rows by object/model/resref, selecting a scene model refreshes the docked Animations panel for that model, and "Scan Animations" indexes clips across scanned library models on a background worker. MCP parity smoke used `K1:pmbam` before this loader-facing UI work. Verification: `python -m py_compile src/gui/panels/qt_content_browser_panel.py src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py tests/test_core_contracts.py`; `pytest tests/test_content_browser_panel.py tests/test_core_contracts.py::test_scene_animation_entries_collect_all_runtime_models tests/test_core_contracts.py::test_retarget_apply_promotes_target_model_for_animation_list tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser -q`; direct `AnimationLibraryScanWorker` smoke on `K1:s_female02` found 43 clips; offscreen detached Content Browser dock smoke confirmed floating window flags.
  Follow-up: Replaced floating dock promotion with a reusable `QtFloatingDockHost` QMainWindow so detached panels become real resizable workspace windows that can host dock widgets, instead of tool-style floating docks. Verification: `python -m py_compile src/gui/windows/qt_main_window.py`; offscreen detach smoke confirmed the Content Browser dock is reparented into `QtFloatingDockHost` with normal window/min/max flags.
  Follow-up: Fixed detached Content Browser host resizing so a single docked panel fills the real floating window instead of leaving an empty central workspace that makes the browser appear fixed-width. The floating host now keeps its placeholder at zero size and resizes the hosted dock after window resizes while avoiding forced expansion once multiple panels are docked into that host. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_floating_content_browser_host_resizes_single_dock -q`.
  Follow-up: Added cross-host reparenting for detached dock panels so dragging a panel out and releasing over another floating workspace combines it into that window instead of recreating a separate host. Center drops tabify with the existing panel, edge drops dock left/right/bottom, and panels can still be split back out into their own host. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_floating_dock_host_can_combine_detached_panels -q`.
  Follow-up: Replaced fragile cross-window drop promotion with an explicit detachable-panel context menu. Right-clicking a dock title/body now offers `Dock to New Window`, `Dock to Window`, and `Return to Main Window`, so panels can be combined into an existing floating workspace without depending on Qt cross-`QMainWindow` drag/drop. Added Qt object-lifetime guards around delayed dock promotion to prevent stale `QDockWidget` wrappers from crashing after Qt deletes the underlying object. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_floating_dock_host_ignores_deleted_dock_wrapper tests/test_content_browser_panel.py::test_floating_dock_host_can_combine_detached_panels -q`.
  Follow-up: Refined floating workspace behavior so dock tabs appear at the top, tab stacks and single toolboxes expand to fill their host window, and the `Dock to Window` menu exposes placement choices for each target workspace (`As Tab`, `Left`, `Right`, `Bottom`). Multi-panel hosts now keep a descriptive `Workspace: ...` title and remain discoverable as docking targets. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py -q`; `pytest tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable -q`.
  Follow-up: Removed the visible empty central workspace from floating toolbox windows. A floating host with one toolbox now makes that `QDockWidget` the central widget so it fills the whole window; when additional toolboxes are added, the host converts the existing central toolbox back into dock areas for tabbing or side-by-side placement. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py -q`; `pytest tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable -q`.
  Follow-up: Made Content Browser floating windows ignore narrow saved dock widths from the main window and clamp to the browser's intended floating default size. The Content Browser panel now advertises an expanding size policy so it fills a single-toolbox floating host like the other panels. Verification: `python -m py_compile src/gui/panels/qt_content_browser_panel.py src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py -q`.
  Follow-up: Cleared stale `QDockWidget` maximum-width caps when rehosting panels into floating workspace windows. Visible smoke testing showed Content Browser, Scene, and Properties inheriting narrow docked maximum widths inside otherwise-resized hosts; rehosted docks and their payload widgets now relax those caps before central/dock placement. Verification: onscreen dock smoke captured screenshots in `artifacts/dock_smoke/`, first exposed the width-cap failures, and then passed all single-host, tab, left/right/bottom placement, and workspace-discoverability checks after the patch; `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; focused max-width regression test added.
  Follow-up: Moved first-launch game auto-detection and Content Browser library indexing into a visible pre-main-window bootstrap. The Qt launcher now shows a GhostRigger splash/progress window, detects missing K1/K2 install paths, saves discovered paths, scans library model rows before constructing the main shell when startup scanning is enabled, and passes the preloaded rows into the Content Browser so the old post-launch auto-detect/scan timer is skipped. Verification: visible startup smoke captured screenshots in `artifacts/prelaunch_smoke/` and confirmed 6,071 model rows were indexed before the main window opened; focused prelaunch payload tests added; `python -m py_compile src/gui/panels/qt_content_browser_panel.py src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py -q`; `pytest tests/test_core_contracts.py::test_main_window_routes_library_and_animation_library_to_content_browser tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable tests/test_qt_only_imports.py::test_application_imports_use_central_qt_lib -q`.
  Follow-up: Reworked startup loading visuals into a theme-aware `QtStartupSplash` with the GhostRigger logo, embedded shared progress/toast panel, and copyright text (`GhostRigger (C) 2026 Shaolin (CrispyWonton). Co-developed by LordVaderCW.`). The normal `QtProgressToast` now shares the same themed `QtProgressPanel`, and startup suppresses the separate initial theme-applied toast so loading feedback stays inside the splash. Verification: visible startup smoke captured themed splash screenshots in `artifacts/prelaunch_smoke/`; `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_startup_splash_uses_themed_embedded_progress tests/test_content_browser_panel.py::test_preloaded_library_skips_post_show_auto_detect_timer -q`.
  Follow-up: Hardened progress toast theme synchronization so every show/update/finish call reapplies the active `ThemeManager` theme, native/default theme clears still restyle the embedded progress panel, and live theme changes refresh any existing toast instead of leaving stale colours. Verification: visible toast-theme smoke captured Matrix and Light themed toast widgets in `artifacts/toast_theme_smoke/` and confirmed the stylesheet tokens changed; `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_progress_toast_reapplies_active_theme_on_show tests/test_content_browser_panel.py::test_progress_panel_stylesheet_tracks_theme_tokens -q`.
  Follow-up: Linked `QtStartupSplash` directly to `ThemeManager` as a theme-aware widget instead of passing only a one-time resolved theme. The splash now registers with the startup theme manager, applies the selected prelaunch theme before work begins, and refreshes if the manager changes theme while the splash is alive. Verification: visible splash-theme smoke captured Matrix and Light screenshots in `artifacts/splash_theme_smoke/` and confirmed the live manager signal changed splash/progress tokens; `python -m py_compile src/gui/windows/qt_main_window.py tests/test_content_browser_panel.py`; `pytest tests/test_content_browser_panel.py::test_startup_splash_registers_with_theme_manager -q`.
  T1403 follow-up: Added startup splash customization to the Theme Editor under a new Splash tab. Themes can now edit and preview `splash.logoPath`, `splash.productText`, `splash.subtitleText`, `splash.copyrightText`, `splash.width`, `splash.height`, and `splash.logoSize`; the runtime `QtStartupSplash` consumes the same styles/metrics while continuing to use theme colour tokens for panels, text, borders, and progress. Verification: visible Theme Editor and runtime splash smoke captured screenshots in `artifacts/theme_editor_splash_smoke/`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_editor_window.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py tests/test_content_browser_panel.py`; focused splash editor/runtime tests passed.
  T1403 follow-up: Added splash-specific colour tokens and controls to the Theme Editor Splash tab so the Default/native theme no longer inherits Matrix-green fallback values in the splash preview. `splash.background`, `splash.panel`, `splash.brandBackground`, `splash.progressBackground`, `splash.border`, `splash.text`, `splash.secondaryText`, `splash.accent`, `splash.progressTrack`, and `splash.progressFill` now drive both the editor preview and runtime splash, with neutral Default/native packaged values and derived values for other themes. Verification: focused packaged-theme/default-token tests; visible Default splash editor smoke in `artifacts/theme_editor_splash_smoke/`.
  T1403 follow-up: Extended Default/native colour normalization from splash-only to the full theme palette. The packaged Default theme now defines neutral native values for all colour tokens, and `ThemeLoader` repairs stale Matrix fallback values from older native user overrides before the Theme Editor displays them. Programmatic Theme Editor teardown now skips the dirty-change confirmation prompt so automated visual smoke runs can close without blocking, while user-initiated closes still prompt. Verification: visible Default Colours tab smoke against the existing user override reported no Matrix legacy values in `artifacts/theme_editor_splash_smoke/default_palette_summary.json`; focused native-palette and close-teardown regression tests passed.
  T1403 follow-up: Connected Default/native splash editing and runtime splash styling to the live `QApplication` palette so the splash reflects the actual app greys instead of portable fallback XML values. Added `splash.surfaceStyle` with `matte`, `bevelled`, `glossy`, and `flat` finishes, applied the same surface effects to the embedded splash progress panel, exposed the control in the Theme Editor preview, and refreshed the local Default/LordVaderCW user theme overrides to the current native dark palette. Verification: visible Theme Editor smoke captured the Default splash with live palette and surface effects in `artifacts/theme_editor_splash_smoke/theme_editor_default_native_splash_fx.png`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_editor_window.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py tests/test_content_browser_panel.py`; `python tools/validate_themes.py`; `pytest tests/test_theme_layout_loading.py -q`; `pytest tests/test_content_browser_panel.py -q`; `pytest tests/ -m "not slow" -x`; `git diff --check`.
  T1403 follow-up: Moved the startup Content Browser dock into the left dock column under the Scene outliner instead of the bottom dock area, preserving the output log/Python splitter underneath the viewport. The layout XML now gives Scene and Content Browser explicit left-stack height preferences for each packaged layout, and startup applies a vertical dock split so the browser opens bottom-left by default. Verification: visible main-window smoke captured the startup dock layout; focused main-window routing/layout contract tests passed.
- MDL parser stock-corpus recovery: Patched GhostRigger's owned binary MDL reader to apply logical MDL bounds when reading variable-length trimesh, child-node, and light flare arrays, and to sanitize non-finite face plane coefficients/normals before PyKotor converts faces. This fixes the 54 stock model parse failures captured in `diagnostics/mdl_parser_errors_2026-05-22.json`, including `K1:fx_explode_03`, `K2:504ondh`, and `K1:w_dblsbr_001`. No roadmap task ID applies. Affected areas: `src/core/mdl/ghostrigger_mdl_reader.py`, `tests/test_regression.py`. Verification: MCP helper ground-truth checks confirmed the three representative failures now load through `inspect_mdl`, `inspect_mdl_ghostrigger`, and `compare_model_pipelines`; prior-failure replay checked 54/54 models with 0 remaining errors; full manifest parser sweep checked 6,069 models with 0 parser errors and saved `diagnostics/mdl_parser_errors_after_patch_2026-05-22.json`.
- Main UI Tk-removal cleanup and MDL parser diagnostics: Removed the dead `Open Legacy Tk Workbench` action from the Qt Tools menu and deleted the `_launch_legacy_tk` process launcher, keeping the main shell aligned with M3/T302's completed Tk removal. Added a regression guard so the legacy Tk menu/action does not return. Ran a manifest-wide MCP/PyKotor parser sweep across the refreshed 6,069-model corpus and saved the 54 parser failures to `diagnostics/mdl_parser_errors_2026-05-22.json` for follow-up triage. No roadmap task ID applies. Affected areas: `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`, `diagnostics/mdl_parser_errors_2026-05-22.json`. Verification: direct MCP helper checks confirmed `K1:fx_explode_03` fails during `inspect_mdl` with `unpack requires a buffer of 4 bytes` and `K1:w_dblsbr_001` fails with `cannot convert float NaN to integer`; `python -m py_compile src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_main_window_moves_utility_tabs_to_tools_windows tests/test_qt_only_imports.py::test_legacy_tk_modules_are_deleted tests/test_qt_only_imports.py::test_no_gui_module_imports_tkinter`; offscreen main-window menu probe confirmed Tools no longer contains Legacy Tk.
- T1101/T1102/T1103 regression follow-up: Aligned the QA suite with the current GPU-only Qt viewport contracts. Unreal Animator preview stop now leaves both viewports on GPU instead of attempting to restore the removed CPU path, the MCP compatibility helper exposes quaternion sign-equivalence comparison, selected model outlines draw from projected mesh hulls instead of model bounds, stale root `src/gui/viewport_core.py` static test paths now point at `src/gui/rendering/viewport_core.py`, screenshot goldens and deterministic export hashes were refreshed for current Qt output, and the scan manifest was regenerated from the configured MCP registry's real K1/K2 MDL inventory. Affected areas: `src/gui/windows/qt_unreal_animator.py`, `src/gui/viewports/qt_viewport.py`, `src/kotormcp/tools/ghostrigger_tools.py`, `exports/scan_manifest.json`, `tests/golden/`, `tests/test_core_contracts.py`, `tests/test_regression_screens.py`, `tests/test_skeleton_template_hud_wiring.py`. Verification: MCP-style direct comparisons for `K1:pmbam` and `K1:pmhc01` matched PyKotor/GhostRigger pipelines; `pytest tests/test_core_contracts.py::test_unreal_animator_uses_gpu_during_animation_preview tests/test_regression.py::test_quaternion_sign_equivalence_in_comparison tests/test_regression_screens.py tests/test_skeleton_template_hud_wiring.py tests/test_regression_export.py -m "not slow"`; `pytest tests/test_regression_export.py -m slow`; `pytest tests/ -m "not slow"`. Remaining slow corpus findings after manifest refresh: `tests/test_mcp_skinning.py -x` now reaches `K1:fx_explode_03` and fails on a parser `unpack requires a buffer of 4 bytes`; `tests/test_mcp_textures.py -x` now reaches `K1:3dgui` and reports missing texture resources `logo_sw_01` and `w_lghtsbr`.
- MCP/KotorRigger tools recovery: Localized `.cursor/mcp.json` to this machine's GhostRigger checkout and Steam K1/K2 paths, restored the missing `kotormcp.tools.ghostrigger_tools` compatibility module used by full-scan and diagnostic scripts, and made pytest/scan helpers consume the configured MCP paths before falling back to defaults. The restored helpers expose `_resource_pair`, `inspect_mdl`, `inspect_mdl_ghostrigger`, `compare_model_pipelines`, `inspect_skinning`, and `validate_textures`, using PyKotor as the ground-truth loader and GhostRigger's own importer/resource resolver for parity checks. No roadmap task ID applies. Affected areas: `.cursor/mcp.json`, `src/kotormcp/tools/ghostrigger_tools.py`, `tests/conftest.py`, `tests/test_mcp_resource_exposure.py`, `scripts/qa_common.py`, `scripts/full_scan.py`. Verification: required MCP resource query attempted and returned no configured resources before the fix; `python -m py_compile src/kotormcp/tools/ghostrigger_tools.py scripts/full_scan.py scripts/qa_common.py tests/conftest.py`; direct `K1:n_bith` MCP helper validation confirmed PyKotor/GhostRigger node parity, four skin nodes, and loadable `n_bith01` texture; `python -m pytest tests/test_mcp_resource_exposure.py tests/test_mcp_full_scan.py::test_t1103_mode_distribution_writer_counts_ambiguous_and_errors -q`.

## 2026-05-21

- KMAX scene gizmo/properties follow-up: Removed the synthetic `:scene` wrapper from imported scene composites, kept scene placement on the copied model root, stopped scene-root scale/type-in edits from mutating mesh vertices, and added a GPU per-draw scene transform matrix so imported object translate/rotate/scale is applied by uniforms instead of CPU vertex rewrites. Scene object properties now show runtime model node/mesh/vertex/face counts and scaled dimensions. No roadmap task ID applies. Affected areas: `src/gui/viewports/qt_viewport.py`, `src/gui/gizmo/transform_controller.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/panels/qt_properties_panel.py`, `tests/test_gizmo_follows_object.py`, `tests/test_core_contracts.py`. Verification: attempted required MCP access, but `kotormcp.tools.ghostrigger_tools` is not present in this environment and in-repo `ghostrigger_model_info` could not locate the configured K1 installation; `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/gizmo/transform_controller.py src/gui/rendering/gpu_renderer.py src/gui/panels/qt_properties_panel.py`; `pytest tests/test_gizmo_follows_object.py tests/test_gizmo_kmax_transform.py tests/test_core_contracts.py::test_kmax_scene_composite_preserves_authored_root_name_for_animation_skinning tests/test_core_contracts.py::test_kmax_scene_gpu_transform_uses_authored_vbo_basis tests/test_core_contracts.py::test_kmax_scene_reload_preserves_selected_object_for_pivot_tools -q`. Broader `pytest tests/ -m "not slow" -x` reached 197 passed / 2 skipped before stopping on the pre-existing Unreal Animator GPU restore assertion `test_unreal_animator_uses_gpu_during_animation_preview`.
- Viewport transform gizmo hardening: Restored the existing rotate-ring renderer, added the `TransformGizmoMode` alias and explicit update-contract helpers, kept gizmo pivot/origin data synchronized through selection, drag, type-in edits, undo/redo, scene reload, and KMAX pivot persistence, added W/E/R mode selection while preserving SPACE cycling, and cleared stale gizmo state when selection is empty. Affected areas: `src/gui/gizmo/`, `src/gui/viewports/qt_viewport.py`, `tests/test_gizmo_*.py`. Verification: `python -m py_compile src/gui/gizmo/gizmo_mode.py src/gui/gizmo/__init__.py src/gui/gizmo/gizmo_renderer.py src/gui/gizmo/transform_gizmo.py src/gui/gizmo/transform_controller.py src/gui/viewports/qt_viewport.py tests/test_gizmo_mode_state.py tests/test_gizmo_follows_object.py tests/test_gizmo_kmax_transform.py`; `python -m pytest tests/test_gizmo_mode_state.py tests/test_gizmo_follows_object.py tests/test_gizmo_kmax_transform.py -q`; `python -m pytest tests/test_core_contracts.py -q -k "transform_gizmo or gizmo_picker"`; `python -m pytest tests/test_axis_mode.py tests/test_pivot_data.py tests/test_kmax_serialization.py -q`. Broader `python -m pytest tests/ -x -q` reached 191 passed / 2 skipped before stopping on the pre-existing Unreal Animator GPU restore assertion `test_unreal_animator_uses_gpu_during_animation_preview`.
  Follow-up: Added the existing Adjust Pivot dock to the Modules menu via `Open Adjust Pivot`, wired to the same detachable panel instance instead of creating a duplicate. Verification: `python -m py_compile src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py::test_main_window_exposes_adjust_pivot_in_modules_menu -q`.
  Follow-up: Preserved the active KMAX scene-object selection through Adjust Pivot and pivot-alignment refreshes by preventing synthetic scene-root auto-selection and reselecting the captured scene object id after the composite viewport reload. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py::test_kmax_scene_reload_preserves_selected_object_for_pivot_tools -q`.
  Follow-up: Made Adjust Pivot mode controls persistent exclusive toggles and kept translated gizmo pivot origin aligned to the object's actual final position, including snapped movement. Verification: `python -m py_compile src/gui/panels/adjust_pivot_panel.py src/gui/gizmo/transform_controller.py tests/test_core_contracts.py tests/test_gizmo_follows_object.py`; `python -m pytest tests/test_core_contracts.py::test_adjust_pivot_mode_buttons_are_persistent_toggles tests/test_gizmo_follows_object.py -q`.
  Follow-up: Made the translate gizmo centre dot a real view-plane drag handle so dragging the visible gizmo centre moves the selected object instead of being an inert decoration, cleared CPU/GPU transform caches for scene-object descendants during live gizmo movement, and prevented stale saved Pivot Only mode from silently hijacking normal object-gizmo drags on new viewport sessions. Verification: `python -m py_compile src/gui/gizmo/gizmo_renderer.py src/gui/gizmo/transform_controller.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py tests/test_gizmo_follows_object.py tests/test_core_contracts.py`; `python -m pytest tests/test_gizmo_follows_object.py tests/test_core_contracts.py::test_transform_cache_evict_clears_frame_and_gpu_child_caches -q`; `python -m pytest tests/test_core_contracts.py::test_transform_gizmo_controller_applies_translate_rotate_scale_and_cancel tests/test_core_contracts.py::test_gizmo_picker_prioritizes_uniform_scale_center_handle tests/test_core_contracts.py::test_adjust_pivot_mode_buttons_are_persistent_toggles -q`; `python -m pytest tests/test_core_contracts.py::test_adjust_pivot_mode_starts_object_only_for_normal_gizmo_drags tests/test_core_contracts.py::test_adjust_pivot_mode_buttons_are_persistent_toggles tests/test_core_contracts.py::test_main_window_exposes_adjust_pivot_in_modules_menu -q`.
- Viewport transform controls: Added persistent per-object KMAX pivot data, the `AxisMode` enum and `TransformReferenceController`, an Axis / Reference Coordinate System toolbar dropdown, a dockable Adjust Pivot toolbox, first-pass pivot editing/alignment actions, pick-reference workflow hooks, gizmo axis-basis/pivot-origin integration, and focused pivot/axis tests. Affected areas: `src/core/scene/`, `src/gui/gizmo/`, `src/gui/viewports/qt_viewport.py`, `src/gui/windows/qt_main_window.py`, `src/gui/panels/`, docs, tests. Verification: `python -m py_compile ...` for touched scene/gizmo/viewport/window/panel/test files; `python -m pytest tests/test_pivot_data.py tests/test_axis_mode.py -q`; `python -m pytest tests/test_qt_only_imports.py -q`; `python -m pytest tests/test_core_contracts.py::test_kmax_scene_composite_preserves_authored_root_name_for_animation_skinning -q`. Broader `python -m pytest tests/ -x` progressed past the new pivot/axis tests and stopped on the pre-existing Unreal Animator GPU restore assertion `test_unreal_animator_uses_gpu_during_animation_preview`.
- T402 follow-up: Reworked viewport mesh picking and outline feedback so object clicks require an actual triangle ray hit instead of falling back to projected mesh/model bounds, empty clicks deselect, and the yellow selected-model hull is replaced by a hovered-mesh silhouette outline. Affected areas: `src/gui/viewports/qt_viewport.py`, `tests/test_core_contracts.py`. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py -q -k "mesh_pick_requires_real_triangle_and_hover_outline or mesh_multiselect_box or context_menu_does_not_pick"`; `python -m pytest tests/test_core_contracts.py -q -k "viewport or gizmo or camera_hit_test or light_gizmos"`.
- T1403/KMAX playback follow-up: Fixed scene-composite animation playback by placing imported runtime models under a separate KMAX scene wrapper while preserving the authored MDL root names and local transforms. Scene copies now retain original source DFS node indices so binary skin meshes keep resolving `qBone`/`tBone` rows in the source MDL index space instead of the expanded `scene_root` wrapper space; this keeps `K1:n_bith` on the corrected `G5_FULL_REF` palette path when `pause1` starts. Also made the `S_Male02` logical-offset binary import regression check assert the actual contract (late helper node reached and all animations loaded) instead of an install-specific node count. Affected areas: `src/gui/viewports/qt_viewport.py`, `src/core/animation/gpu_skinning.py`, `tests/test_core_contracts.py`, `tests/test_regression.py`. Verification: direct `K1:n_bith` binary load, KMAX composite import, and `pause1` skin-palette evaluation confirmed wrapper `N_Bith:scene`, copied root `N_Bith`, effective node count `75`, no missing non-empty bone refs, and `qBone_tBone_dfs_indexed_TR_no_invert`; `python -m pytest tests/test_core_contracts.py -q -k "kmax_scene_composite_preserves_authored_root_name_for_animation_skinning or add_model_to_scene_dialog_stays_compact or gpu_viewport_uses_overlay"`; `K1_PATH=h:\steam\steamapps\common\swkotor python -m pytest tests/test_regression.py -q -k "read_mdl_safe_k1_supermodel_node_order_uses_logical_offsets or read_mdl_safe_k1_control_model"`.
- GPU viewport overlay restoration: Restored the viewport information layer on the GPU-only render path. GPU frames now pass through the viewport overlay compositor for the advanced translate/rotate/scale gizmo, bottom-left axes gimbal, HUD model stats, bounds readout, FPS timing badge, measurement helpers, camera helpers, and selection/edit overlays without calling the removed CPU model renderer. Affected areas: `src/gui/viewports/qt_viewport.py`, `tests/test_core_contracts.py`. Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `python -m pytest tests/test_core_contracts.py -q -k "viewport_render_loop_is_gpu_only or gpu_viewport_uses_overlay or performance_overlay or gpu_grid_is_native"`; `python -m pytest tests/test_theme_layout_loading.py -q -k "native_viewport_theme_keeps_overlay_render_path_available or renderer_has_native_theme_overlay_defaults"`.
- KMAX scene workflow: Added the first-pass general `.kmax` scene format, serializer, validator, active `KMaxSceneManager`, blank-scene viewport startup, scene File menu actions with `Ctrl+S` / `Ctrl+Shift+S`, explicit add/clear/cancel model-library import prompt, multi-object scene outliner, composite viewport rendering for multiple model instances, per-instance transform persistence, examples, and documentation. Follow-up: enforced the viewport GPU-only rendering contract by removing the CPU fallback from the main canvas, thumbnail render, and `GpuRenderer.render()` failure path, and by disabling the legacy `_render_cpu()` hook. Follow-up: fixed the Add Model to Scene dialog so layout application no longer expands it to main-window size, the dialog opens as a compact modal, and local dialog theming no longer triggers the global theme-apply toast. Affected areas: `src/core/scene/`, `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/dialogs/`, `src/gui/panels/`, `README.md`, `AGENTS.md`, `docs/knowledgebase/kmax_scene_system.md`, `examples/kmax/`. Verification: `python -m py_compile ...`; `pytest tests/test_kmax_serialization.py -q`; `pytest tests/test_core_contracts.py -q -k "viewport_render_loop_is_gpu_only or main_window or viewport"`; `pytest tests/test_core_contracts.py -q -k "add_model_to_scene_dialog_stays_compact or main_window or viewport"`.
- T1501/T1503/T1504/T1604: Added the standalone Qt Module Editor / Level Editor foundation with versioned KMAP models, JSON serialization, validation, scene operations, LYT/WOK service bridges, build/export manifests, split editor panels, and a top-level Module Editor window. Updated the main shell to open the standalone editor and removed the embedded left-side Modules tab. Verification: added `tests/test_kmap_serialization.py`; focused tests run with `pytest tests/test_kmap_serialization.py`.
- T1501: Fixed Qt main-window startup after restoring the command-bar horizontal overflow wrapper by importing `make_horizontal_overflow_area` in `qt_main_window.py`. Verification: `pytest tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable -q`.
- T1501/T1604: Reworked the standalone Module Editor layout so the 3D viewport is the dominant center workspace, workflow tabs sit in the left tool stack, the scene list is a shallow strip, and validation/output no longer consumes the lower half of the window by default. Verification: offscreen Module Editor layout smoke, `pytest tests/test_kmap_serialization.py -q`, `pytest tests/test_qt_only_imports.py -q`.
- T1501/T1604: Added padding around the Module Editor viewport tool chrome and passed the configured viewport navigation profile through to the embedded viewport on window creation, Module Editor open, and settings updates. Verification: offscreen Module Editor viewport padding/navigation smoke, `pytest tests/test_qt_only_imports.py -q`.
- T1501/T1604: Added extra lower padding under the Level Editor top command/tool rows and embedded viewport toolbar so buttons no longer sit flush against the content below. Verification: offscreen Level Editor top-padding smoke, `python -m py_compile src/gui/panels/module_editor/module_editor_viewport_panel.py`.
- T1501/T1604: Fixed the Level Editor embedded viewport toolbar padding so the toolbar keeps its full scroll-area height and uses a separate spacer before the 3D canvas, preventing clipped viewport buttons. Verification: offscreen Level Editor toolbar-gap smoke, `python -m py_compile src/gui/panels/module_editor/module_editor_viewport_panel.py`, `pytest tests/test_qt_only_imports.py -q`.
- T1501/T1604: Increased the Level Editor embedded viewport toolbar height to 64 px so the 3D view controls have more vertical room. Verification: offscreen Level Editor toolbar-height smoke, `python -m py_compile src/gui/panels/module_editor/module_editor_viewport_panel.py`.
- T1501/T1604: Made the Level Editor embedded viewport toolbar height follow the wrapped button content, using 64 px as the minimum and expanding for multi-row controls so buttons are not clipped at narrower widths. Verification: offscreen Level Editor dynamic toolbar-height smoke, `python -m py_compile src/gui/panels/module_editor/module_editor_viewport_panel.py`, `pytest tests/test_qt_only_imports.py -q`.
- T1501/T1604: Added library-asset import to the Level Editor. The standalone editor now has an Assets tab backed by scanned Game Library rows, the main Library panel can send selected assets to the Level Editor, module/tile rows become KMAP room instances, and other library model rows become blueprint entries with source metadata preserved. Verification: `python -m py_compile src/core/modules/module_editor_controller.py src/gui/panels/module_editor/module_editor_asset_browser.py src/gui/panels/module_editor/__init__.py src/gui/panels/qt_library_panel.py src/gui/windows/module_editor_window.py src/gui/windows/qt_main_window.py tests/test_kmap_serialization.py`, `pytest tests/test_kmap_serialization.py -q`, offscreen Module Editor asset-import smoke, `pytest tests/test_qt_only_imports.py -q`.

## 2026-05-21

- Added a Game Engine Architecture research note for the GhostRigger retargeting pipeline. The new knowledgebase entry records useful Volume I lessons for treating retargeting as an asset-conditioning pipeline, keeping solver/evaluator/viewport/writer subsystem boundaries separate, preserving KOTOR MDL/MDX resource identity, making clip timing deterministic, and expanding debug overlay/capture/report tooling before Override testing. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `knowledge_base/retargeting/research/game_engine_architecture_for_aurora_retargeting.md`, `knowledge_base/retargeting/README.md`.
  Verification: local PDF outline and keyword/page scan; documentation-only update; `git diff --check`.

- Added a book-backed calibrated retarget-frame layer for Aurora node animation experiments. The new research note records the John Vince graphics-math principles now guiding the retargeter, especially basis vectors, inverse/order-sensitive transforms, orthonormal frames, quaternion composition, and interpolation. The new opt-in `calibrated_frame_delta` solver path builds full source/target segment frames from mapped reference poses, transfers current source frame motion through those calibrated bases, and includes synthetic coverage for PMBAM-style intermediate arm chains without changing the default safer `reference_frame_delta` mode. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_calibration.py`, `src/core/retargeting/retarget_solver.py`, `tests/test_retarget_calibration.py`, `knowledge_base/retargeting/research/computer_graphics_math_for_aurora_retargeting.md`, `knowledge_base/retargeting/README.md`.
  Verification: `python -m pytest tests/test_retarget_calibration.py -q --basetemp .pytest_tmp_retarget_calibration`; `python -m pytest tests/test_basic_retarget_solver.py tests/test_retarget_reference_pose.py tests/test_retarget_mapping_profile.py tests/test_retarget_preview_gate.py tests/test_retarget_calibration.py -q --basetemp .pytest_tmp_retarget_calibration_gates`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_retarget_preview_export.py tests/test_retarget_calibration.py -q --basetemp .pytest_tmp_retarget_calibration_full`; `python -m py_compile src/core/retargeting/retarget_calibration.py src/core/retargeting/retarget_solver.py src/core/retargeting/retarget_frames.py src/core/retargeting/retarget_solve_audit.py`; `git diff --check`.

- Reverted the basic retarget solver default back to the safer `reference_frame_delta` transfer after real PMBAM viewport testing showed the new segment-direction solve over-constrained the body pose even though its abstract segment audit passed. Segment-direction mode remains available explicitly for diagnostics and future chain-frame experiments, but the UI/export default no longer uses it. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_solver.py`, `tests/test_basic_retarget_solver.py`.
  Verification: restored the previous PMBAM Override candidate from backup for user testing; pending focused/full retarget gate rerun before commit.

- Added segment-direction retarget solving and mapped pose-error reporting for the GhostRigger UE-to-Aurora solver. The solver can align mapped anatomical parent-child segment directions against explicit source/target reference poses when that transfer mode is explicitly selected, records per-sample segment angle error in the solve report, and keeps the reference-frame delta path available for nodes without mapped child segments. The MDL readback verifier now treats case-only node-name normalization from real PMBAM readback as a warning while still comparing structure, controllers, and evaluated poses semantically. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_solver.py`, `src/core/retargeting/retarget_solve_audit.py`, `src/core/validation/animation_roundtrip_validator.py`, `tests/test_basic_retarget_solver.py`, `tests/test_mdl_animation_override_roundtrip.py`.
  Verification: `python -m pytest tests/test_mdl_animation_override_roundtrip.py tests/test_basic_retarget_solver.py -q --basetemp .pytest_tmp_roundtrip_segment`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_ghostrigger_retarget_preview_controller.py tests/test_retarget_preview_export.py tests/test_ghostrigger_retarget_export_controller.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/validation/animation_roundtrip_validator.py src/core/retargeting/retarget_solver.py src/core/retargeting/retarget_solve_audit.py src/core/retargeting/retarget_preview.py src/core/retargeting/retarget_preview_export.py`; verified a real PMBAM `victory` override candidate with MDL readback enabled and staged matching `pmbam.mdl/.mdx` into the local K1 Override folder for visual testing.

- Added verified export for the last successful GhostRigger retarget preview. The new core export helper writes exactly the approved preview `AnimationBlock` through the existing strict Aurora writer gates, refuses failed/stale previews before touching output files, deep-copies the original target model and animation block, keeps MDL readback verification on by default, warns on Override filename/resref mismatches, and never re-runs the retarget solver during export. The Qt UI now exposes `Export Retarget Preview...`, disables it until a current passing preview exists, marks previews stale when inputs/target change, asks before overwriting MDL/MDX outputs, and reports the verified export candidate without invoking Patch Manager. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_preview_export.py`, `src/core/retargeting/aurora_animation_writer.py`, `src/gui/windows/qt_retarget_preview_controller.py`, `src/gui/windows/qt_main_window.py`, `tests/test_retarget_preview_export.py`, `tests/test_ghostrigger_retarget_export_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_preview_export.py -q --basetemp .pytest_tmp_preview_export`; `python -m pytest tests/test_ghostrigger_retarget_export_controller.py -q --basetemp .pytest_tmp_preview_export_ui`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_ghostrigger_retarget_preview_controller.py tests/test_retarget_preview_export.py tests/test_ghostrigger_retarget_export_controller.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/retarget_preview.py src/core/retargeting/retarget_preview_export.py src/core/retargeting/aurora_animation_writer.py src/gui/windows/qt_retarget_preview_controller.py src/gui/windows/qt_main_window.py`; `git diff --check`.

- Wired the tested in-memory retarget preview gate into the Qt GhostRigger UI. The main window now exposes `Load UE/FBX Source Animation...`, `Load Retarget Profile...`, and `Preview Retarget` actions, keeps Preview disabled until the required source/target/profile inputs are present, builds previews through the core service, applies successful local override previews through a real Qt viewport adapter with playback and node overlay hooks, reports audit warnings in the output log/status bar, and preserves the no-export/no-Patch-Manager boundary for preview. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/gui/windows/qt_retarget_preview_controller.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `tests/test_ghostrigger_retarget_preview_controller.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_ghostrigger_retarget_preview_controller.py -q --basetemp .pytest_tmp_preview_ui`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py tests/test_ghostrigger_retarget_preview_controller.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/retarget_preview.py src/core/retargeting/retarget_solver.py src/core/retargeting/retarget_profile.py src/gui/windows/qt_retarget_preview_controller.py src/gui/windows/qt_main_window.py src/gui/qt_lib.py`; `git diff --check`.

- Added the in-memory retarget viewport preview gate. GhostRigger can now build a preview model from the pure solver result, attach the generated Aurora animation as a local override on a deep-copied target model, keep the original target untouched, run a headless numeric audit over evaluator playback, block preview application on invalid slots/unknown nodes/non-finite transforms/non-root translation drift/root drift, and apply successful previews through a small viewport adapter with optional node overlay and standard angle capture hooks. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_preview.py`, `tests/test_retarget_preview_gate.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_preview_gate.py -q --basetemp .pytest_tmp_retarget_preview`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py tests/test_retarget_preview_gate.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/source_animation.py src/core/retargeting/retarget_profile.py src/core/retargeting/retarget_mapping.py src/core/retargeting/reference_pose.py src/core/retargeting/retarget_solver.py src/core/retargeting/retarget_preview.py src/core/animation/animation_engine.py src/core/game/kotor_loader.py src/core/geometry/model_data.py src/core/validation/animation_block_validator.py`; `git diff --check`.

- Added the first basic UE-source to KOTOR/Aurora retarget solver. The new pure solver validates the retarget profile and KOTOR slot, builds explicit source/target reference poses, transfers sampled source global rotation motion into mapped target reference frames, emits absolute parent-relative Aurora orientation controllers only for mapped existing nodes, strips root translation by default, leaves non-root translations at target reference/rest values, preserves quaternion hemisphere continuity, validates the generated animation block, and reports solve diagnostics without mutating source clips, profiles, or target models. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_solver.py`, `src/core/retargeting/retarget_frames.py`, `src/core/retargeting/root_motion.py`, `src/core/retargeting/retarget_solve_audit.py`, `tests/test_basic_retarget_solver.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_basic_retarget_solver.py -q --basetemp .pytest_tmp_basic_retarget_solver`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py tests/test_basic_retarget_solver.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/source_animation.py src/core/retargeting/fbx_importer.py src/core/retargeting/coordinate.py src/core/retargeting/retarget_profile.py src/core/retargeting/retarget_mapping.py src/core/retargeting/reference_pose.py src/core/retargeting/retarget_frame_audit.py src/core/retargeting/retarget_solver.py src/core/retargeting/retarget_frames.py src/core/retargeting/root_motion.py src/core/retargeting/retarget_solve_audit.py src/core/game/kotor_loader.py src/core/geometry/model_data.py src/core/animation/animation_engine.py src/core/validation/animation_block_validator.py`; `git diff --check`.

- Added the source-to-KOTOR retarget mapping profile and reference-pose gate. GhostRigger can now load/save normalized retarget profiles, suggest source/target semantic roles without trusting them blindly, reject unknown UE source nodes, unknown Aurora target nodes, duplicate normal target mappings, helper/twist/IK misuse, and invalid KOTOR animation slots. The new reference builder constructs explicit source clip-rest/clip-time and target rest/animation-slot poses without mutating assets, and the mapped segment audit reports source/target lengths, direction angles, ratios, and zero-length warnings for later retarget-frame construction. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/retarget_profile.py`, `src/core/retargeting/retarget_mapping.py`, `src/core/retargeting/target_skeleton_audit.py`, `src/core/retargeting/reference_pose.py`, `src/core/retargeting/retarget_frame_audit.py`, `tests/test_retarget_mapping_profile.py`, `tests/test_retarget_reference_pose.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py -q --basetemp .pytest_tmp_retarget_profile`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py tests/test_retarget_mapping_profile.py tests/test_retarget_reference_pose.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/source_animation.py src/core/retargeting/fbx_importer.py src/core/retargeting/source_skeleton_audit.py src/core/retargeting/coordinate.py src/core/retargeting/retarget_profile.py src/core/retargeting/retarget_mapping.py src/core/retargeting/target_skeleton_audit.py src/core/retargeting/reference_pose.py src/core/retargeting/retarget_frame_audit.py src/core/game/kotor_loader.py src/core/geometry/model_data.py src/core/animation/animation_engine.py`; `git diff --check`.

- Added a backend-neutral UE/FBX source animation import and sampled-pose gate. The new source layer represents imported skeleton clips as parent-before-child source nodes, rest/reference poses, sampled global/local transforms, explicit axis/unit metadata, twist/IK/helper/root/deform classifications, matrix-based basis conversion helpers, and a source skeleton audit report. The public FBX importer now fails clearly when no supported backend is configured, while deterministic fake-backend tests prove evaluated global matrix sampling, local reconstruction, frame-0 reference fallback warnings, multi-clip selection behavior, and optional real-FBX fixture gating. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/source_animation.py`, `src/core/retargeting/fbx_importer.py`, `src/core/retargeting/coordinate.py`, `src/core/retargeting/source_skeleton_audit.py`, `tests/test_ue_fbx_source_import.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_ue_fbx_source_import.py -q --basetemp .pytest_tmp_ue_fbx_import`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_mdl_animation_override_roundtrip.py tests/test_roundtrip_verification.py tests/test_ue_fbx_source_import.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/retargeting/source_animation.py src/core/retargeting/fbx_importer.py src/core/retargeting/coordinate.py src/core/retargeting/source_skeleton_audit.py src/core/geometry/model_data.py`.

- Added an MDL animation override writer/readback round-trip gate. Export requests can now opt into post-write verification that reloads the written MDL/MDX, confirms the local override exists under the canonical KOTOR slot name, compares controller times and values with quaternion sign-equivalence, evaluates pre-write and read-back poses through the Aurora semantics oracle, checks node hierarchy/rest-transform preservation, and reports writer-boundary mismatches before a bad export can be handed off. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/validation/animation_roundtrip_validator.py`, `src/core/retargeting/aurora_animation_writer.py`, `tests/test_mdl_animation_override_roundtrip.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_mdl_animation_override_roundtrip.py -q --basetemp .pytest_tmp_mdl_anim_roundtrip`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_aurora_animation_controller_semantics.py tests/test_roundtrip_verification.py tests/test_mdl_animation_override_roundtrip.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/animation/animation_engine.py src/core/geometry/model_data.py src/core/game/kotor_loader.py src/core/retargeting/aurora_animation_writer.py src/core/validation/animation_block_validator.py src/core/validation/animation_roundtrip_validator.py src/core/mdl/mdl_writer.py`.

- Added a synthetic Aurora animation controller semantics gate. The new headless evaluator treats orientation and position controller keys as absolute parent-local values, falls back to rest transforms for unkeyed components, propagates parent transforms by FK, and verifies shortest-path quaternion interpolation. Export-ready animation injection now also runs structural animation-block validation after slot resolution and before local override mutation or MDL/MDX writing, rejecting unknown KOTOR nodes and invalid key data without creating output files. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/animation/animation_engine.py`, `src/core/validation/animation_block_validator.py`, `src/core/retargeting/aurora_animation_writer.py`, `tests/test_aurora_animation_controller_semantics.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_aurora_animation_controller_semantics.py -q --basetemp .pytest_tmp_controller_semantics`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_animation_export_slot_gate.py tests/test_roundtrip_verification.py tests/test_aurora_animation_controller_semantics.py -q --basetemp .pytest_tmp_retarget_gates`; `python -m py_compile src/core/animation/animation_engine.py src/core/geometry/model_data.py src/core/game/kotor_loader.py src/core/retargeting/aurora_animation_writer.py src/core/validation/animation_block_validator.py`.

- Wired strict KOTOR animation slot validation into the Aurora animation override export boundary. Export-ready MDL injection now resolves the requested slot through the target model/supermodel chain before local override mutation or MDL/MDX writing, copies the outgoing animation to the canonical resolved slot name, rejects UE clip names that are not valid KOTOR slots, and returns before creating export files on invalid slots. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/retargeting/aurora_animation_writer.py`, `tests/test_animation_export_slot_gate.py`, `.gitignore`.
  Verification: `python -m pytest tests/test_animation_export_slot_gate.py -q --basetemp .pytest_tmp_slot_export_gate`; `python -m pytest tests/test_animation_slot_resolution.py tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_retarget_gate`.

- Added KOTOR animation slot and supermodel-chain resolver helpers for the GhostRigger UE-to-Aurora retargeting workflow. The loader now exposes local-first valid slot listing, inherited-slot resolution with local override metadata, and diagnostic supermodel chain metadata so retarget exports can verify a real KOTOR slot before writing an MDL/MDX override. Roadmap reference: T1403 animation playback workflow.
  Affected areas: `src/core/game/kotor_loader.py`, `src/core/geometry/model_data.py`, `tests/test_animation_slot_resolution.py`.
  Verification: `python -m pytest tests/test_animation_slot_resolution.py -q --basetemp .pytest_tmp_slot_resolution`.

- Restored the grouped `src.core.diagnostics` compatibility package after syncing LordVader's latest `qt-ghostrigger` reorganization, so `src.core.qt_core` can expose validation and module-reference helpers alongside the moved `animation`, `geometry`, `game`, and `mdl` packages. No roadmap task ID applies; this is repository-sync compatibility work.
  Affected areas: `src/core/diagnostics/`, `src/core/qt_core.py` import surface.
  Verification: `python -m py_compile scripts\verify_roundtrip.py src\core\mdl\mdl_writer.py tests\test_roundtrip_verification.py src\core\validation\viewport_validator.py src\core\retargeting\aurora_animation_writer.py`; focused round-trip/core contract pytest after the sync.

- Added theme-specific Matrix bar colours and a dedicated `matrix` font role. All packaged themes now define `matrixBar.*` colour tokens matched to their palette, and the Matrix bar font defaults to the bundled `Aurebesh AF` font while remaining editable in the Theme Editor Fonts tab. No roadmap task ID applies.
  Follow-up: gave the header IPC status label its own `HeaderIpcMeta` object name and verified it resolves through `matrixBar.ipcText` for every packaged theme instead of inheriting the generic header meta colour.
  Follow-up: fixed the crash reported in `logs/ghostrigger_2026-05-21_135423.log`; the Matrix bar painter now passes `QtGui.QFont.Weight` enum values to `QFont.setWeight()` instead of raw integers, which PySide6 rejected during repaint.
  Follow-up: compacted the Settings dialog by splitting it into top-level tabs for Paths, General, Matrix Bar, Theme/Layout, and Measurement, with scrollable tab pages and two-row action grids so hidden sections no longer force a huge window size.
  Follow-up: fixed Settings dialog theme application so Apply Theme targets and repaints the dialog itself instead of disabling the parent main window updates, preventing the settings window from visually disappearing until resize.
  Follow-up: polished the Theme Editor metric tables. Theme and layout metrics now hide row numbers, use bold label widgets inside metric cells, use spin-box value editors with explicit unit suffixes, and include tab width/padding/margin metrics in both fallback tokens and packaged layout XML. Layout application and preview now understand the new tab sizing tokens.
  Follow-up: lowered the default tab height to 14 px and added theme-controlled tab style modes. Themes now load/save `<styles><style name="tab.mode" value="standard|flat|beveled"/></styles>`, the Theme Editor exposes a tab style mode selector, and the stylesheet builder renders standard, flat, and beveled tab treatments.
  Follow-up: refined collapsible tool sections. The shared `CollapsibleGroupBox` expander is now a fixed square control that ignores global button minimum sizes, reserves stable header space, restores expanded geometry, and collapses to a fixed header height. Mesh Tools now top-aligns sections with stretch only after the final group so collapsed sections stack from top to bottom.
  Follow-up: made Settings dialog Apply Theme persist immediately by emitting the normal settings payload after a successful apply, so the chosen theme is written to `settings.json` and no longer reverts to the previously saved Classic selection on restart.
  Follow-up: made Theme Editor Apply Theme persist through the main window settings writer as well. Theme/layout settings now merge with theme-owned fields applied after layout-owned fields so stale layout-manager data cannot overwrite the selected theme.
  Follow-up: moved theme stylesheet generation onto a worker `QThread` and added theme-apply progress signals. The main window now shows the themed progress toast during theme application, updates it while the background stylesheet build runs, and keeps it visible before the unavoidable main-thread Qt widget restyle step.
  Follow-up: moved the viewport mode toolbar out of the viewport widget and into a shell-owned toolbar band below the main command bar. The same viewport buttons are reparented into the new band, while side spacers track the left and right panel widths so the strip sits in the central space above the viewport column.
  Follow-up: changed the Output Log title from a clickable tool button into a plain themed section label inside the log pane, matching the Python terminal presentation. Both Output Log and Python section labels now use stylesheet tokens for monospace title styling.
  Follow-up: added the startup-console look as a packaged `Droid` theme. The new `config/themes/themes/droid.xml` uses dark graphite panels, grey controls, high-contrast text, Matrix-green accents, matching Matrix bar tokens, and the bundled Aurebesh Matrix font. Docs and theme-loading tests now list Droid as a built-in default theme.
  Follow-up: moved the header, command bar, and viewport toolbar into a non-movable reserved top `QToolBar` so the viewport tool strip sits directly under Open/Auto-Rig/Character Builder and Qt docks cannot occupy the top UI area. Detachable/toolbox docks now explicitly disallow the top dock area and are redirected back to workspace side areas if needed.
  Follow-up: replaced the reserved top area's horizontal scroll strips with wrapping flow layouts. At the default window width, command buttons and viewport controls now wrap onto left-aligned rows underneath instead of clipping or requiring a horizontal scrollbar; layout application no longer forces those wrapping rows back to a single fixed height.
  Follow-up: added a built-in `Default` native theme. `config/themes/themes/default.xml` sets `application.native=true`, causing the stylesheet builder to return no QSS and the theme applier to restore the platform Qt palette instead of applying GhostRigger font/palette overrides. Applying Default clears local GhostRigger styles and disables the Matrix bar animation so the app can be inspected with plain platform styling.
  Follow-up: fixed the gimbal crash caused by a renderer instance missing `show_gimbal`. The viewport now initializes that compatibility flag, all gimbal hover/toggle/draw paths use a defensive accessor, and the CPU base-render suppression path preserves/restores the visible gimbal state instead of converting a missing attribute into "gimbal off."
  Follow-up: restored the visible model transform gimbal after the compatibility fix. The viewport now falls back to the loaded model root if selection state is dropped, positions the root gimbal at the model bounds center like the previous renderer path, and feeds the overlay gizmo from the active theme gimbal colours.
  Follow-up: fixed the Default native theme viewport regression where the advanced transform gizmo, corner axes gimbal, and HUD stats disappeared. `FrameRenderer` now initializes render/overlay runtime state before any theme hook runs, theme colour application no longer resets animation/gimbal/walkmesh state, and native theme application resets viewport/GPU colours without requiring QSS.
  Follow-up: tied the native Default look into the viewport renderer itself. Default/native mode now derives the CPU canvas, HUD pills, grid lines, gimbal highlight text, GPU clear colour, GPU grid colours, and GPU wire colour from the active Qt palette instead of falling back to GhostRigger's dark renderer palette.
  Follow-up: restored native-style contrast for themed spin-box steppers. All custom themes now define editable `spinbox.*` colour tokens for the up/down button fill, hover, pressed, border, and arrow colours, and the generated QSS draws bordered stepper containers for `QSpinBox` and `QDoubleSpinBox` instead of leaving them visually flat against the field.
  Follow-up: replaced the QSS triangle arrows with explicit light/dark SVG arrow assets for spin-box steppers, fixing the themed up/down controls that were rendering square blocks instead of arrows.
  Follow-up: added startup theme stylesheet precaching before the Qt main-window launch. `ThemeApplier` now shares a process-wide stylesheet cache, and `main.py` prints per-theme precache progress plus a summary to the launching terminal so later theme switches use warmed stylesheets immediately.
  Follow-up: fixed collapsible tool-section expanders so they stay small square controls instead of inheriting global command-button sizing. The shared `CollapsibleGroupBox` toggle now opts out of layout button-mode sizing and the generated theme QSS includes a dedicated fixed-size selector for `QToolButton#CollapsibleGroupToggle`.
  Follow-up: moved Matrix bar controls out of Settings and into the Theme Editor. Matrix bar mode, glyphs, font override, and PNG/GIF path are now theme `<styles>` entries (`matrixBar.style`, `matrixBar.glyphs`, `matrixBar.fontFamily`, `matrixBar.imagePath`), while older app-level Matrix bar settings remain a fallback for existing user configs.
  Follow-up: fixed Default/native theme Matrix bar images. Native theme application now honors the active theme's Matrix bar config instead of forcing Matrix panels disabled, and the Theme Editor Matrix Bar preview renders selected PNG/GIF media instead of always showing the glyph strip.
  Follow-up: limited Matrix bar media to the top header only. The command bar is a normal themed frame again, Matrix media rendering now supports percentage crop rectangles, and the Theme Editor preview includes a bounded, scaled image selector so large source images no longer force the editor window oversized.
  Follow-up: adjusted the Matrix Bar image preview into a fixed-height source-image box with a crop rectangle overlay. The preview displays the full source image scaled into the box, the crop rectangle defines the Matrix bar slice, and the media no longer resizes the Theme Editor window.
  Affected areas: `main.py`, `config/themes/themes/`, `config/themes/layouts/`, `config/themes/README.md`, `knowledge_base/theme_layout_system.md`, `src/gui/assets/qt_matrix_background.py`, `src/gui/assets/qt_theme.py`, `src/gui/libtheme/style_tokens.py`, `src/gui/libtheme/theme_model.py`, `src/gui/libtheme/theme_loader.py`, `src/gui/libtheme/theme_validator.py`, `src/gui/libtheme/theme_applier.py`, `src/gui/libtheme/qt_stylesheet_builder.py`, `src/gui/libtheme/layout_applier.py`, `src/gui/libtheme/theme_editor_window.py`, `src/gui/libtheme/collapsible_group.py`, `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/panels/qt_log_panel.py`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/panels/qt_mesh_tools_panel.py`, `tests/test_theme_layout_loading.py`, `tools/validate_themes.py`, docs.
  Verification: `python -m py_compile src/gui/assets/qt_matrix_background.py src/gui/libtheme/theme_editor_window.py src/gui/libtheme/style_tokens.py`; `python -m py_compile src/gui/windows/qt_main_window.py`; `python -m py_compile src/gui/dialogs/qt_settings_dialog.py`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_model.py src/gui/libtheme/theme_loader.py src/gui/libtheme/theme_validator.py src/gui/libtheme/theme_applier.py src/gui/libtheme/qt_stylesheet_builder.py src/gui/libtheme/theme_editor_window.py src/gui/libtheme/layout_applier.py`; `python -m py_compile src/gui/libtheme/collapsible_group.py src/gui/panels/qt_mesh_tools_panel.py src/gui/panels/qt_mesh_selection_toolbar.py src/gui/panels/qt_mesh_operation_options.py src/gui/panels/qt_lighting_panel.py`; `python -m py_compile src/gui/libtheme/theme_applier.py src/gui/windows/qt_main_window.py`; `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py src/gui/libtheme/qt_stylesheet_builder.py`; `python -m py_compile src/gui/panels/qt_log_panel.py src/gui/libtheme/qt_stylesheet_builder.py`; `python -m py_compile src/gui/libtheme/theme_loader.py tests/test_theme_layout_loading.py`; `python -m py_compile src/gui/windows/qt_main_window.py src/gui/libtheme/qt_stylesheet_builder.py`; `python -m py_compile src/gui/assets/qt_theme.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py src/gui/libtheme/layout_applier.py src/gui/libtheme/qt_stylesheet_builder.py`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_model.py src/gui/libtheme/qt_stylesheet_builder.py src/gui/libtheme/theme_applier.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py`; `python -m py_compile tools/validate_themes.py tests/test_theme_layout_loading.py`; `python -m py_compile src/gui/viewports/qt_viewport.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m py_compile src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m pytest tests/test_core_contracts.py -q -k "gizmo or gpu_viewport_uses_overlay or performance_overlay or gpu_grid_is_native"`; repeated `python tools/validate_themes.py`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_loader.py src/gui/libtheme/qt_stylesheet_builder.py tests/test_theme_layout_loading.py tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python tools/validate_themes.py`; `python -m py_compile src/gui/libtheme/qt_stylesheet_builder.py tests/test_theme_layout_loading.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python tools/validate_themes.py`; `python -m py_compile main.py src/gui/libtheme/theme_applier.py tests/test_theme_layout_loading.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -c "import logging, main; logging.basicConfig(level=logging.INFO); main._precache_themes(main._APP_DIR, logging.getLogger('precache-smoke'))"`; `python -m py_compile src/gui/libtheme/collapsible_group.py src/gui/libtheme/layout_applier.py src/gui/libtheme/qt_stylesheet_builder.py tests/test_theme_layout_loading.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m py_compile src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_loader.py src/gui/dialogs/qt_settings_dialog.py src/gui/libtheme/theme_editor_window.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m py_compile src/gui/libtheme/theme_editor_window.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m py_compile src/gui/assets/qt_matrix_background.py src/gui/libtheme/style_tokens.py src/gui/libtheme/theme_editor_window.py src/gui/windows/qt_main_window.py tests/test_theme_layout_loading.py`; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m py_compile src/gui/libtheme/theme_editor_window.py tests/test_theme_layout_loading.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; offscreen smoke verified all packaged themes resolve `matrixBar.*` tokens, expose the `matrix` font role as `Aurebesh AF`, apply `matrixBar.ipcText` to the IPC header label, render the Matrix/header bars without the `QFont.setWeight` TypeError, instantiate the compact tabbed Settings dialog at `720x500` with a `452x358` size hint, keep the Settings dialog visible/updates-enabled after Apply Theme, persist an Apply Theme selection to a temp `settings.json`, persist a Theme Editor Apply Theme selection through the main-window settings writer, edit Theme Editor metric/layout spin boxes including `tab.width`, load every packaged layout with `tabHeight=14`, switch Theme Editor tab mode to `flat`, collapse/expand Mesh Tools sections with square toggles and top-to-bottom ordering, apply a theme through the worker/progress path with background-build and final Qt-widget progress events, instantiate the main shell offscreen with the viewport toolbar hosted by `ViewportToolbarBand` instead of the viewport layout, instantiate `QtLogPanel` offscreen with Output Log as a `QLabel` and no header `QToolButton`, instantiate the main shell offscreen with the reserved top toolbar spanning the full window while visible docks start at the central workspace top and reject `TopDockWidgetArea`, instantiate the default-width shell with wrapped command and viewport rows reporting expanded heights instead of clipped single-line geometry, apply the Default theme offscreen with empty application/window stylesheets and disabled Matrix bars, simulate a missing `FrameRenderer.show_gimbal` attribute through CPU suppress/restore plus toggle paths while preserving visible gimbal state, verify the gimbal fallback uses the model root bounds center plus active theme axis/highlight colours, verify the native Default viewport renders the advanced gizmo handles plus HUD overlay path without a QSS theme apply, verify a deterministic native Qt palette produces matching viewport background/HUD colours while preserving gizmo handles, instantiate themed spin boxes for Matrix/Dark/Droid/Light/Classic with the new stepper background and border tokens, verify those stylesheets reference the expected light/dark SVG arrow assets instead of QSS triangle borders, smoke-check the startup theme precache terminal output for all six packaged themes, confirm a Matrix-themed collapsible group toggle remains 16x16 after layout button-mode application, confirm Settings no longer exposes a Matrix Bar tab while the Theme Editor does, confirm the Matrix Bar preview displays a selected PNG pixmap, confirm Matrix media is no longer wired to the command bar, confirm crop values persist from the preview selector, and confirm the selected crop fills a bounded preview area without tinting.

- Completed the requested third-pass UI theme/layout tweaks. Settings now opens non-modally, a new `Customise` menu exposes Settings and Theme Editor directly, packaged theme button defaults are 16 px high by 64 px wide, progress toasts use theme tokens, the lighting panel and Character Builder rail/inspector respond to theme/layout hooks, the matrix header/command bar can use theme-driven colours plus configurable glyphs/fonts/PNG/GIF/disabled modes, the version/IPC header text is tokenized, the viewport gimbal uses theme colours, the inactive X-Ray toolbar button is hidden, Ctrl+D diagnostics is wired through the diagnostics window again, Theme Editor colour rows show swatches with clearer token/value columns, mesh/lighting tool group sections can collapse/expand, and floated dock panels restore to docked state while remembering size. No roadmap task ID applies.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/assets/qt_matrix_background.py`, `src/gui/libtheme/`, `src/gui/panels/`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `config/themes/themes/`, `tests/test_theme_layout_loading.py`.
  Verification: `python -m py_compile` for touched shell, dialog, theme, panel, viewport, renderer, and diagnostics modules; offscreen main-window smoke for non-modal Settings, direct Theme Editor menu/window, themed toast, and Ctrl+D diagnostics window; `python tools/validate_themes.py`; `python -m pytest tests/test_theme_layout_loading.py -q`; `python -m pytest tests/test_qt_only_imports.py -q`. The focused theme pytest passed, then reported the known Windows temp-directory cleanup `PermissionError` after completion.

- Finished the viewport theme follow-up. The viewport toolbar shell, floating view-snap buttons, transform type-in bar, empty/model viewport background, CPU renderer background/grid/HUD colours, and GPU renderer clear/grid/wire colours now consume active theme tokens instead of staying on local dark styling. Compact layout metrics are also forwarded into the transform type-in strip. No roadmap task ID applies.
  Affected areas: `src/gui/viewports/qt_viewport.py`, `src/gui/viewports/qt_transform_typein_bar.py`, `src/gui/rendering/viewport_core.py`, `src/gui/rendering/gpu_renderer.py`, `tests/test_theme_layout_loading.py`.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/viewports/qt_transform_typein_bar.py src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py tests/test_theme_layout_loading.py`; offscreen Classic/Compact `QtViewportWidget` smoke; `python -m pytest tests/test_theme_layout_loading.py -q`; `python tools/validate_themes.py`. The focused pytest run passed, then reported the known Windows temp-directory cleanup `PermissionError` after completion.

- Completed the second-pass theme/layout overhaul. Theme apply is now cached, debounced, and timed; unchanged applies are skipped; app styling is applied once through `QApplication`; theme-aware hooks no longer trigger recursive full applies. The settings button is now "Apply Theme", Light/Classic readability tokens were strengthened, Compact layout density was tightened, and Default/Wide/Cinematic gained explicit density metrics. Added a Theme Editor with local live preview for colours, fonts, metrics, button modes, layout metrics, safe user XML save/backup, and explicit Apply Theme / Apply Layout actions. No roadmap task ID applies.
  Affected areas: `src/gui/libtheme/`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/windows/qt_main_window.py`, `src/gui/panels/qt_camera_panel.py`, `src/gui/panels/qt_mesh_tools_panel.py`, `src/gui/panels/qt_character_builder_panel.py`, `src/gui/sequence_editor/`, `src/gui/windows/qt_retarget_window.py`, `config/themes/`, `tools/validate_themes.py`, docs.
  Verification: `python -m pytest tests/test_theme_layout_loading.py -q`; `python tools/validate_themes.py`; offscreen Qt smoke for `QtSettingsDialog` and `ThemeEditorWindow`; `python -m py_compile` for the touched theme/layout, dialog, panel, sequence, retarget, and Character Builder modules; `python -m pytest tests/test_qt_only_imports.py -q`; `python -m pytest tests/ -m "not slow" -x -q` reached 733 passing and 15 skipped tests before stopping on the pre-existing `tests/test_regression_screens.py::test_t1102_character_builder_mode_screenshots[headless_body]` similarity `0.0000` failure. Focused pytest runs passed and then reported a Windows temp-directory cleanup `PermissionError` after completion.

- Added an XML-driven theme and layout system for the Qt UI. Matrix is now a selectable packaged theme alongside Dark, Light, and Classic; Default, Compact, Wide, and Cinematic layouts control real main-window, splitter, panel, toolbar, viewport, button-mode, icon-size, and hot-reload behaviour. Native OS light/dark detection uses `darkdetect`, user override directories use `platformdirs`, icons can resolve through `qtawesome`, and invalid XML is handled with diagnostics rather than crashes. No roadmap task ID applies.
  Affected areas: `src/gui/libtheme/`, `config/themes/`, `src/gui/assets/qt_theme.py`, `src/gui/windows/qt_main_window.py`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/qt_lib.py`, docs.
  Verification: `python -m py_compile` for `src/gui/libtheme/*.py`, touched Qt shell/dialog/viewport/facade modules, and `tests/test_theme_layout_loading.py`; `pytest tests/test_theme_layout_loading.py -q`; `pytest tests/test_qt_only_imports.py -q`; offscreen Qt smoke instantiated `QtSettingsDialog` and `QtGhostRiggerMainWindow` with Matrix/default layout. The focused theme-layout pytest passed, then reported a Windows temp-directory cleanup `PermissionError` after completion.

- Added the native GhostRigger Sequence Editor and `.grseq` GhostRigger Level Sequence asset workflow. The new system includes frame-accurate sequence time, stable scene-object bindings, typed keyframes/tracks, transform/camera-cut/camera-property/light-property/visibility/material/event/sub-sequence/rig/character/audio tracks, deterministic evaluation with scene-state restoration, playback control, clipboard/undo hooks, image-sequence rendering support, settings persistence, a standalone Qt editor window, embedded sequence viewport, timeline/dope sheet, outliner/track list, property panel, transport bar, curve editor, sequence library selector, main-window action, and focused core tests. No roadmap task ID applies.
  Affected areas: `src/sequence/`, `src/gui/sequence_editor/`, `src/gui/qt_lib.py`, `src/gui/windows/qt_main_window.py`, `tests/test_sequence_editor_core.py`.
  Verification: `python -m compileall src/sequence src/gui/sequence_editor src/gui/qt_lib.py src/gui/windows/qt_main_window.py`; `pytest tests/test_sequence_editor_core.py -q`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py -q`; offscreen Qt smoke instantiated `SequenceEditorWindow` and created the default `intro_cutscene` sequence. Pytest reported all selected tests passing, followed by a Windows temp-directory cleanup `PermissionError` after completion.
  Follow-up: made the Sequence Editor dockable in the main application while preserving the standalone window workflow. Docked editors evaluate and preview through the main viewport; standalone editors keep their own embedded preview viewport. Added viewport-free `Curve Editor` and `Camera Cut Review` layout modes alongside `+ Viewport` variants.
  Verification: `python -m compileall src/gui/sequence_editor src/gui/windows/qt_main_window.py src/gui/qt_lib.py tests/test_sequence_editor_core.py`; `pytest tests/test_sequence_editor_core.py -q`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py -q`.
- Added Sprint 3.5 Phase 3.A round-trip verification for GhostRigger's MDL reader/writer before further retargeting-quality work. The new verifier reports byte identity as a bonus, structural controller preservation as the primary writer oracle, and viewport SSIM as visual fallback. It also exposed and fixed a stock `S_Male02` `talk` animation writer cycle where descendant `anim_root=talkdummy` was incorrectly treated as the serialized tree root. Locked Principle #13 and the first-known UE5-to-KOTOR pipeline note were documented. No roadmap task ID applies; this is Sprint 3.5 verification-gate work.
  Affected areas: `scripts/verify_roundtrip.py`, `src/core/mdl/mdl_writer.py`, `tests/test_roundtrip_verification.py`, `knowledge_base/retargeting/verification/phase3a_roundtrip_results_20260521.md`, `knowledge_base/retargeting/prior_art_landscape.md`, `knowledge_base/retargeting/first_known_ue5_to_kotor_pipeline.md`, `knowledge_base/retargeting/audits/sprint3_5_quality_gate_principle_20260521.md`, `outreach/kotorblender_collaboration_note.md`, `exports/verification/roundtrip/`.
  Verification: `python -m pytest tests/test_roundtrip_verification.py -q --basetemp .pytest_tmp_roundtrip` passed 5 tests; `python scripts/verify_roundtrip.py --input tests\fixtures\kotor_stock\k1\S_Male02.mdl --animation pause1 --output exports\verification\roundtrip\s_male02 --levels 1,2 --game k1` passed Level 2 with 166/166 local animations preserved and max node/controller deltas of `0.000e+00`; `python scripts/verify_roundtrip.py --input tests\fixtures\kotor_stock\k1\pmbam.mdl --animation victory --output exports\verification\roundtrip\pmbam --levels 1,2,3 --frames 0,30,60,90,120,150 --game k1` passed Level 2 for PMBAM structure and Level 3 with min SSIM `1.00000`.

- Added Sprint 3.5 Phase 2 reverse-retargeting quality fix. R3.A now records UE5 rest-pose basis metadata, R3.B remaps per-bone orientation deltas through a matrix-conjugated source/target basis bridge, and the writer now treats source clip frame 0 as the retarget reference pose before applying motion deltas. This produced a Tier 2+ candidate PMBAM `victory` override, but it is not Tier 3 and does not unblock R4 until Phase 3 ground-truth verification and the final quality gate pass. No roadmap task ID applies; this is Sprint 3.5 reverse-retargeting quality-gate work.
  Affected areas: `scripts/blender_extract_ue5_animation.py`, `src/core/retargeting/animation_injector.py`, `src/core/retargeting/aurora_animation_writer.py`, `tests/test_basis_remapping.py`, `tests/test_aurora_animation_writer.py`, `tests/test_animation_injector.py`, `knowledge_base/retargeting/quality_audit/r3b_visual_issues_20260521.md`, `exports/r3_idle_test_v2/`.
  Verification: `python -m pytest tests/test_basis_remapping.py tests/test_aurora_animation_writer.py tests/test_animation_injector.py tests/test_coordinate_converter.py -q --basetemp .pytest_tmp_basis` passed 21 tests with 1 external-FBX smoke skipped; `python scripts\inject_animation.py --source-fbx C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx --target-mdl tests\fixtures\kotor_stock\k1\pmbam.mdl --slot victory --rename-map knowledge_base\retargeting\ue5_to_aurora_rename_map.json --output exports\r3_idle_test_v2 --write-mdl --output-mdl exports\r3_idle_test_v2\pmbam__victory__r3b_v2.mdl` wrote MDL SHA-256 `903bd5214080d69762a962a234c56fc5b19de2ba5043eabdf251d58b0a39454c`, 20 animated bones, and 302 frames at 30 FPS; viewport validation rendered front, three-quarter, side-left, back, and all 302 low-resolution frames successfully; `python scripts\audit_bone_positions.py --model exports\r3_idle_test_v2\pmbam__victory__r3b_v2.mdl --rest-validation exports\r3_idle_test_v2\quality_audit\front\pmbam__victory__r3b_v2_validation.json --validation exports\r3_idle_test_v2\quality_audit\full_motion_64\pmbam__victory__r3b_v2_validation.json --output exports\r3_idle_test_v2\quality_audit\bone_position_audit.json` reported 0 length issues and 0 frame-to-frame motion issues.

- Added Sprint 3.5's quality-gate course correction and diagnostic audit. R4 live testing is now explicitly blocked until Tier 3 viewport quality is reached, Locked Principle #12 is documented, multi-angle and all-frame viewport captures were generated for the current R3.B MDL, the viewport still-render camera preset bug was fixed by setting `ArcBallCamera.azimuth/elevation`, and the quality audit identifies pose/basis mismatch rather than binary playback or bone-length stretch as the current blocker. No roadmap task ID applies; this is Sprint 3.5 reverse-retargeting quality-gate work.
  Affected areas: `src/gui/rendering/viewport_core.py`, `scripts/audit_bone_positions.py`, `tests/test_viewport_validator.py`, `knowledge_base/retargeting/quality_audit/r3b_visual_issues_20260521.md`, `knowledge_base/retargeting/audits/sprint3_5_quality_gate_principle_20260521.md`, `handoff/r4_patch_manager_test/`, `exports/r3_idle_test/quality_audit/`.
  Verification: `python scripts\validate_mdl.py --mdl exports\r3_idle_test\pmbam__victory__r3b.mdl --animation victory --frames 0,30,60,90,120,150,180,210,240,270,300 --output exports\r3_idle_test\quality_audit\front --camera front_ortho`; regenerated three-quarter, side-left, and back captures after the camera fix and verified frame-150 hashes differ by camera; rendered 302 full-motion 64x64 frames through the viewport; `python scripts\audit_bone_positions.py --model exports\r3_idle_test\pmbam__victory__r3b.mdl --rest-validation exports\calibration\baselines\pmbam_validation.json --validation exports\r3_idle_test\quality_audit\full_motion_64\pmbam__victory__r3b_validation.json --output exports\r3_idle_test\quality_audit\bone_position_audit.json` reported 0 length issues and 0 frame-to-frame motion jumps at the configured thresholds.

- Added the Sprint 3 R4 Patch Manager live-test handoff package for the retargeted UE5 idle PMBAM `victory` override. The handoff includes Patch Manager manifest metadata, override MDL/MDX payload, vanilla rollback references, Ghost Rigger injection and viewport validation reports, a frame-150 reference capture, install-verification helper, execution prompt, Sprint 3 retrospective, and initial Sprint 4 quality scope. No roadmap task ID applies; this is Sprint 3 reverse-retargeting handoff work.
  Affected areas: `handoff/r4_patch_manager_test/`, `knowledge_base/retargeting/audits/sprint3_reverse_retargeting_retrospective_20260521.md`, `knowledge_base/retargeting/sprint4_animation_quality_scope_20260521.md`, `CHANGES.md`.
  Verification: SHA-256 hashes were computed for all handoff payload files: modified `pmbam.mdl` `bfcd3468838050d25159afa3c90d963fec1a06fcbfc64b6ea9982adc4a8be8df`, modified `pmbam.mdx` `84dc9b42faa0b2004c0e10eef6ab0bc65e02ee7bffaca027b829574832a58154`, vanilla baseline MDL `f439fbdbf9e50ef994d14c333d0829017ad72bcfc1bf6f922420943e37ebf3f1`, vanilla baseline MDX `a912c9fb3f8e785f06652e778295ebe6655d20fdb82a437456cd71939b4c6b22`, injection manifest `c910363dda141c2e76493d5e85f54ada45cfb9e7b628a50e1b4cf669dbb916ba`, viewport report `fac8c5ce23774a87b9ae7ca646d9266e03cd8cdac147b573dae2df2da2ee7eb4`, and frame-150 PNG `0061e6c07c462afef569ff9204287037b4381890d3fb0e0d5f40225a83bf1c2a`.

- Added Sprint 3 R3.B/R3.C reverse animation injection into PMBAM. The pipeline now takes the R3.A UE5 idle payload, stores source rest/parent transforms, computes source-local rest-relative rotation deltas, converts them to Aurora, applies them to PMBAM bind-local rotations, appends a local `victory` override, writes binary MDL/MDX through `MDLBinaryWriter`, and validates playback in the Ghost Rigger viewport. No roadmap task ID applies; this is Sprint 3 reverse-retargeting work.
  Affected areas: `src/core/retargeting/coordinate_converter.py`, `src/core/retargeting/aurora_animation_writer.py`, `src/core/retargeting/animation_injector.py`, `src/core/retargeting/__init__.py`, `scripts/inject_animation.py`, `scripts/inspect_pmbam_animation.py`, `tests/test_coordinate_converter.py`, `tests/test_aurora_animation_writer.py`, `tests/test_animation_injector.py`, `knowledge_base/retargeting/sprint3_r3_animation_injection_20260521.md`, `exports/r3_idle_test/`.
  Verification: `python scripts\inspect_pmbam_animation.py` confirmed PMBAM has 0 local animations, 267 inherited animations, and inherited `victory`; `python -m pytest tests\test_coordinate_converter.py tests\test_aurora_animation_writer.py tests\test_animation_injector.py -q --basetemp .pytest_tmp_r3b` passed 16 tests with 1 opt-in Blender smoke skipped; `python scripts\inject_animation.py --source-fbx "C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx" --target-mdl tests\fixtures\kotor_stock\k1\pmbam.mdl --slot victory --rename-map knowledge_base\retargeting\ue5_to_aurora_rename_map.json --output exports\r3_idle_test --game K1 --write-mdl` wrote `exports\r3_idle_test\pmbam__victory__r3b.mdl` with SHA-256 `bfcd3468838050d25159afa3c90d963fec1a06fcbfc64b6ea9982adc4a8be8df`, 20 animated bones, 302 frames at 30 FPS, 293,360-byte output size below the 2x stop condition; `python scripts\validate_mdl.py --mdl exports\r3_idle_test\pmbam__victory__r3b.mdl --animation victory --frames 0,75,150,225,300 --output exports\r3_idle_test\viewport_captures --camera front_ortho` loaded 61 nodes, 44 meshes, 1 local animation, and rendered 5 captures successfully.

- Added Sprint 3 R3.A reverse animation extraction for the UE5 Game Animation Sample idle FBX. The new path imports `M_Neutral_Stand_Idle_Loop_export.fbx` through Blender 4.2, extracts 302 frames of source bone world transforms, adapts 87 source channels through the reverse UE5-to-Aurora policy, maps 20 PMBAM-compatible channels, drops 64 helper/finger/twist/extra channels, marks 3 spine collapse candidates, and writes retarget-ready JSON for the later MDL writer injection stage. No roadmap task ID applies; this is Sprint 3 reverse-retargeting work.
  Affected areas: `src/core/retargeting/ue5_source_adapter.py`, `src/core/retargeting/blender_animation_injection.py`, `src/core/retargeting/animation_injector.py`, `src/core/retargeting/__init__.py`, `scripts/blender_extract_ue5_animation.py`, `scripts/inject_animation.py`, `tests/test_animation_injector.py`, `knowledge_base/retargeting/sprint3_r3_animation_injection_20260521.md`, `exports/r3_idle_test/`.
  Verification: `python -m py_compile src\core\retargeting\ue5_source_adapter.py src\core\retargeting\blender_animation_injection.py src\core\retargeting\animation_injector.py scripts\blender_extract_ue5_animation.py scripts\inject_animation.py tests\test_animation_injector.py`; `python -m pytest tests\test_animation_injector.py -q --basetemp .pytest_tmp_anim_injector` passed 5 tests with 1 opt-in Blender smoke skipped; `python scripts\inject_animation.py --source-fbx "C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx" --target-mdl tests\fixtures\kotor_stock\k1\pmbam.mdl --slot victory --rename-map knowledge_base\retargeting\ue5_to_aurora_rename_map.json --output exports\r3_idle_test --game K1` passed with source SHA-256 `20d4f1fe351aab873fbea54d66e38063665eceaeb4ca519004981c289b01a211`, target SHA-256 `f439fbdbf9e50ef994d14c333d0829017ad72bcfc1bf6f922420943e37ebf3f1`, 87 source bones, 61 target nodes, 20 mapped channels, 64 dropped channels, 3 collapsed channels, 0 unmapped channels, and 302 frames at 30 FPS.

- Added Sprint 3 R2.75's Ghost Rigger viewport validation harness. The new validator loads binary or ASCII MDL files, renders deterministic headless captures through `FrameRenderer`, evaluates inherited Aurora animations through `AnimationEngine`, extracts per-frame node world transforms, computes optional SSIM against reference captures, and exposes the flow through `scripts/validate_mdl.py`. It also records the Day 4.5 v6 validation-methodology addendum that reclassifies Blender as an FBX authoring gate and the Ghost Rigger viewport as the canonical MDL visual gate. No roadmap task ID applies; this is Sprint 3 reverse-retargeting gate work.
  Affected areas: `src/core/validation/`, `scripts/validate_mdl.py`, `tests/test_viewport_validator.py`, `knowledge_base/validation/`, `knowledge_base/retargeting/audits/day4_5_v6_addendum_20260521.md`, `exports/calibration/baselines/`.
  Verification: `python -m py_compile src\core\validation\capture_specs.py src\core\validation\viewport_validator.py scripts\validate_mdl.py`; `python -m pytest tests\test_viewport_validator.py -q --basetemp .pytest_tmp_viewport` passed 7 tests with 1 ASCII-fixture skip; captured vanilla PMBAM front, three-quarter, side-left, and `g1a1` reference frames with `scripts\validate_mdl.py`. Baseline PMBAM reported SHA-256 `f439fbdbf9e50ef994d14c333d0829017ad72bcfc1bf6f922420943e37ebf3f1`, 61 nodes, 44 meshes, and 267 inherited animations.

- Added a reproducible Sprint 3 Gate 2 validation script for UE5 Game Animation Sample exports. The script consumes the Blender FBX inspection JSON, compares the exported Manny/UEFN skeleton against the PMBAM Day 4.5 v6 bind contract through the reverse rename map, writes JSON/Markdown gate reports, and records HALT diagnostics before animation injection is attempted. No roadmap task ID applies; this is reverse-retargeting gate work.
  Affected areas: `scripts/gate2_validate_reverse_sample.py`.
  Verification: exported `M_Neutral_Stand_Idle_Loop` from the Game Animation Sample through Unreal 5.6 to `C:\Users\NewAdmin\Documents\KaiGenInteractive\AnimationLibrary\Exports\M_Neutral_Stand_Idle_Loop_export.fbx`; inspected it through Blender 4.2 using `scripts/blender_gate2_inspect_fbx.py`; ran `python scripts\gate2_validate_reverse_sample.py ...` to produce `M_Neutral_Stand_Idle_Loop_gate2_validation.json` and `M_Neutral_Stand_Idle_Loop_gate2_report.md`. The FBX imported cleanly with 87 bones, one mesh, and a 302-frame action; the reverse gate correctly HALTed because the sample uses `attach` rather than mapped `root` and the current raw quaternion A-pose validator exceeds the 25 degree core-bone stop condition across UE5/Aurora bone-axis conventions.

- Added Sprint 1.5's Workbench UE5 Rig Export path. The Unreal Animator now exposes a `UE5 Rig Export` panel with character/animation pickers, output directory selection, a primary `Export UE5 FBX` action, validation metrics, artifact links, and an open-folder handoff. The backend wraps the validated Day 4.5 v6 export, runs the visual gate, writes the Sprint 1.5 manifest, and generates per-export UE5 setup notes. No roadmap task ID applies; this is Phase 2 retargeting sprint work.
  Affected areas: `src/workbench/ue5_rig_export.py`, `src/workbench/__init__.py`, `src/gui/panels/qt_ue5_rig_export_panel.py`, `src/gui/windows/qt_unreal_animator.py`, `src/gui/qt_lib.py`, `tests/test_workbench_ue5_rig_export.py`.
  Verification: `python -m pytest tests/test_workbench_ue5_rig_export.py -q` passed 6 tests; Qt import smoke loaded `QtUE5RigExportPanel` and `QtUnrealAnimatorWindow`.

- Added Sprint 1 Day 4.5 v6's rename-only KOTOR-to-UE5 FBX export path. The exporter now preserves Aurora's native bind pose, builds Blender armatures from `bind_world_matrix_4x4`, binds mesh weights by bone-name vertex groups, bakes KOTOR animation curves as deltas from rest, applies UE-style bone names, and serializes zero-weight twist/head/neck helper leaves without rest-pose forcing. No roadmap task ID applies; this is the retargeting sprint recovery from the Day 4 visual failure.
  Affected areas: `src/core/retargeting/fbx_exporter.py`, `src/core/retargeting/skeleton_renamer.py`, `scripts/blender_fbx_export.py`, `knowledge_base/retargeting/aurora_to_ue5_rename_map.json`, `tests/test_skeleton_renamer.py`, `tests/test_ground_truth_contract.py`, `tests/test_blender_fbx_export_script.py`, `tests/test_fbx_exporter_visual.py`, `knowledge_base/retargeting/prior_art_landscape.md`, `knowledge_base/retargeting/audits/day4_5_v6_skeleton_renamer_20260521.md`, `docs/unity_humanoid_setup.md`, `docs/ue5_ik_retargeter_setup.md`, `exports/retargets/day4_5/`.
  Verification: MCP ground truth refreshed for PMBAM/PFBAM/Quinn before export changes; generated Phase 0 v6 bind/mesh/clip contracts under `knowledge_base/retargeting/ground_truth/day4_5_v6_20260521T032834Z/`; `python -m pytest tests/test_skeleton_renamer.py tests/test_ground_truth_contract.py tests/test_blender_fbx_export_script.py tests/test_fbx_exporter_visual.py -q` passed 22 tests; `python -m pytest tests/test_sampler.py tests/test_coordinate_normalizer.py tests/test_baker.py -q` passed 9 regression tests; `python -m pytest tests/test_fbx_exporter.py -q` passed 10 legacy FBX tests. The new FBX `exports/retargets/day4_5/pmbam__g1a1__day4_5_v6.fbx` roundtripped with 68/68 bones, 1184/1184 vertices, 45/45 frames, no generated `_end` bones, Z-up/-Y-forward metadata, and visual gate metrics of height `0.9999999658`, width `1.0`, silhouette proxy `0.9994565217`, and no missing Unity humanoid bones. Live KOTOR install remained read-only.

- Added shared Qt overflow scroll wrappers for dense toolbar strips and dock contents. The main command bar, viewport toolbar, Character Builder toolbar, detachable dock panels, and Character Builder status dock now expose scrollbars when their contents exceed the available container. No roadmap task ID applies.
  Affected areas: `src/gui/assets/qt_theme.py`, `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/panels/qt_character_builder_panel.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/assets/qt_theme.py src/gui/windows/qt_main_window.py src/gui/viewports/qt_viewport.py src/gui/panels/qt_character_builder_panel.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_overflow_helpers_scroll_dense_toolbar_rows tests/test_core_contracts.py::test_main_window_command_bar_and_docks_are_scrollable tests/test_core_contracts.py::test_viewport_and_character_builder_toolbars_are_scrollable tests/test_core_contracts.py::test_main_window_exposes_module_meshes_as_detachable_dock tests/test_core_contracts.py::test_module_mesh_panel_can_request_detachable_window -q`; `pytest tests/test_qt_only_imports.py tests/test_composite_workflow.py::test_launch_hud_defaults_to_accurig_like_body_workflow -q`; offscreen Qt smoke instantiated the compact viewport and Character Builder and found the expected toolbar/status scroll areas.

- Added a dockable 3ds Max-style Mesh Tools panel and a Python-native editable mesh backend with sub-object modes for Object, Vertex, Edge, Border, Face, Polygon, and Element. The backend includes topology caching, validation, selection conversion, mesh history snapshots, attach/combine, detach, weld/target weld, compatible edge weld helper, bridge, connect, cap border, delete, isolated-vertex cleanup, and normal flip/recalculate operations with UV/material/Aurora metadata preservation hooks. No roadmap task ID applies.
  Affected areas: `src/mesh_tools/`, `src/gui/panels/qt_mesh_tools_panel.py`, `src/gui/panels/qt_mesh_selection_toolbar.py`, `src/gui/panels/qt_mesh_operation_options.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `tests/test_mesh_tools.py`.
  Verification: `Get-ChildItem src/mesh_tools/*.py | ForEach-Object { python -m py_compile $_.FullName }; python -m py_compile src/gui/panels/qt_mesh_tools_panel.py src/gui/panels/qt_mesh_selection_toolbar.py src/gui/panels/qt_mesh_operation_options.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py src/gui/qt_lib.py`; `pytest tests/test_mesh_tools.py -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/ -m "not slow" -q` ran 783 passing tests and 14 skipped before reporting pre-existing screenshot similarity failures in `tests/test_regression_screens.py` plus static tests looking for deleted `src/gui/viewport_core.py`.

- Added a native cinematic camera workflow for the Qt viewport. Cameras are real scene objects with serializable lens/frame/DOF/render metadata, a camera manager, viewport camera switching, lock-view-to-camera navigation, helper/frustum drawing, viewport picking, camera explorer/properties UI, render-still dialog, safe-frame/letterbox overlays, and PNG/JPG/TGA still output through the existing viewport render pipeline. No roadmap task ID applies.
  Affected areas: `src/gui/camera/`, `src/gui/panels/qt_camera_panel.py`, `src/gui/dialogs/qt_render_frame_dialog.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/windows/qt_main_window.py`, `src/gui/gizmo/transform_controller.py`, `src/gui/qt_lib.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/camera/*.py src/gui/panels/qt_camera_panel.py src/gui/dialogs/qt_render_frame_dialog.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py src/gui/gizmo/transform_controller.py src/gui/qt_lib.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_cinematic_camera_model_links_focal_length_and_fov tests/test_core_contracts.py::test_camera_manager_serializes_scene_cameras_and_active_camera tests/test_core_contracts.py::test_render_output_builds_incrementing_camera_paths tests/test_core_contracts.py::test_qt_viewport_exposes_cinematic_camera_workflow_methods tests/test_core_contracts.py::test_qt_lighting_panel_select_light_syncs_from_viewport_without_emitting -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/test_core_contracts.py -q -k "camera or transform_gizmo_controller or lighting_panel_select_light"`; offscreen viewport smoke created `Camera001`, switched to it, returned to perspective, and saved a PNG still frame.
  Follow-up: fixed still renders so viewport-only camera guides/safe-frame grids are suppressed before capture and only the checked render-dialog overlays are burned in. Letterbox bars in saved PNG/JPG/TGA output now burn as fully opaque black instead of semi-transparent grey. No roadmap task ID applies.
  Verification: `python -m py_compile src/gui/camera/camera_overlays.py src/gui/camera/frame_renderer.py src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_camera_letterbox_render_burns_opaque_black_bars tests/test_core_contracts.py::test_still_frame_renderer_suppresses_viewport_camera_overlays tests/test_core_contracts.py::test_qt_viewport_exposes_cinematic_camera_workflow_methods -q`; offscreen render smoke confirmed a letterbox pixel saved as `(0, 0, 0, 255)` with safe-frame/guides disabled.

- Added a Qt viewport grid visibility toggle so users can hide or restore the floor grid from the viewport toolbar or Model menu. The CPU renderer now respects `show_grid`, and the Qt GPU path forwards the same state instead of forcing the native GPU grid on. No roadmap task ID applies.
  Affected areas: `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/rendering/viewport_core.py src/gui/viewports/qt_viewport.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_grid_toggle_controls_cpu_and_gpu_paths tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback -q`; `pytest tests/test_core_contracts.py::test_qt_viewport_gpu_grid_is_native_and_xray_is_overlay_only tests/test_core_contracts.py::test_qt_viewport_grid_toggle_controls_cpu_and_gpu_paths -q`; `pytest tests/test_qt_only_imports.py -q`. `pytest tests/ -x` ran 705 passing tests before stopping on existing screenshot failure `tests/test_regression_screens.py::test_t1102_character_builder_mode_screenshots[headless_body]` with similarity `0.0000`.
  Follow-up: mapped grid hide/show to `Alt+G`, added the shortcut to viewport navigation help, aligned co-loaded WOK/PWK/DWK/BWM overlays to the loaded model's local viewport position using the model's AABB walkmesh bounds when available, and exposed co-loaded walkmesh overlays as selectable display-only rows in the Module Meshes Walkmeshes tab. Panel hide/unhide now suppresses the linked overlay draw through the proxy row. No roadmap task ID applies.
  Verification: `python -m py_compile src/gui/rendering/viewport_navigation.py src/gui/rendering/viewport_core.py src/gui/viewports/qt_viewport.py src/gui/panels/qt_properties_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_grid_toggle_controls_cpu_and_gpu_paths tests/test_core_contracts.py::test_viewport_navigation_profiles_are_available tests/test_core_contracts.py::test_qt_viewport_gpu_grid_is_native_and_xray_is_overlay_only tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_coloaded_walkmesh_overlay_nodes tests/test_core_contracts.py::test_coloaded_walkmesh_overlay_aligns_to_existing_model_walkmesh_bounds -q`; `pytest tests/test_core_contracts.py::test_module_mesh_properties_panel_splits_meshes_nulls_and_walkmeshes tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_selects_and_hides_meshes tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_coloaded_walkmesh_overlay_nodes -q`; `pytest tests/test_qt_only_imports.py -q`; local stock-room probe for `K1:m01aa_01a` confirmed the co-loaded WOK overlay proxy bounds match the internal model walkmesh bounds after offset. `pytest tests/ -x` ran 707 passing tests before stopping on existing screenshot failure `tests/test_regression_screens.py::test_t1102_character_builder_mode_screenshots[headless_body]` with similarity `0.0000`.
  Follow-up: separated the bottom-right viewport performance badge from the render-bounds stats badge so they stack in two neat rows instead of drawing on top of each other. No roadmap task ID applies.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_performance_overlay_stacks_above_stats_badge -q`.

## 2026-05-20

- Upgraded the Qt lightmap baker into a UV-aware, previewable Python-native baking workflow. Added artist-facing UV channel inspection (`UV1`/`UV2`/`UV3` mapped to zero-indexed internals), stricter validation for overlaps/out-of-bounds/coverage, xatlas-backed lightmap UV generation with split UV index storage and a safe fallback atlas, Open3D raycasting support with CPU fallback, preview caching, preview/comparison windows, live preview worker cancellation, and final bake assignment metadata that does not overwrite original MDL lightmap fields. No roadmap task ID applies.
  Affected areas: `src/gui/lighting/lightmap_*.py`, `src/gui/lighting/uv_*.py`, `src/gui/lighting/preview_cache.py`, `src/gui/lighting/raycast_backend.py`, `src/gui/dialogs/qt_lightmap_baker_dialog.py`, `src/gui/dialogs/*preview_window.py`, `src/gui/qt_lib.py`, `requirements.txt`, `pyproject.toml`, `tests/test_lightmap_baker.py`.
  Verification: `python -m py_compile src/gui/lighting/lightmap_bake_settings.py src/gui/lighting/lightmap_bake_job.py src/gui/lighting/lightmap_uv_validator.py src/gui/lighting/lightmap_rasterizer.py src/gui/lighting/lightmap_lighting_solver.py src/gui/lighting/lightmap_shadow_solver.py src/gui/lighting/uv_atlas_generator.py src/gui/lighting/preview_cache.py src/gui/lighting/lightmap_compare.py src/gui/lighting/raycast_backend.py src/gui/lighting/lightmap_sampler.py src/gui/lighting/lightmap_baker.py src/gui/lighting/lightmap_bake_worker.py`; `python -m py_compile src/gui/dialogs/qt_lightmap_baker_dialog.py src/gui/dialogs/uv_preview_window.py src/gui/dialogs/uv_geometry_preview_window.py src/gui/dialogs/lightmap_preview_window.py src/gui/qt_lib.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/test_qt_only_imports.py -q`. `pytest tests/ -m "not slow" -x` ran 703 passing tests before stopping on `tests/test_regression_screens.py::test_t1102_character_builder_mode_screenshots[headless_body]` with screenshot similarity `0.0000`, outside the lightmap path.

- Added Sprint 1 Day 4 FBX export for the locked `pmbam + g1a1 + aligned Quinn` forward path. GhostRigger now serializes a deterministic intermediate JSON, invokes Blender 4.2 LTS in background mode, exports a single self-contained FBX with mesh + aligned skeleton + baked animation, re-imports the FBX through Blender for roundtrip validation, writes a manifest sidecar, and exposes the narrow Day 4 path through `ghostrigger_export_unity_fbx`.
  Affected areas: `src/core/retargeting/fbx_exporter.py`, `scripts/blender_fbx_export.py`, `src/core/retargeting/__init__.py`, `src/kotormcp/tools/retargeting.py`, `src/kotormcp/tools/__init__.py`, `tests/test_fbx_exporter.py`, `tests/test_mcp_retargeting_tools.py`, `knowledge_base/retargeting/ground_truth/day4_20260521_000000Z/g1a1_metadata.json`, `knowledge_base/retargeting/audits/fbx_exporter_day4_20260520.md`, `exports/retargets/day4/`.
  Verification: Blender 4.2.0 was detected; `python -m pytest tests\test_fbx_exporter.py -v` passed 10 tests; the combined Day 2-Day 4 regression suite passed 42 tests. The generated FBX roundtripped with 89/89 bones, 661/661 vertices, 89/89 frames, no leaf bones, Z-up/-Y-forward axis metadata, and frame-0 rotation max delta `1.3700886175582738e-07`. A direct `ghostrigger_export_unity_fbx` dispatch smoke returned a 1309-byte PASS payload. Live KOTOR install remained out of scope and was not written to.

- Added Sprint 1 Day 3B and 3B.5 mesh rebinding with skeleton pre-alignment. The new loader extracts KotOR skin meshes from the isolated stock corpus, resolves skin-local MDX palette slots into global skeleton indices, and normalizes vertices into canonical world space. The rebinder remaps weights to Quinn, handles twist redistribution, preserves a raw-transplant debug mode, and now defaults to `skeleton_aligner.py`, which builds a Quinn-named Aurora-proportioned bind skeleton before the transplant. The raw Day 3B parity failure is documented and the aligned Day 3B.5 parity gates pass.
  Affected areas: `src/core/retargeting/mesh_loader.py`, `src/core/retargeting/mesh_rebinder.py`, `src/core/retargeting/skeleton_aligner.py`, `src/core/retargeting/__init__.py`, `tests/test_mesh_loader.py`, `tests/test_mesh_rebinder.py`, `tests/test_skeleton_aligner.py`, `knowledge_base/retargeting/twist_segment_maps/ue5_manny.json`, `knowledge_base/retargeting/ground_truth/day3b_20260520_230725Z/`, `knowledge_base/retargeting/ground_truth/day3b5_20260521_005604Z/`, `knowledge_base/retargeting/audits/mesh_rebinder_parity_20260520.md`.
  Verification: MCP ground truth was captured for PMBAM and Quinn before implementation; `python -m pytest tests\test_skeleton_aligner.py tests\test_mesh_rebinder.py tests\test_mesh_loader.py -v` passed 18 tests; `python -m pytest tests\test_sampler.py tests\test_coordinate_normalizer.py tests\test_baker.py tests\test_retargeting_test_utils.py tests\test_mcp_retargeting_tools.py -q` passed 13 regression tests. Raw pmbam parity failed as expected at 5.3948% mean / 7.3790% p95; aligned pmbam passed at ~3.75e-07% mean / ~2.15e-06% p95, and aligned pfbam passed at 0.2537% mean / 1.6592% p95. Live KOTOR install was read only; no write commands were issued against `C:\Program Files (x86)\Steam\steamapps\common\swkotor`.

- Added Sprint 1 Day 3A's canonical retargeting coordinate normalizer and pure animation bake core. The normalizer converts GhostRigger XYZW node rotations into WXYZ, accumulates canonical world-space bind matrices, and cross-checks against the audited G5 `MatrixPaletteUploader` inverse-bind cache. The baker computes source-to-target bind offsets, preserves same-skeleton identity retargets, bakes target-shaped `SampledClip` output, and derives first-pass Quinn twist bone rotations without adding any mesh or FBX work yet. No roadmap task ID applies; this is a retargeting sprint gate.
  Affected areas: `src/core/retargeting/coordinate_normalizer.py`, `src/core/retargeting/baker.py`, `src/core/retargeting/__init__.py`, `tests/test_coordinate_normalizer.py`, `tests/test_baker.py`, `knowledge_base/retargeting/ground_truth/day3a_20260520_224749Z/`, `diagnostics/retargeting/day3a_baker_metrics.json`, `knowledge_base/retargeting/audits/baker_correctness_20260520.md`.
  Verification: MCP ground truth captured PMBAM, live S_Male01, Quinn, S_Male01->Quinn map, and PMBAM animation inventory before implementation; `python -m pytest tests\test_coordinate_normalizer.py tests\test_baker.py -v` passed with G5 inverse-bind max delta `6.661338147750939e-16` and identity WXYZ rotation delta `0.0`; `python -m pytest tests\test_sampler.py tests\test_retargeting_test_utils.py tests\test_mcp_retargeting_tools.py -q` passed. Day 3A issued no write commands against `C:\Program Files (x86)\Steam\steamapps\common\swkotor`; post-check timestamps in the live install remained older than the Day 3A implementation window.

- Added the Sprint 1 Day 2 fixed-rate retargeting sampler on top of the isolated stock corpus. The sampler resolves supermodel chains through the fixture corpus, samples local-space TRS arrays with rotations stored as WXYZ, records first-wins controller ownership for diagnostics, and calls the existing G5 `MatrixPaletteUploader` path once per sampled frame instead of introducing a parallel bake formula. The Day 2 audit also records that stock KOTOR clip frame zero is not guaranteed to equal the static bind pose, so tests assert parity with `AnimationEngine.evaluate(0.0)`.
  Affected areas: `src/core/retargeting/sampler.py`, `src/core/retargeting/__init__.py`, `tests/test_sampler.py`, `.gitignore`, `knowledge_base/retargeting/audits/sampler_g5_parity_20260520.md`.
  Verification: `python -m py_compile src\core\retargeting\sampler.py src\core\retargeting\__init__.py tests\test_sampler.py`; `python -m pytest tests\test_sampler.py tests\test_retargeting_test_utils.py tests\test_mcp_retargeting_tools.py -q`.

- Resolved the Day 1 design call on `custom_mixamo_a1`: it is classified as an external Patch Manager artifact, not a GhostRigger retarget output. GhostRigger now isolates retargeter tests from the live KOTOR install with a private stock corpus extracted directly from BIF/chitin resources, and tests can filter live-install external animation noise through `EXTERNAL_ANIMATION_PATTERNS`.
  Affected areas: `scripts/extract_stock_corpus.py`, `tests/fixtures/kotor_stock/README.md`, `tests/fixtures/kotor_stock/manifest.json`, `src/core/retargeting/test_utils.py`, `tests/test_retargeting_test_utils.py`, `.gitignore`, `knowledge_base/retargeting/audits/custom_mixamo_a1_forensics_20260520.md`.
  Verification: `python scripts\extract_stock_corpus.py --kotor-install "C:/Program Files (x86)/Steam/steamapps/common/swkotor" --game k1 --output tests/fixtures/kotor_stock/ --k1-version "/K1/k1_win_gog_swkotor.exe via Ghidra Odyssey repo; swkotor.exe x86:LE:32:default windows compiler; 24591 functions"` wrote 14 BIF-backed files; fixture `S_Male01` loads with 12 stock animations and no `custom_mixamo_a1`; live Override/Patch Manager timestamps were checked before mutation-sensitive work.

- Documented Sprint 1 Day 1 forensics for the `custom_mixamo_a1` animation seen in `S_Male01`. The audit confirms the clip is in the local K1 Override `s_male01.mdl`, not stock/core `S_Male01`, and records the companion `custom-animation-smoke-test.dll` provenance clue. No roadmap task ID applies; this is a retargeting sprint gate.
  Affected areas: `knowledge_base/retargeting/audits/custom_mixamo_a1_forensics_20260520.md`, `diagnostics/retargeting/custom_mixamo_a1/`.
  Verification: MCP extraction of Override and CHITIN `s_male01.mdl/.mdx`; GhostRigger loader inspection of both resources; `tools.model_inspector` JSON dump through the active game directory; local binary/text search for `custom_mixamo_a1`.

- Added the first read-only MCP surface for the animation retargeting system. The new tools inspect Aurora/KOTOR or bundled Quinn skeleton summaries, build deterministic retarget mapping reports, and list available KOTOR animations through the supermodel chain. Documented the Phase 0 retargeting gap audit, MCP API contract, initial Aurora supermodel ground-truth notes, mapping rules, test matrix, and quaternion convention guidance. Also corrected the Unreal retarget mapper so real KOTOR mesh-object-as-bone nodes such as `pelvis_g`, `torso_g`, `torsoUpr_g`, `rhand_g`, and `lhand_g` are eligible source skeleton nodes while skin/render meshes stay excluded. No roadmap task ID applies yet; this is groundwork for the bidirectional Aurora <-> Unreal retargeting roadmap.
  Affected areas: `src/kotormcp/tools/retargeting.py`, `src/kotormcp/tools/__init__.py`, `src/unreal/animation_retargeting.py`, `knowledge_base/retargeting/`, `tests/test_mcp_retargeting_tools.py`, `tests/test_core_contracts.py`.
  Verification: MCP ground truth checked `k1:s_male01`, `k1:s_male02`, `k1:s_female02`, and `k1:pmbam`; manual MCP smoke showed `pmbam -> S_Female02 -> S_Female01 -> S_Male02 -> S_Male01` with 268 available animations and `s_male02 -> Quinn` mapping core nodes (`pelvis_g`, `torso_g`, `torsoUpr_g`, `neck_g`, `rhand_g`, `lhand_g`, `rthigh_g`, `lfoot_g`). Ran `python -m py_compile src\kotormcp\tools\retargeting.py src\kotormcp\tools\__init__.py tests\test_mcp_retargeting_tools.py`; `python -m py_compile src\unreal\animation_retargeting.py tests\test_core_contracts.py`; `python -m pytest tests\test_mcp_retargeting_tools.py -q`; `python -m pytest tests\test_core_contracts.py -q -k "retarget or quinn"`.

- Added a modular generated lightmap baking system for the Qt lighting workflow. The baker validates existing UV/lightmap UV channels, rasterizes UV triangles into texel buffers, solves direct lighting from Aurora/dynamic/generated lights, supports optional shadow rays, applies padding/dilation, writes PNG/TGA/JPG textures plus a JSON manifest, runs through a Qt worker dialog, previews generated maps in the viewport without overwriting original KOTOR lightmaps, and exposes generated assignments to export sidecar metadata. No roadmap task ID applies.
  Affected areas: `src/gui/lighting/lightmap_*.py`, `src/gui/dialogs/qt_lightmap_baker_dialog.py`, `src/gui/panels/qt_lighting_panel.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`, `src/converters/mesh_converter.py`, `tests/test_lightmap_baker.py`.
  Verification: `python -m compileall src/gui/lighting src/gui/dialogs/qt_lightmap_baker_dialog.py src/gui/panels/qt_lighting_panel.py src/gui/windows/qt_main_window.py src/gui/viewports/qt_viewport.py src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py src/converters/mesh_converter.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/test_core_contracts.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: reduced generated bake washout by replacing the runaway point/area light `inverse-square * radius` term with smooth radius falloff and adding tone mapping before gamma, so clustered Aurora lights preserve gradients instead of clipping to white.
  Verification: `python -m py_compile src/gui/lighting/lightmap_lighting_solver.py tests/test_lightmap_baker.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: clarified UV validation severity so bakeable meshes with risky primary-UV fallback, out-of-range UVs, overlaps, or inverted winding show as `WARN` instead of misleading `OK`, and the bake warning text now flags primary UV fallback as potentially unsuitable for replacement lightmaps.
  Verification: `python -m py_compile src/gui/lighting/lightmap_uv_validator.py src/gui/dialogs/qt_lightmap_baker_dialog.py src/gui/lighting/lightmap_baker.py tests/test_lightmap_baker.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: changed scene-load defaults so inherited KOTOR lightmaps are disabled by default and the viewport starts in dynamic scene-lighting mode. The baker's extra ambient contribution is now opt-in so generated lightmaps are driven by actual scene light objects and mesh UVs unless the user explicitly adds ambient fill.
  Verification: `python -m py_compile src/gui/lighting/settings.py src/gui/lighting/lightmap_controller.py src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py src/gui/lighting/lightmap_bake_settings.py src/gui/dialogs/qt_lightmap_baker_dialog.py tests/test_core_contracts.py tests/test_lightmap_baker.py`; `pytest tests/test_lightmap_baker.py tests/test_core_contracts.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: vectorized the lightmap direct-light solve path so valid texels are lit in NumPy batches instead of per-texel Python loops. This keeps output equivalent to the scalar solver while reducing CPU overhead; shadow rays remain a separate CPU fallback until a UV-space GPU framebuffer baker is implemented.
  Verification: `python -m py_compile src/gui/lighting/lightmap_lighting_solver.py tests/test_lightmap_baker.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: added a ModernGL offscreen GPU direct-light backend for generated lightmap bakes, enabled by default through a new `GPU acceleration` dialog option. Direct lighting now uploads rasterized texel buffers through RAM to GPU textures, processes large light lists in chunks without dropping lights, accumulates the result before tone/gamma, and reports whether the GPU or CPU fallback was used. Optional shadow rays still run on the CPU and automatically bypass the GPU path when enabled.
  Verification: `python -m py_compile src/gui/lighting/lightmap_gpu_solver.py src/gui/lighting/lightmap_bake_settings.py src/gui/lighting/lightmap_bake_job.py src/gui/lighting/lightmap_baker.py src/gui/dialogs/qt_lightmap_baker_dialog.py tests/test_lightmap_baker.py`; `pytest tests/test_lightmap_baker.py -q`; `pytest tests/ -m "not slow" -x`; local bake probe confirmed `GPU direct-light solve used ModernGL backend 'default'` and produced non-flat output.

- Expanded the Aurora lighting workflow into a modular professional lighting system. Added editable light data/adapters, selection, grouping, generated rig presets, map/lightmap/shader controllers, viewport picking, light-aware gizmo scaling, export conversion records, persisted lighting settings, and a compact Light Explorer-style Qt panel with multi-selection/property editing. No roadmap task ID applies.
  Affected areas: `src/gui/lighting/`, `src/gui/panels/qt_lighting_panel.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/gizmo/transform_controller.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/rendering/viewport_core.py`, `src/gui/windows/qt_main_window.py`, `src/gui/qt_lib.py`.
  Verification: `python -m py_compile src/gui/lighting/__init__.py src/gui/lighting/light_types.py src/gui/lighting/light_model.py src/gui/lighting/aurora_light_adapter.py src/gui/lighting/light_manager.py src/gui/lighting/light_selection.py src/gui/lighting/light_grouping.py src/gui/lighting/light_picker.py src/gui/lighting/lighting_rig_presets.py src/gui/lighting/light_export_bridge.py src/gui/lighting/settings.py src/gui/panels/qt_lighting_panel.py src/gui/viewports/qt_viewport.py src/gui/gizmo/transform_controller.py src/gui/rendering/gpu_renderer.py src/gui/rendering/viewport_core.py src/gui/windows/qt_main_window.py src/gui/qt_lib.py`; `pytest tests/test_core_contracts.py -q -k "lighting or light_gizmos or transform_gizmo_controller"`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: changed viewport light helpers to fixed, restrained display geometry so large Aurora spot/point radii no longer cover the scene. Radius/cone volumes now default off and, when shown, use compact helper-size geometry while real light radius remains editable and still feeds shading/export data.
  Verification: `python -m py_compile src/gui/lighting/light_gizmo_renderer.py src/gui/lighting/settings.py src/gui/lighting/lighting_viewport_controller.py src/gui/panels/qt_lighting_panel.py src/gui/rendering/gpu_renderer.py src/gui/rendering/viewport_core.py`; `pytest tests/test_core_contracts.py -q -k "lighting or light_gizmos"`; `pytest tests/test_qt_only_imports.py -q`.

- Fixed the post-load GPU viewport black-L glitch by resetting cached offscreen GPU framebuffers when a new model is loaded and making the first post-load render use the fast path before the existing delayed HQ refresh. This targets the model-load render target state rather than QLabel pixmap scaling. No roadmap task ID applies.
  Affected areas: `src/gui/rendering/gpu_renderer.py`, `src/gui/viewports/qt_viewport.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/rendering/gpu_renderer.py src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_gpu_viewport_resets_render_targets_on_model_load tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes -q`; offscreen Qt viewport smoke load of `K1:n_bith` rendered through the GPU path.

- Refined the measurement/snap UI into a compact 3ds Max-style transform type-in strip with X/Y/Z fields, grid spacing entry, mode indicator, snap toggle, angle snap toggle/preset, and percent snap toggle/preset. Added `PercentSnap`, persisted snap/percent settings, applied numeric type-in transforms to real scene data, and integrated live percent snapping into scale gizmo drags. No roadmap task ID applies.
  Affected areas: `src/measurement/`, `src/gui/viewports/qt_transform_typein_bar.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/gizmo/transform_controller.py`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/qt_lib.py`, `tests/test_measurement.py`.
  Verification: `python -m py_compile src/measurement/percent_snap.py src/measurement/__init__.py src/measurement/unit_settings.py src/measurement/dimension_calculator.py tests/test_measurement.py`; `python -m py_compile src/gui/viewports/qt_transform_typein_bar.py src/gui/qt_lib.py src/gui/gizmo/transform_controller.py src/gui/viewports/qt_viewport.py src/gui/dialogs/qt_settings_dialog.py`; `pytest tests/test_measurement.py -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/test_core_contracts.py::test_transform_gizmo_controller_applies_translate_rotate_scale_and_cancel tests/test_core_contracts.py::test_qt_viewport_uses_profiled_navigation_actions tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a -q`.

- Added a central measurement system with 3ds Max-style system/display units, persistent measurement settings, active-unit grid labels, selected-object dimension readouts, a viewport distance tool, and configurable angle snapping for rotation gizmo drags. Display-unit changes update UI formatting without mutating scene data, and exporters remain documented as consuming raw system-unit transforms. No roadmap task ID applies.
  Affected areas: `src/measurement/`, `src/gui/dialogs/qt_settings_dialog.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `src/gui/panels/qt_properties_panel.py`, `src/gui/gizmo/transform_controller.py`, `src/gui/windows/qt_main_window.py`, `src/converters/mesh_converter.py`, `tests/test_measurement.py`.
  Verification: `python -m py_compile src/measurement/__init__.py src/measurement/unit_system.py src/measurement/unit_settings.py src/measurement/measurement_formatter.py src/measurement/angle_snap.py src/measurement/dimension_calculator.py src/measurement/grid_measurement.py src/measurement/measurement_controller.py tests/test_measurement.py`; `python -m py_compile src/gui/gizmo/transform_controller.py src/gui/rendering/viewport_core.py src/gui/viewports/qt_viewport.py src/gui/panels/qt_properties_panel.py src/gui/dialogs/qt_settings_dialog.py src/gui/windows/qt_main_window.py src/converters/mesh_converter.py`; `pytest tests/test_measurement.py -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/test_core_contracts.py::test_transform_gizmo_controller_applies_translate_rotate_scale_and_cancel tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a tests/test_core_contracts.py::test_qt_viewport_gpu_grid_is_native_and_xray_is_overlay_only -q`; `pytest tests/ -m "not slow" -x`.

- Added a modular Qt viewport transform gizmo with Translate, Rotate, and Scale modes. SPACE/Tab cycles the active mode; the overlay renders for both CPU and GPU viewport paths; gizmo picking takes priority only over visible handles; drags update the selected node/mesh data, with ESC cancel and undo/redo support including vertex-backed scale edits. No roadmap task ID applies.
  Affected areas: `src/gui/gizmo/`, `src/gui/qt_lib.py`, `src/gui/viewports/qt_viewport.py`, `tests/test_core_contracts.py`.
  Verification: `Get-ChildItem src/gui/gizmo/*.py | ForEach-Object { python -m py_compile $_.FullName }; python -m py_compile src/gui/qt_lib.py src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py -q`; `pytest tests/ -m "not slow" -q`.
  Follow-up: replaced the rotation gizmo's hard-coded 2D ellipses with camera-projected world-space axis rings, muted back halves, brighter front halves, and polyline-based ring picking so the rotation mode reads like a real viewport control instead of a flat overlay.
  Verification: `Get-ChildItem src/gui/gizmo/*.py | ForEach-Object { python -m py_compile $_.FullName }; python -m py_compile tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_transform_gizmo_controller_applies_translate_rotate_scale_and_cancel tests/test_core_contracts.py::test_gizmo_picker_hits_projected_rotation_polylines -q`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py -q`.
  Follow-up: inverted the gizmo rotation drag delta so X/Y/Z rotation handles apply the expected viewport handedness instead of rotating opposite the user's drag.
  Verification: `python -m py_compile src/gui/gizmo/transform_controller.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_transform_gizmo_controller_applies_translate_rotate_scale_and_cancel -q`.
  Follow-up: made the scale gizmo's center cube take pick priority over overlapping axis handles and highlight all three scale axes while hovered/active so uniform scale is visually explicit.
  Verification: `python -m py_compile src/gui/gizmo/gizmo_picker.py src/gui/gizmo/gizmo_renderer.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_gizmo_picker_prioritizes_uniform_scale_center_handle -q`.

- Added a first-pass Aurora lighting system for realistic viewport rendering. Binary light controllers now populate radius, color, and multiplier on `ModelNode` light nodes; the GPU shader can render Aurora-backed point, spot, directional, and area-style lights with distance falloff; the viewport exposes Scene Lit, Unlit, and Studio lighting modes plus per-map toggles for diffuse, lightmap, environment, specular, and normal map channels; and the main window has a dockable Lighting panel for inspecting and editing individual Aurora lights. No roadmap task ID applies.
  Affected areas: `src/core/model_data.py`, `src/core/kotor_loader.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/panels/qt_lighting_panel.py`, `src/gui/qt_lib.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`, `tests/test_qt_only_imports.py`, `tests/test_regression.py`.
  Verification: direct resource-manager load of `K1:m01aa_04a` confirmed 11 Aurora lights with real radius/multiplier/color values; offscreen GPU render smoke test for `K1:m01aa_04a` in Scene Lit and Unlit modes; `python -m py_compile src/gui/panels/qt_lighting_panel.py src/gui/qt_lib.py src/gui/windows/qt_main_window.py src/gui/rendering/gpu_renderer.py src/gui/viewports/qt_viewport.py src/core/kotor_loader.py src/core/model_data.py`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/ -x`.
  Follow-up: fixed detail-map routing so lightmap, environment, specular, and normal/bump maps are independent of the diffuse-map toggle. Module lightmaps now combine with Aurora scene lighting instead of being bypassed, and TXI/MDL bump maps bind to a GPU normal-map sampler.
  Verification: `python -m py_compile src/gui/rendering/gpu_renderer.py src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; offscreen GPU render smoke test for `K1:m01aa_03a` with Scene Lit and with diffuse disabled; `pytest tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint tests/test_core_contracts.py::test_qt_realistic_texture_prewarm_loads_detail_textures_without_paint_stall -q`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/ -x`.
  Follow-up: corrected GPU lightmap sampling to keep the full secondary UV array for expanded per-face VBOs and to write UV1 from `face_uvs` when a mesh uses texture-vertex indices. Module lightmaps now stay on the baked-lighting path instead of adding live Aurora scene lights on top of the baked map.
  Verification: inspected `K1:m01aa_01a` from the local game install to confirm real secondary UVs and lightmap textures; `python -m py_compile src/gui/rendering/gpu_renderer.py tests/test_regression.py`; `pytest tests/test_regression.py::test_vbo_expanded_path_uses_per_face_lightmap_uvs tests/test_regression.py::test_gpu_shader_exposes_lightmap_composite_modes -q`.
  Follow-up: replaced the hardcoded overbright lightmap default with dockable controls for lightmap intensity and contribution mode (`Baked`, `Phong`, `Emissive`), and added GPU wire gizmos for Aurora light nodes: point radius rings, spot cones, directional arrows/targets, and area rectangles.
  Verification: `python -m py_compile src/gui/rendering/gpu_renderer.py src/gui/viewports/qt_viewport.py src/gui/panels/qt_lighting_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py tests/test_regression.py`; `pytest tests/test_regression.py::test_gpu_shader_exposes_lightmap_composite_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint -q`; offscreen GPU render smoke test for `K1:m01aa_01a` with lightmaps and light gizmos enabled.
  Follow-up: removed automatic camera recentering from viewport selection so clicking or panel-selecting a node only changes selection/highlight state. Explicit framing still uses the current selection when invoked with `Z`/frame. Lighting editor refreshes now preserve the selected light instead of jumping back to the first light after a radius or property edit. No roadmap task ID applies.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/panels/qt_lighting_panel.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_selection_does_not_auto_recenter_but_z_frames_selection tests/test_core_contracts.py::test_qt_lighting_panel_editor_refresh_preserves_selected_light -q`.
  Follow-up: viewport clicks can now select Aurora light gizmos directly. Light selection syncs back to the Lighting panel without re-emitting a panel selection signal, so direct viewport selection and panel-based editing stay aligned. No roadmap task ID applies.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py src/gui/panels/qt_lighting_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_can_pick_light_gizmos tests/test_core_contracts.py::test_qt_lighting_panel_select_light_syncs_from_viewport_without_emitting tests/test_core_contracts.py::test_qt_lighting_panel_editor_refresh_preserves_selected_light tests/test_core_contracts.py::test_qt_viewport_selection_does_not_auto_recenter_but_z_frames_selection -q`.

- Explicitly releases GPU and RAM-side mesh buffers when clearing the current model or replacing it with another model/module. Model loading now drives the existing progress toast through file/resource read, parse, RAM buffer preparation, UI handoff, and GPU upload phases so long mesh loads happen behind the visible progress notification instead of looking frozen.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/gpu_renderer.py`, `tests/test_core_contracts.py`.
  Verification: `python -m py_compile src/gui/windows/qt_main_window.py src/gui/viewports/qt_viewport.py src/gui/rendering/gpu_renderer.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_gpu_static_mesh_prebuild_uses_ram_and_chunked_uploads tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: removed the remaining `Handing model to viewport` UI stall by reusing worker-prepared render bounds, deferring TXI metadata on big/module models, caching the `ResourceManager` instead of rebuilding it during handoff, and preventing the GPU paint path from synchronously decoding archive textures. A timing probe for `K1:m01aa_01a` reduced main-window handoff from about 5.2 seconds to about 0.03 seconds.
  Verification: `python -m py_compile src/gui/windows/qt_main_window.py src/gui/viewports/qt_viewport.py src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_gpu_static_mesh_prebuild_uses_ram_and_chunked_uploads tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes -q`; `pytest tests/ -m "not slow" -x`; manual offscreen timing probe for `K1:m01aa_01a`.
  Follow-up: restored realistic-mode texture streaming after the non-blocking handoff change. Background prewarm now gathers diffuse, lightmap, environment, specular, bump, and per-face texture names from renderable geometry nodes, refreshes the GPU viewport as texture batches become resident, and reruns after deferred TXI metadata reveals companion textures.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_realistic_texture_prewarm_loads_detail_textures_without_paint_stall tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild -q`; `pytest tests/test_core_contracts.py::test_gpu_static_mesh_prebuild_uses_ram_and_chunked_uploads tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_qt_realistic_texture_prewarm_loads_detail_textures_without_paint_stall -q`; `pytest tests/ -m "not slow" -x`.
  Follow-up: selection now drives the viewport orbit/zoom pivot. Selecting a mesh, clicked face, or node recenters the arcball target while preserving the current eye position, and `Z` frames the selected face/mesh/node bounds before falling back to full-model framing.
  Verification: `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`; `pytest tests/test_core_contracts.py::test_qt_viewport_selection_becomes_orbit_and_z_frame_pivot -q`; `pytest tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a tests/test_core_contracts.py::test_qt_viewport_context_menu_does_not_pick_on_right_click tests/test_core_contracts.py::test_qt_gpu_viewport_disables_gpu_culling_for_cpu_parity tests/test_core_contracts.py::test_qt_viewport_selection_becomes_orbit_and_z_frame_pivot tests/test_core_contracts.py::test_viewport_navigation_profiles_are_available -q`; `pytest tests/ -m "not slow" -x`.

- Reduced UI stalls during model/mesh loading by keeping local MDL reads in RAM, preparing static GPU mesh buffers on the loader thread, reusing those prebuilt arrays during GPU rendering, and chunking first-frame GPU buffer uploads so the Qt event loop can continue between batches. Editing a node now invalidates both the RAM-prebuilt mesh data and GPU buffers for the affected subtree. Restored the lazy `src.gui.gpu_renderer` compatibility alias used by the existing GPU contract test, and kept unknown `GHOSTRIGGER_SKIN_FORMULA` values on the documented G5 fallback instead of slipping into the no-qBone FBX fallback.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/__init__.py`, `src/core/gpu_skinning.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint tests/test_core_contracts.py::test_gpu_static_mesh_prebuild_uses_ram_and_chunked_uploads tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild -q`; `pytest tests/test_regression.py::test_skin_node_palette_env_switch_unknown_value_falls_back_to_G5 tests/test_regression.py::test_skin_node_palette_without_qbones_uses_hierarchy_bind tests/test_core_contracts.py::test_gpu_static_mesh_prebuild_uses_ram_and_chunked_uploads tests/test_core_contracts.py::test_model_load_worker_uses_single_read_and_gpu_prebuild -q`; `python -m py_compile src/core/gpu_skinning.py src/gui/__init__.py src/gui/windows/qt_main_window.py src/gui/viewports/qt_viewport.py src/gui/rendering/gpu_renderer.py tests/test_core_contracts.py`; `pytest tests/ -m "not slow" -x`.

## 2026-05-19

- Fixed K1 module room loading/render hardening for `m02aa_01a`. The model is
  valid game data (`effect`, 127 nodes, 56 meshes) but includes module geometry
  that may not carry ordinary character-style UV arrays, so the viewport render
  timer now catches render failures and falls back from GPU to CPU instead of
  letting the Qt event callback take down the app. Also repaired the MCP render
  helper's grouped-GUI import path so a freshly loaded MCP server can render
  through the current `src/gui/rendering` layout.
  Affected areas: `src/gui/viewports/qt_viewport.py`,
  `src/gui/rendering/gpu_renderer.py`, `src/kotormcp/tools/ghostrigger.py`,
  `src/kotormcp/tools/debug_materials.py`, `tests/test_regression.py`.
  Verification: MCP `ghostrigger_model_info`/`ghostrigger_audit` for
  `k1:m02aa_01a`; `python -m pytest tests/test_regression.py::test_gpu_vbo_handles_module_mesh_without_uvs tests/test_regression.py::test_k1_m02aa_01a_module_model_loads_and_renders_without_crashing -q`;
  `python -m pytest tests/test_qt_only_imports.py::test_gui_root_only_keeps_central_qt_lib tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`;
  direct local MCP handler render for `k1:m02aa_01a`; `python scripts/full_scan.py --game k1 --tier modules --category modules --max-models 10 --timeout 30`.
- Added authoritative KOTOR module location labels keyed by module filename and
  room-model stem, including K1 prefixless room models such as `m02aa_01a` and
  K2 room stems such as `201tel_01a`. The Game Library Module tab now enriches
  rows with `module_code`, `location`, `area_name`, and a human-readable
  `area_label`, uses those labels in the filter dropdown, and shows them in
  model list rows/tooltips.
  Affected areas: `src/core/module_categories.py`,
  `src/gui/panels/qt_library_panel.py`, `tests/test_module_categories.py`.
  Verification: `pytest tests/test_module_categories.py tests/test_qt_only_imports.py -q`;
  `python -m py_compile src/core/module_categories.py src/gui/panels/qt_library_panel.py tests/test_module_categories.py`.
- Added a dedicated `MODULE` Character Mode for area/module/tile/effect models. Module classifications now show a `MODULE` badge instead of `UNSUPPORTED`, with its own UI color and enum display/icon key.
  Affected areas: `src/core/model_data.py`, `src/gui/panels/qt_properties_panel.py`, `tests/test_character_mode.py`.
  Verification: `pytest tests/test_character_mode.py::test_detect_character_mode_synthetic tests/test_character_mode.py::test_character_mode_has_required_members -q`; `python -m py_compile src/core/model_data.py src/gui/panels/qt_properties_panel.py tests/test_character_mode.py`.
- Added module-mesh inspection and visibility controls for imported modules. The Properties panel now has a `Module Meshes` tab listing mesh name, vertex/face counts, texture, visibility, and mesh group; selecting a mesh row selects/highlights it in the viewport. The viewport now supports mesh picking, a 3ds Max-style right-click mesh menu for hide/unhide/hide-unselected/unhide-all plus selection set and mesh group creation, and GPU-only `Realistic`, `Shaded`, and `Flat` render modes. Hidden module meshes are skipped by both CPU and GPU render paths; gray-mesh auto-hide is intentionally left for the next classification pass now that mesh inspection exists.
  Affected areas: `src/gui/panels/qt_properties_panel.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/rendering/viewport_core.py`, `src/gui/rendering/gpu_renderer.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_selects_and_hides_meshes tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/viewports/qt_viewport.py src/gui/rendering/viewport_core.py src/gui/rendering/gpu_renderer.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`.
  Follow-up: `Realistic`, `Shaded`, and `Flat` now all keep diffuse texture sampling when Texture is enabled. Mesh selection now supports multi-select from the Properties list, Ctrl+A select-all in the viewport, front-facing triangle preference for click picking, and viewport drag-box selection for selecting multiple module meshes.
  Verification: `pytest tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_selects_and_hides_meshes tests/test_core_contracts.py::test_module_mesh_properties_panel_supports_multi_select_all tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/viewports/qt_viewport.py src/gui/rendering/gpu_renderer.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`.
  Follow-up: right-click no longer changes mesh selection or acts like a left-click picker. The context menu opens only when the cursor is over an already selected mesh, and hide/unhide actions operate on the current selected mesh set.
  Verification: `pytest tests/test_core_contracts.py::test_qt_viewport_context_menu_does_not_pick_on_right_click tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a -q`; `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`.
  Follow-up: hiding selected meshes now keeps the selected mesh set, and the context menu can open from an existing selection even if the clicked point is empty or the selected meshes are hidden. Right-click still does not pick or replace selection; it only acts on the current selected set.
  Verification: `pytest tests/test_core_contracts.py::test_qt_viewport_context_menu_does_not_pick_on_right_click -q`; `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`.
  Follow-up: the viewport right-click menu now opens even when no mesh is selected, keeping global actions such as `Unhide All` available. Selection-specific actions stay disabled until meshes are selected, and right-click still does not pick or replace selection.
  Verification: `pytest tests/test_core_contracts.py::test_qt_viewport_context_menu_does_not_pick_on_right_click -q`; `python -m py_compile src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`.
  Follow-up: the module mesh browser and viewport selection now include any node with real vertices and faces, even if the loader did not classify it as a formal mesh node. This exposes gray module/helper geometry in `Module Meshes` so it can be selected and hidden.
  Verification: `pytest tests/test_core_contracts.py::test_module_mesh_properties_panel_includes_geometry_nodes_outside_mesh_nodes tests/test_core_contracts.py::test_module_mesh_properties_panel_supports_multi_select_all tests/test_core_contracts.py::test_qt_viewport_exposes_mesh_multiselect_box_and_ctrl_a -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/viewports/qt_viewport.py tests/test_core_contracts.py`.
  Follow-up: the module geometry browser now separates normal `Meshes` from `Walkmeshes` in a nested tab control. Walkmesh/AABB geometry is still selectable and hideable, but no longer mixed into the normal module mesh list.
  Verification: `pytest tests/test_core_contracts.py::test_module_mesh_properties_panel_splits_meshes_and_walkmeshes tests/test_core_contracts.py::test_module_mesh_properties_panel_supports_multi_select_all -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py tests/test_core_contracts.py`.
  Follow-up: the module geometry browser now adds a `NULL Meshes` tab for geometry whose texture is empty, `NULL`, `none`, or `****`. These external/helper meshes remain selectable and hideable, but are no longer mixed into the normal `Meshes` tab.
  Verification: `pytest tests/test_core_contracts.py::test_module_mesh_properties_panel_splits_meshes_nulls_and_walkmeshes tests/test_core_contracts.py::test_module_mesh_properties_panel_supports_multi_select_all -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py tests/test_core_contracts.py`.
  Follow-up: added a detachable/dockable `Module Meshes` panel opened from `Modules -> Open Module Meshes`. It mirrors the module geometry browser, stays synchronized with viewport selection/visibility, and can float or dock like the Nodes, 2DA, and Resource panels.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_exposes_module_meshes_as_detachable_dock tests/test_core_contracts.py::test_module_mesh_properties_panel_splits_meshes_nulls_and_walkmeshes -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`.
  Follow-up: added an `Open Window` button inside the embedded Module Meshes tab and a right-click `Open Module Meshes Window` action on the mesh/NULL/walkmesh lists, so the detachable dock can be opened directly from the module browser.
  Verification: `pytest tests/test_core_contracts.py::test_module_mesh_panel_can_request_detachable_window tests/test_core_contracts.py::test_main_window_exposes_module_meshes_as_detachable_dock -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`.
  Follow-up: removed the `Module Meshes` tab from the regular Properties sidebar now that module geometry has a detachable/dockable panel. The dock remains synchronized with viewport mesh selection and visibility, while the sidebar returns to the simpler `General` properties view.
  Verification: `pytest tests/test_core_contracts.py::test_regular_properties_panel_can_omit_module_mesh_tab tests/test_core_contracts.py::test_module_mesh_properties_panel_lists_selects_and_hides_meshes tests/test_core_contracts.py::test_module_mesh_properties_panel_supports_multi_select_all tests/test_core_contracts.py::test_module_mesh_properties_panel_splits_meshes_nulls_and_walkmeshes tests/test_core_contracts.py::test_module_mesh_panel_can_request_detachable_window tests/test_core_contracts.py::test_main_window_exposes_module_meshes_as_detachable_dock -q`; `python -m py_compile src/gui/panels/qt_properties_panel.py src/gui/windows/qt_main_window.py tests/test_core_contracts.py`.
  Follow-up: module `Shaded` mode now uses a soft diffuse-texture-preserving shade instead of feeding textures through lightmap/spec/env detail maps. Module `Realistic` also uses that soft shaded path until explicit scene lights are available, while `Flat` remains raw diffuse texture. Detail maps remain reserved for non-module realistic rendering.
  Verification: `pytest tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint -q`; `python -m py_compile src/gui/rendering/gpu_renderer.py tests/test_core_contracts.py`.
  Follow-up: module Flat/Shaded modes now force room geometry into the opaque pass, ignore diffuse alpha for those modes, and keep selected module nodes two-sided/opaque. Shaded uses a stronger but still texture-preserving normal shade so it differs from Flat without triggering the slow transparent composite path.
  Verification: `pytest tests/test_core_contracts.py::test_gpu_renderer_exposes_module_render_modes_and_selection_tint tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes -q`; `python -m py_compile src/gui/rendering/gpu_renderer.py tests/test_core_contracts.py`.
- Added `CHEETSHEET.md` as the maintained reference for useful embedded Python terminal commands, starting with selected-model inspection, animation playback, animation override, viewport, and log examples. Updated `AGENTS.md` so future terminal helper changes also update the cheatsheet.
  Affected areas: `CHEETSHEET.md`, `AGENTS.md`, `CHANGES.md`.
  Verification: documentation-only change; reviewed the new cheatsheet commands against the current terminal helper bindings.
- Added Python terminal helper bindings for the main window's selected model and animation controls. The embedded terminal now exposes `window`, `main_window`, `viewport()`, `model()`, `selected_model()`, `animation_names()`, `select_animation(name)`, `play_animation(name, loop=None)`, `stop_animation()`, `seek_animation(percent)`, and `override_animation(target_name, source_name="", source_model=None)`.
  Affected areas: `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_exposes_animation_helpers_to_python_terminal tests/test_core_contracts.py::test_main_window_bottom_area_is_resizable_splitter -q`; `python -m compileall -q src\gui\windows\qt_main_window.py tests\test_core_contracts.py`.
- Fixed the Matrix background Aurebesh font lookup after GUI category grouping. It now loads AurebeshAF from the shared `src/gui/fonts/AurebeshAF` directory instead of the nonexistent `src/gui/assets/fonts/AurebeshAF` path.
  Affected areas: `src/gui/assets/qt_matrix_background.py`, `tests/test_qt_only_imports.py`.
  Verification: `pytest tests/test_qt_only_imports.py::test_matrix_background_uses_bundled_aurebesh_font_dir tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `python -m compileall -q src\gui\assets\qt_matrix_background.py tests\test_qt_only_imports.py`.
- Wrapped the main workspace and bottom Output Log/Python terminal area in a vertical splitter so the bottom area can be resized by dragging its divider, matching the left/right splitter behavior.
  Affected areas: `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_bottom_area_is_resizable_splitter tests/test_core_contracts.py::test_main_window_combines_animation_tabs_and_removes_builder_tab -q`; `python -m compileall -q src\gui\windows\qt_main_window.py tests\test_core_contracts.py`.
- Removed the embedded Builder tab from the main right-side tab stack because Character Builder already opens from Tools. Combined the former `Anims` and `Anim Lib` tabs into one `Animation Library` tab with internal `Current` and `Library` pages, while keeping the existing animation panel objects and callbacks intact. Blueprint module actions now open the standalone Blueprint Editor window instead of trying to select a removed tab.
  Affected areas: `src/gui/panels/qt_animation_panel.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_combines_animation_tabs_and_removes_builder_tab tests/test_core_contracts.py::test_main_window_moves_utility_tabs_to_tools_windows tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `python -m compileall -q src\gui\panels\qt_animation_panel.py src\gui\windows\qt_main_window.py tests\test_core_contracts.py`.
- Moved the Diagnostics, Texture, Normal, and Blueprint utility panels out of the main right-side tab stack. Diagnostics now opens as `Tools -> Diagnostics...`, Texture and Normal now share `Tools -> Texture Tool...`, and Blueprint Editor now opens as `Tools -> Blueprint Editor...`; existing `self.*_panel` references remain pointed at the embedded panels so current callbacks keep working.
  Affected areas: `src/gui/panels/qt_diagnostics_panel.py`, `src/gui/panels/qt_texture_panel.py`, `src/gui/windows/qt_blueprint_editor.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_moves_rig_panel_to_modules_window tests/test_core_contracts.py::test_main_window_moves_utility_tabs_to_tools_windows tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `python -m compileall -q src\gui\panels\qt_diagnostics_panel.py src\gui\panels\qt_texture_panel.py src\gui\panels\qt_normal_map_panel.py src\gui\windows\qt_blueprint_editor.py src\gui\windows\qt_main_window.py tests\test_core_contracts.py`.
- Moved the Rig UI out of the main right-side tab stack and into a standalone Rigging window opened from `Modules -> Open Rigging Window`. The main window keeps `self.rig_panel` pointed at the window's embedded panel so existing Auto-Rig, Remove Rigging, Weight Stats, and status update paths continue to work.
  Affected areas: `src/gui/panels/qt_rig_panel.py`, `src/gui/windows/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_main_window_moves_rig_panel_to_modules_window tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `python -m compileall -q src\gui\panels\qt_rig_panel.py src\gui\windows\qt_main_window.py tests\test_core_contracts.py`.
- Split the bottom Output Log panel into a horizontal log/terminal workspace. The existing log stays on the left, and the right side now hosts an embedded Python terminal with command history, Run/Copy/Clear controls, stdout/stderr capture, expression display, and access to Qt plus the log panel context.
  Updated follow-up: moved the Output Log Save/Copy/Clear controls into a footer under the log pane, and moved the Python Run/Copy/Clear controls into a footer under the terminal pane, so each pane owns its own buttons.
  Affected areas: `src/gui/panels/qt_log_panel.py`, `tests/test_qt_only_imports.py`.
  Verification: `pytest tests/test_qt_only_imports.py::test_log_panel_embeds_python_terminal -q`; `python -m compileall -q src\gui\panels\qt_log_panel.py tests\test_qt_only_imports.py`.
- Restored the original joint-dot fill palette while keeping the new key-joint marking as a blue accent ring instead of replacing the dot color. Also added `shin` to the side-name palette so KotOR lower-leg names such as `lshin_g` keep their side color.
  Affected areas: `src/gui/viewports/qt_viewport.py`, `tests/test_joint_dot_overlay.py`.
  Verification: `pytest tests/test_joint_dot_overlay.py::test_t401_color_constants_match_spec tests/test_joint_dot_overlay.py::test_t401_accurig_left_names_classify_red tests/test_joint_dot_overlay.py::test_t401_accurig_right_names_classify_green tests/test_joint_dot_overlay.py::test_t401_tokenised_side_naming tests/test_joint_dot_overlay.py::test_t401_center_spine_names_classify_cyan tests/test_joint_dot_overlay.py::test_t401_default_center_classification tests/test_joint_dot_overlay.py::test_t401_hip_is_center_not_side tests/test_joint_dot_overlay.py::test_t401_key_joints_keep_original_fill_palette tests/test_joint_dot_overlay.py::test_t401_key_joint_names_are_marked_for_blue_accent tests/test_joint_dot_overlay.py::test_t401_non_key_joints_keep_original_palette -q`; `python -m compileall -q src\gui\viewports\qt_viewport.py tests\test_joint_dot_overlay.py`.
- Added a `HUMANOID` character mode for full humanoid NPC/body models that have a humanoid skeleton and their own visible head/facial rig, so models such as Bith-style NPCs and Calo Nord-style bodies no longer classify as `HEAD`. The Character Builder toolbar, property badge, workflow rail, scene priority, and viewport camera preset now understand the new mode. Also narrowed blue joint-dot highlighting to anatomical key joints only (head, neck, shoulder/clavicle, spine/torso, elbow/forearm, hand, knee/shin, and foot), while ordinary roots, pelvis, thighs, fingers, and other nodes keep the original center/side palette.
  Affected areas: `src/core/model_data.py`, `src/gui/viewports/qt_viewport.py`, `src/gui/panels/qt_character_builder_panel.py`, `src/gui/panels/qt_properties_panel.py`, `src/gui/panels/qt_workflow_rail.py`, `tests/test_character_mode.py`, `tests/test_joint_dot_overlay.py`.
  Verification: `pytest tests/test_character_mode.py::test_detect_character_mode_synthetic tests/test_character_mode.py::test_character_mode_has_required_members tests/test_joint_dot_overlay.py::test_t401_color_constants_match_spec tests/test_joint_dot_overlay.py::test_t401_accurig_left_names_classify_red tests/test_joint_dot_overlay.py::test_t401_accurig_right_names_classify_green tests/test_joint_dot_overlay.py::test_t401_key_joints_classify_blue tests/test_joint_dot_overlay.py::test_t401_non_key_joints_keep_original_palette tests/test_joint_dot_overlay.py::test_t401_hip_is_center_not_side -q`; `pytest tests/test_character_mode.py tests/test_joint_dot_overlay.py tests/test_head_workflow.py tests/test_headless_body_workflow.py tests/test_head_inspector.py tests/test_head_viseme_panel.py tests/test_head_phoneme_panel.py tests/test_head_camera_preset.py -q`; `pytest tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `python -m compileall -q src\core\model_data.py src\gui\viewports\qt_viewport.py src\gui\panels\qt_properties_panel.py src\gui\panels\qt_character_builder_panel.py src\gui\panels\qt_workflow_rail.py tests\test_character_mode.py tests\test_joint_dot_overlay.py`.
- Fixed Qt icon asset resolution after GUI category grouping. The main window, shared theme, and icon manager now resolve tab arrows and PNG icons from the shared `src/gui/icons` directory instead of looking for nonexistent `windows/icons` or `assets/icons` folders, and the Qt import guard now checks those paths.
  Affected areas: `src/gui/windows/qt_main_window.py`, `src/gui/assets/qt_theme.py`, `src/gui/assets/qt_icon_manager.py`, `tests/test_qt_only_imports.py`.
  Verification: `pytest tests/test_qt_only_imports.py::test_qt_icon_paths_resolve_after_gui_grouping -q`; `python -m compileall -q src\gui\assets\qt_theme.py src\gui\assets\qt_icon_manager.py src\gui\windows\qt_main_window.py tests\test_qt_only_imports.py`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button -q`.
- Rerouted the remaining application entry points, scripts, IPC reload defaults, and texture-loading helper imports through the central `src.gui.qt_lib` facade. `main.py` now launches via `src.gui.qt_lib.windows.qt_main_window`, diagnostic/render scripts use `src.gui.qt_lib.rendering.*`, and the facade aliases now support `importlib.reload()` for hot-reload workflows. Updated `AGENTS.md` and added a repository import guard so deleted root GUI modules are not reintroduced by runnable code.
  Affected areas: `main.py`, `scripts/`, `src/ipc/server.py`, `src/core/kotor_install.py`, `src/gui/qt_lib.py`, `src/gui/rendering/gpu_renderer.py`, `tests/test_qt_only_imports.py`, `AGENTS.md`.
  Verification: `python -m compileall -q main.py scripts src\gui src\ipc src\core\kotor_install.py tests\test_qt_only_imports.py`; `python main.py --help`; imported and reloaded representative `src.gui.qt_lib` facade modules with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_qt_only_imports.py::test_application_imports_use_central_qt_lib -q`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose -q`.
- Replaced the temporary root-level `src/gui/*.py` compatibility shim pile with a single central `src/gui/qt_lib.py` facade. The root `src/gui` Python files are now only `qt_lib.py` and `__init__.py`, while the implementation modules remain in category folders. Application and test imports were rerouted through `src.gui.qt_lib.<category>.<module>`, and the Qt import guard now fails if root shim modules return.
  Affected areas: `src/gui/qt_lib.py`, `src/gui/`, `src/kotormcp/tools/`, `tests/`, `src/gui/README.md`.
  Verification: `python -m compileall -q src\gui tests\test_qt_only_imports.py tests\test_core_contracts.py tests\test_regression.py tests\test_joint_dot_overlay.py`; imported representative `src.gui.qt_lib` modules with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_qt_only_imports.py -q`; `pytest tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose -q`; `pytest tests/test_joint_dot_overlay.py::test_t403_thumbnail_visibility_is_opt_in tests/test_joint_dot_overlay.py::test_t405_gradient_endpoints tests/test_regression.py::test_skin_node_palette_restores_3f_qbone_tbone_path tests/test_regression.py::test_skin_node_palette_without_qbones_uses_hierarchy_bind tests/test_head_camera_preset.py tests/test_head_viseme_panel.py tests/test_head_phoneme_panel.py tests/test_head_inspector.py -q`.
- Added the top-level `qt_lib` facade package so organized GUI modules can be imported as `qt_lib.<category>.<module>`, for example `qt_lib.panels.qt_common_panels`. Updated grouped `src/gui` implementation modules to cross-import through `qt_lib`, documented the convention, and added Qt-only guard tests that fail if grouped GUI code goes back to `src.gui.*` or `gui.*` root-shim imports.
  Affected areas: `qt_lib/`, `src/gui/`, `tests/test_qt_only_imports.py`, `src/gui/README.md`.
  Verification: imported representative `qt_lib` and `src.gui` paths with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_qt_only_imports.py::test_grouped_gui_modules_use_qt_lib_imports tests/test_qt_only_imports.py::test_qt_lib_facade_imports_grouped_modules -q`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose -q`; `python -m compileall -q qt_lib src\gui tests\test_qt_only_imports.py`.
- Organized `src/gui` into category subpackages for assets, dialogs, panels, rendering, textures, viewports, and windows. Root-level compatibility shims remain so existing imports keep working, including private helper imports used by diagnostics and tests. Updated the Qt-only import guard to recurse through the new subpackages and documented the layout in `src/gui/README.md`.
  Affected areas: `src/gui/`, `tests/test_qt_only_imports.py`, `src/gui/README.md`.
  Verification: imported old and new GUI module paths with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_qt_only_imports.py tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose -q`; `pytest tests/test_joint_dot_overlay.py::test_t403_thumbnail_visibility_is_opt_in tests/test_joint_dot_overlay.py::test_t405_gradient_endpoints -q`; `python -m compileall -q src\gui tests\test_qt_only_imports.py`.
- Changed GPU wireframe rendering to use a 3ds Max-style cyan-blue line color instead of inheriting each mesh's material or texture color. The fragment shader now has an explicit wireframe color path used by wire-only and GPU wire overlay passes.
  Affected areas: `src/gui/gpu_renderer.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes -q`; `python -m compileall -q src\gui\gpu_renderer.py tests\test_core_contracts.py`.
- Kept Qt viewports on the GPU for wireframe and texture-disabled display modes instead of falling back to CPU rendering. The GPU renderer now tracks `show_solid` and `show_texture`, skips texture sampling when textures are disabled, uses GPU wireframe mode for wire views, and the Qt overlay no longer repaints wireframe through the CPU on top of a GPU frame.
  Affected areas: `src/gui/qt_viewport.py`, `src/gui/gpu_renderer.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_qt_gpu_viewport_uses_overlay_not_cpu_textured_fallback tests/test_core_contracts.py::test_qt_gpu_viewport_keeps_gpu_for_wire_and_texture_off_modes tests/test_core_contracts.py::test_gpu_renderer_supports_texture_off_and_wireframe_modes tests/test_core_contracts.py::test_qt_gpu_viewport_disables_gpu_culling_for_cpu_parity tests/test_core_contracts.py::test_unreal_animator_uses_gpu_during_animation_preview -q`; `python -m compileall -q src\gui\qt_viewport.py src\gui\gpu_renderer.py tests\test_core_contracts.py`.
- Fixed Unreal Animator target animation preview for imported Quinn FBX skins by making qBone-less GPU skin meshes use hierarchy inverse-bind matrices instead of identity qBone/tBone fallbacks, allowing imported world-space skin meshes to use LBS when an animation pose is active, and forcing both Unreal Animator viewports onto GPU while animation preview is playing before restoring the user's previous GPU/CPU choices on Stop.
  Affected areas: `src/core/gpu_skinning.py`, `src/gui/viewport_core.py`, `src/gui/qt_unreal_animator.py`, `tests/test_core_contracts.py`, `tests/test_regression.py`.
  Verification: `pytest tests/test_regression.py::test_skin_node_palette_restores_3f_qbone_tbone_path tests/test_regression.py::test_skin_node_palette_without_qbones_uses_hierarchy_bind tests/test_core_contracts.py::test_unreal_animator_uses_gpu_during_animation_preview tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose -q`; `pytest tests/test_core_contracts.py::test_unreal_animator_inserts_synthetic_spine_between_pelvis_and_torso tests/test_core_contracts.py::test_unreal_animator_inserts_synthetic_spine_when_pelvis_and_torso_share_rootdummy tests/test_core_contracts.py::test_unreal_animator_repositions_existing_spine_between_pelvis_and_torso tests/test_core_contracts.py::test_unreal_animator_source_bone_browser_lists_and_selects_spine tests/test_core_contracts.py::test_unreal_animator_manually_adds_and_deletes_source_synthetic_bones tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose tests/test_core_contracts.py::test_unreal_animator_uses_gpu_during_animation_preview -q`.
- Added a developer-facing Unreal Animator `Reload Code` button and `Ctrl+Shift+R` action that asks the Qt main window to reload the Unreal Animator Python modules, rebuild the window, restore its geometry, refresh the supermodel library, and reload the last selected source supermodel when available.
  Affected areas: `src/gui/qt_unreal_animator.py`, `src/gui/qt_main_window.py`, `tests/test_core_contracts.py`.
  Verification: `pytest tests/test_core_contracts.py::test_unreal_animator_exposes_reload_code_button tests/test_core_contracts.py::test_unreal_animator_manually_adds_and_deletes_source_synthetic_bones -q`; `python -m compileall -q src\gui\qt_unreal_animator.py src\gui\qt_main_window.py tests\test_core_contracts.py`.
- Added Unreal Animator source-supermodel preparation that dynamically inserts or repositions a `spine_g` joint between `pelvis_g` and `torso_g` when a KotOR animation supermodel lacks that real intermediary. Added a KotOR source bone browser tab so source bones, including the synthetic `spine_g`, can be inspected and selected. Updated Unreal aliasing so `spine_g` maps to Unreal `spine_01` and `torso_g` maps higher in the spine chain.
  Affected areas: `src/gui/qt_unreal_animator.py`, `src/unreal/animation_retargeting.py`, `tests/test_core_contracts.py`.
  Updated follow-up: the source-supermodel spine repair now also handles real KotOR layouts where `pelvis_g` and `torso_g` are siblings under a hidden `rootdummy`, rewiring the visible path to `pelvis_g -> spine_g -> torso_g` while keeping `rootdummy` hidden.
  Corrected follow-up: the KotOR source skeleton remains authoritative. The Unreal Animator no longer synthesizes the full 89-bone Quinn skeleton or hides KotOR-only source bones. It now removes stale broad Quinn synthetic nodes automatically, keeps `spine_g` as the known automatic source fix, and exposes manual `Add Synthetic`, `Insert Before`, and `Delete Synthetic` controls in the source bone browser. Manual synthetic deletion reparents children back to the KotOR parent while preserving world placement. Selecting an animation still arms frame-zero preview immediately.
  Verification: `pytest tests/test_core_contracts.py::test_unreal_animator_inserts_synthetic_spine_between_pelvis_and_torso tests/test_core_contracts.py::test_unreal_animator_inserts_synthetic_spine_when_pelvis_and_torso_share_rootdummy tests/test_core_contracts.py::test_unreal_animator_repositions_existing_spine_between_pelvis_and_torso tests/test_core_contracts.py::test_unreal_animator_source_bone_browser_lists_and_selects_spine tests/test_core_contracts.py::test_unreal_animator_manually_adds_and_deletes_source_synthetic_bones tests/test_core_contracts.py::test_unreal_animator_animation_selection_arms_preview_pose tests/test_core_contracts.py::test_quinn_aliases_map_common_kotor_supermodel_bones tests/test_core_contracts.py::test_unreal_bone_map_excludes_dummy_and_hook_helpers -q`; `python -m compileall -q src\gui\qt_unreal_animator.py src\unreal\animation_retargeting.py tests\test_core_contracts.py`; loaded a shown `QtUnrealAnimatorWindow` with `QT_QPA_PLATFORM=offscreen` and confirmed the manual synthetic controls exist, KotOR-only `eyelid` remains visible, a manually inserted source spacer appears between `torso_g` and `neck_g`, and selecting `taunt` arms preview.
- Moved the Unreal Animator KotOR source supermodel HUD message to a bottom-centered readable panel and split the message into cleaner lines. Added a scoped viewport setting so the target viewport and other windows keep their default placement.
  Affected areas: `src/gui/viewport_core.py`, `src/gui/qt_viewport.py`, `src/gui/qt_unreal_animator.py`.
  Verification: rendered a no-geometry animation supermodel probe; `python -m compileall -q src\gui\viewport_core.py src\gui\qt_viewport.py src\gui\qt_unreal_animator.py src\unreal\animation_retargeting.py`; constructed `QtUnrealAnimatorWindow` with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_core_contracts.py::test_unreal_bone_map_excludes_dummy_and_hook_helpers tests/test_core_contracts.py::test_quinn_aliases_map_common_kotor_supermodel_bones tests/test_joint_dot_overlay.py::test_t403_thumbnail_visibility_is_opt_in -q`.
- Excluded all Unreal Animator `*dummy` and `*hook` helper nodes from Unreal bone mapping and from the Unreal Animator viewport bone overlay/hit-test cache, because they are KotOR null/helper nodes rather than deforming bones.
  Affected areas: `src/unreal/animation_retargeting.py`, `src/gui/viewport_core.py`, `src/gui/qt_viewport.py`, `src/gui/qt_unreal_animator.py`, `tests/test_core_contracts.py`.
  Updated follow-up: hidden helpers also cannot remain selected or draw a transform gimbal, and the overlay regression covers a `pelvis_g -> rootdummy -> torso_g` chain so the visible skeleton continues through real joints.
  Verification: `pytest tests/test_core_contracts.py::test_unreal_bone_map_excludes_dummy_and_hook_helpers tests/test_core_contracts.py::test_unreal_viewport_hides_dummy_and_hook_helpers_from_bone_overlay tests/test_core_contracts.py::test_unreal_viewport_hidden_helper_selection_does_not_draw_gimbal tests/test_core_contracts.py::test_quinn_aliases_map_common_kotor_supermodel_bones -q`; `python -m compileall -q src\gui\viewport_core.py src\gui\qt_viewport.py src\gui\qt_unreal_animator.py src\unreal\animation_retargeting.py`; constructed `QtUnrealAnimatorWindow` with `QT_QPA_PLATFORM=offscreen`.
- Added an Unreal-themed interface style scoped to the Unreal Animator window only, including dark editor-style panels, blue accents, focused list/tree/input states, styled timeline sliders, and local panel object names/properties.
  Affected area: `src/gui/qt_unreal_animator.py`.
  Verification: `python -m compileall -q src\gui\qt_unreal_animator.py src\gui\qt_main_window.py src\gui\qt_viewport.py`; constructed `QtUnrealAnimatorWindow` with `QT_QPA_PLATFORM=offscreen`; `pytest tests/test_joint_dot_overlay.py::test_t403_thumbnail_visibility_is_opt_in -q`.
- Renamed the Unreal retargeting UI from Unreal Workbench to Unreal Animator in the Qt main-window action, window class/file, viewport subclass/role, and focused viewport test.
  Affected areas: `src/gui/qt_main_window.py`, `src/gui/qt_unreal_animator.py`, `src/gui/qt_viewport.py`, `tests/test_joint_dot_overlay.py`.
  Verification: `pytest tests/test_joint_dot_overlay.py::test_t403_thumbnail_visibility_is_opt_in -q`; `python -m compileall -q src\gui\qt_unreal_animator.py src\gui\qt_main_window.py src\gui\qt_viewport.py`; imported `QtUnrealAnimatorWindow` and `QtGhostRiggerMainWindow` with `QT_QPA_PLATFORM=offscreen`.
- Added `CHANGES.md` as the project-level record for completed fixes and changes.
- Updated `AGENTS.md` to require changelog updates after successful work.
