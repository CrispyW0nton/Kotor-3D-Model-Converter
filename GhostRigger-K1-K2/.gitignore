# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
*.whl
.Python
venv/
.venv/
env/

# ── Jupyter / IDE ─────────────────────────────────────────────────────────────
.ipynb_checkpoints/
.vscode/
.idea/
*.swp
*~

# ── Game data (large extracted game files – not distributed) ──────────────────
game_data/
*.erf
*.mod
*.rim
*.bif
*.key
*.mdx

# ── Audit / render output (generated artefacts) ───────────────────────────────
audit_output/
logs/
# Track summary reports (JSON/txt) but not renders/sheets
!audit_output/batch_render/results_merged.json
!audit_output/batch_render/summary_merged.txt
!audit_output/batch_render/visual_audit.json

# ── One-off debug / prototype render PNGs at repo root ────────────────────────
audit_c_*.png
audit_n_*.png
audit_p_*.png
bantha_*.png
c_*.png
phk47_*.png
ckrayt*.png
chssiss*.png
screenshot*.png

# ── Temp / scratch directories ────────────────────────────────────────────────
lava_exercise/
KotorModTools/
screenshots/
portfolio_post.md

# ── Research / reference sources (large external repos) ──────────────────────
research_sources/
# External libraries checked out alongside the project
../PyKotor/
../render_check/

# ── Demo / capture artefacts ──────────────────────────────────────────────────
*.mp4
capture_demo_video.py

# ── Log files ─────────────────────────────────────────────────────────────────
Logs/

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Random PNG dumps in repo root ─────────────────────────────────────────────
*.png
# But keep PNGs inside research_sources and assets
!research_sources/**/*.png
!assets/**/*.png

# ── PyKotor embedded checkout (checked in to /home/user/webapp/PyKotor) ───────
PyKotor/
render_check/
