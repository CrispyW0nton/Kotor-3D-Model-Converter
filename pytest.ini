[pytest]
# Only collect tests from the tests/ directory.
# The project root contains diagnostic/scratch scripts (test_bantha_hang.py)
# that are not part of the test suite and should not be auto-collected.
testpaths = tests
